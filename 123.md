「フォルダーの一覧」の設定はこれで正しいです(サイト=JP CMS、ファイル識別子=Agent連携用フォルダ)。表示名を「**票ファイル一覧**」に変更してから、次に進みます。

**Step 3: アレイのフィルター処理(今開いているパネルから)**

1. 「データ操作」→「**アレイのフィルター処理**」→ 表示名「**該当CSP票抽出**」
2. From: fx → `body('票ファイル一覧')`(SharePoint版は配列がそのまま本文で返るので `?['value']` なし。もしテスト時に「配列ではない」エラーが出たら `body('票ファイル一覧')?['value']` に変更)
3. 左辺: fx → 次を貼付:

```
and(not(item()?['IsFolder']), contains(item()?['Name'], concat('アーキテクチャレビュー票(', triggerBody()?['text'], ')')), endsWith(item()?['Name'], '.md'))
```

4. 演算子「等しい」/ 右辺 `true`

**Step 4: 条件「判定:該当なし」**

- コントロール「条件」→ 左辺 fx `length(body('該当CSP票抽出'))` / 等しい / `0`
- **True側**: 「エージェントに応答する」→ 出力 `reviewSheetMaster`(説明: 該当CSPのアーキテクチャレビュー票マスタの全文(md))=`該当CSPのアーキテクチャレビュー票mdが見つかりませんでした。CSP指定を確認してください。`/ 出力 `fileName`(説明: 取得したマスタのファイル名(テンプレ版数の確認用))=`なし`

**Step 5: False側**

1. データ操作「作成」→ 表示名「**対象ファイルID**」/ 入力 fx: `first(body('該当CSP票抽出'))?['{Identifier}']`
2. データ操作「作成」→ 表示名「**対象ファイル名**」/ 入力 fx: `first(body('該当CSP票抽出'))?['Name']`
3. 検索「ファイル コンテンツ」→ **SharePoint「ファイル コンテンツの取得」**(パスによる〜ではない方)→ サイト=JP CMS / ファイル識別子= fx `outputs('対象ファイルID')` → 表示名「**票ファイル取得**」
4. 「エージェントに応答する」→ 出力2つ(**名前・説明はTrue側と一字一句同じ**):
   - `reviewSheetMaster` = fx `base64ToString(body('票ファイル取得')?['$content'])`(ツール②で「ファイル コンテンツ」チップ直指定が正解だった場合はそれに合わせる)
   - `fileName` = fx `outputs('対象ファイル名')`

**Step 6**: 両応答の非同期応答オフ → 保存 → フロー名「アーキテクチャレビュー票取得」→ 公開 → 子Cへ接続。

Step 3まで組んだら一度**保存→テスト実行(CSP=AWS)**して、「票ファイル一覧」の出力の先頭1件でキー名が `Name` / `{Identifier}` / `IsFolder` になっているか確認してください。違っていたら式のキーをそれに合わせます(ツール①のときと同じ要領です)。