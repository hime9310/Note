[
  {
    "No": 1,
    "環境": "DCS共通",
    "対応区分": "回答必須",
    "カテゴリ": "ネットワーク",
    "指摘概要": "サンプル概要",
    "指摘内容": "サンプル内容",
    "参考資料": "サンプルURL"
  }
]

```json
[{"No":1,"ステータス":"New","環境":"DCS共通","対応区分":"回答必須","カテゴリ":"NW","指摘概要":"テスト概要1","指摘内容":"テスト内容1","対応ガイドライン・参考資料":"-"},{"No":2,"ステータス":"New","環境":"DCS Hub","対応区分":"参考","カテゴリ":"セキュリティ","指摘概要":"テスト概要2","指摘内容":"テスト内容2","対応ガイドライン・参考資料":"-"}]

```


```json
json(concat('{"No":"', item()?['No'], '","ステータス":"', item()?['ステータス'], '","環境":"', item()?['環境'], '","対応区分":"', item()?['対応区分'], '","カテゴリ":"', item()?['カテゴリ'], '","指摘概要":"', item()?['指摘概要'], '","指摘内容":"', item()?['指摘内容'], '","対応ガイドライン・参考資料":"', item()?['対応ガイドライン・参考資料'], '"}'))

```


```typescript
function main(workbook: ExcelScript.Workbook, rowsJson: string) {
  // 行データ(JSON文字列)を配列に変換
  const rows = JSON.parse(rowsJson) as Record<string, string>[];
  if (rows.length === 0) { return 0; }

  // 対象テーブルを取得
  const table = workbook.getTable("T_アーキテクチャレビュー票");
  if (!table) { throw new Error("テーブル T_アーキテクチャレビュー票 が見つかりません"); }

  // テーブルのヘッダー順を取得し、その順番でデータを並べる
  const headers = table.getHeaderRowRange().getValues()[0] as string[];

  const values: string[][] = rows.map(row =>
    headers.map(h => (row[h] !== undefined && row[h] !== null) ? String(row[h]) : "")
  );

  // まとめて追記
  table.addRows(-1, values);

  return values.length;
}

```

