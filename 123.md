

| 項目 | 設定 |
|---|---|
| From | **fx** → `body('添付ファイル一覧取得')` |
| 左辺 | **fx** → `item()?['Name']` |
| 演算子 | **次の値を含む** |
| 右辺 | **fx** → `triggerBody()?['text']` |

これはツール①のスレッド検索と**まったく同じ形**(文字列 contains 文字列)なので、型の問題が起きません。

**必ず「保存」してから**テスト実行し、フィルターの出力件数を確認してください。

---

**1件になったら: 2段目のフィルターで絞り込み**

RITM番号だけだと構成図など他の添付も拾う可能性があるので、フィルターをもう1つ足します。

1. 「回答済みのアーキテクチャレビュー票取得」の**直後**に「アレイのフィルター処理」を追加 → 表示名「**xlsx絞り込み**」

| 項目 | 設定 |
|---|---|
| From | **fx** → `body('回答済みのアーキテクチャレビュー票取得')` |
| 左辺 | **fx** → `item()?['Name']` |
| 演算子 | **次の値を含む** |
| 右辺 | `.xlsx` ← そのまま文字入力 |

2. 「条件」の左辺を **fx** → `length(body('xlsx絞り込み'))` に変更
3. 「対象ファイル」を **fx** → `last(sort(body('xlsx絞り込み'), 'LastModified'))` に変更

