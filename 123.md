**1. 表示名の候補**

| 候補 | 補足 |
|---|---|
| **アーキテクチャレビュー票Excel絞り込み** | 推奨。何で絞るかが名前だけで分かる |
| アーキテクチャレビュー票のみ抽出 | シンプル |
| 対象ファイル絞り込み | 汎用的だが「何を」が分からない |

1段目が「回答済みのアーキテクチャレビュー票取得」なので、対で **1段目=RITM番号で絞り込み / 2段目=アーキテクチャレビュー票Excel絞り込み** に揃えると読みやすいです(1段目を変える場合は後続の `body('...')` 参照も直してください)。

---

**2. 今の「ファイル特定」は0件になります**

コードビューがこうなっています:

```
"where": "@contains('item()?['Name']','アーキテクチャレビュー票')"
```

`item()?['Name']` が**クォートで囲まれた文字列リテラル**になっており、「`item()?['Name']` という文字列」を検索しに行ってしまっています。1段目は `Name` の動的コンテンツを選んだので正しく入っていますが、2段目は**手打ちしたテキスト**として入っています。

---

**3. .xlsx も同じ1つの条件に入れられます**

左辺を fx にして、次の式を入れてください。

```
if(endsWith(item()?['Name'], '.xlsx'), item()?['Name'], '')
```

| 項目 | 設定 |
|---|---|
| From | **fx** → `body('回答済みのアーキテクチャレビュー票取得')` (現状のままでOK) |
| 左辺 | **fx ボタンから**上の式を入力 ← 手打ちテキストではダメ |
| 演算子 | **含む** |
| 右辺 | `アーキテクチャレビュー票` ← そのまま文字入力 |

**動き**: xlsxなら「ファイル名そのもの」、xlsx以外なら「空文字」を左辺にします。空文字は何も含まないので自動的に除外され、**拡張子とキーワードの両方を1条件でANDできます**。これで3段目は不要です。

---

**4. 後続の参照先**

最終段の名前に合わせて2箇所を直してください(名前を「アーキテクチャレビュー票Excel絞り込み」にした場合):

| アクション | 設定 |
|---|---|
| 条件 の左辺 | `length(body('アーキテクチャレビュー票Excel絞り込み'))` |
| 対象ファイル | `last(sort(body('アーキテクチャレビュー票Excel絞り込み'), 'LastModified'))` |

