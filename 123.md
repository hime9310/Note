コードビューが編集不可だった場合の手順です。**目視で差分を探すより、両方を消して入れ直す方が確実**です。

**一致していなければならない項目**

| 項目 | 一致必要 |
|---|---|
| 出力の**個数** | ○ (両方2つ) |
| 出力の**順番** | ○ (answerSheet → fileName) |
| **名前** | ○ 完全一致(大文字小文字・末尾スペースも) |
| **説明** | ○ 完全一致(片方だけ空、が最頻の原因) |
| **種類** | ○ 両方「テキスト」 |
| **値** | ✕ ここだけ違ってOK |

---

**確実な直し方: 両方の出力を消して入れ直す**

1. **True側**「エージェントに応答する」で、出力2つを **×** で削除
2. **False側**「エージェントに応答する1」でも、出力2つを **×** で削除
3. True側に追加(この順番で)

| 順 | 種類 | 名前 | 説明 | 値 |
|---|---|---|---|---|
| 1 | テキスト | `answerSheet` | `回答済みのアーキテクチャレビュー票の内容` | `回答済みのアーキテクチャレビュー票がまだスレッドに添付されていません。` |
| 2 | テキスト | `fileName` | `読み取ったファイル名` | `なし` |

4. False側に追加(**名前と説明はTrue側の欄からコピー&ペースト**。手打ちしない)

| 順 | 種類 | 名前 | 説明 | 値 |
|---|---|---|---|---|
| 1 | テキスト | (コピー) | (コピー) | **fx** → 下の式 |
| 2 | テキスト | (コピー) | (コピー) | **fx** → `outputs('対象ファイル名')` |

False側1つ目の値:

```
concat('対象ファイル: ', outputs('対象ファイル名'), decodeUriComponent('%0A'), '全行数: ', string(length(body('回答行取得')?['value'])), '件', decodeUriComponent('%0A'), decodeUriComponent('%0A'), '| No | ステータス | 指摘概要 | 指摘内容 | 回答内容 | 回答者 | 回答日 |', decodeUriComponent('%0A'), '|---|---|---|---|---|---|---|', decodeUriComponent('%0A'), join(body('md行変換'), decodeUriComponent('%0A')))
```

