

**修正1: 条件の設定**

「条件」を開いて設定し直してください。

| 項目 | 設定 |
|---|---|
| 左辺 | **fx** → `string(length(body('アーキテクチャレビュー票Excel絞り込み')))` |
| 演算子 | 次の値に等しい |
| 右辺 | `0` ← そのまま文字入力 |

※ `length()` は数値を返すので、また型の不一致が起きないよう `string()` で文字列に揃えています

---

**修正2: False側の3アクションも参照が外れていないか確認**

同じ理由で壊れている可能性があります。それぞれ開いて確認してください。

| アクション | あるべき設定 |
|---|---|
| 対象ファイル | **fx** → `last(sort(body('アーキテクチャレビュー票Excel絞り込み'), 'LastModified'))` |
| 対象ファイル名 | **fx** → `outputs('対象ファイル')?['Name']` |
| 回答行取得 の ファイル | **fx** → `outputs('対象ファイル')?['Path']` |
| 回答行取得 の テーブル | `T_アーキテクチャレビュー票` |

---

