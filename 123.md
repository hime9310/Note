and(not(item()?['IsFolder']), contains(item()?['Name'], triggerBody()?['text']), contains(item()?['Name'], 'アーキテクチャレビュー票'), endsWith(item()?['Name'], '.xlsx'))


|出力名          |説明                        |値                                    |
|-------------|--------------------------|-------------------------------------|
|`answerSheet`|回答済みアーキテクチャレビュー票の内容(md表形式)|`回答済みのアーキテクチャレビュー票がまだスレッドに添付されていません。`|
|`fileName`   |読み取ったファイル名                |`なし`                                 |

first(sort(body('回答済み票抽出'), 'TimeLastModified'))
