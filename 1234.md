ご確認ありがとうございます。手動運用の流れ、承知しました。
ご認識のとおり、現状はNo20〜22(CSP WAF)とNo23(Global WAF)が並列のチェックになっているので、
判定トリガーで順番を付ける形に直したいと考えています。

・No23(Global WAF): 外部公開要件=有 かつ HTTP/HTTPS利用=有 の場合にチェック。
　記載あり → 問題なし / 記載なし → 票(Azure:15 / AWS:15 / GCP:12)でGlobal WAFを案内
・追加行(CSP WAF記載あり): 構成図にAzure WAF / AWS WAF / Cloud Armorの記載がある場合、
　まず「Global WAFに代替できないか」を案内(Cyber相談の要否はEngが判断)
・No20〜22(接続元IP制限): トリガーに「CSP WAF利用のCyber承認=済」を追加。
　承認が確認できるまでは対象外(承認待ち)とし、チェック内容と案内は現状のまま

確認したい点は2つです。
1. Cyber承認の結果はアーキテクチャレビュー表の利用者回答列に記載される認識でよろしいでしょうか。。
   判定の読み取り元にしたいので、書き方を「CSP WAF Cyber承認: 済」のように揃えられると助かります。
2. 行を1つ追加してよいでしょうか(既存のNoは変えず、末尾のNoで追加)。

上記でよければ、この形に直したうえで定型化に進めます。