# 構成図チェック観点 — WAF関連行の反映内容(Teams合意 2026/09/17)

## No20 Global WAF記載有無(変更)

| 列 | 値 |
|---|---|
| チェック観点 | Global WAF記載有無(変更なし) |
| 判断基準 | Global WAFの利用有無が構成図上で確認できること<br>※ホスティング事前相談appで「WAFの導入が必要ですか?」=Yes の場合にチェック。WAFは原則Global WAFが前提 |
| 対象CSP / 環境 / 対応するテンプレートの項目 | 共通 / DCS共通 / Azure:15 / AWS:15 / GCP:12(変更なし) |
| AI判定可否 | ○(◎ → ○) |
| チェックタイミング | **1次チェック**(2次 → 1次。appの出力はスレッドから1次で読めるため。案内を初回のアーキレビュー票に載せられる) |
| 判定トリガー | 回答: 「WAFの導入が必要ですか?」=Yes |
| ネクストアクション | 記載あり → A 問題なし<br>記載なし → D アーキレビュー票のAzure No.15/AWS No.15/GCP No.12で確認(Global WAFの利用を案内)<br>補足: 「WAFの導入が必要ですか?」=No または回答なしの場合は対象外(案内不要) |
| AI判定方法 | Global WAF、GlobalWAF、グローバルWAFの表記を抽出(変更なし) |
| AI出力内容 | Global WAFの記載有無／配置先／保護対象／接続関係(変更なし) |
| 備考 | WAFの選択肢は原則Global WAF。CSP WAF(Azure WAF / AWS WAF / Cloud Armor)はCyberに相談のうえ必須の場合のみ許容(No52、No17〜19) |

※ 現状のネクストアクションは「記載あり → D / 記載なし → A」と向きが逆になっているので、上記に直す。

## No52 CSP WAF記載有無(Global WAF代替の確認)(追加行・末尾)

| 列 | 値 |
|---|---|
| No | 52(既存のNoは変えない) |
| 大分類 | インターネットアクセス |
| チェック観点 | CSP WAF記載有無(Global WAF代替の確認) |
| 判断基準 | 構成図上にCSP WAF(Azure WAF / AWS WAF / Cloud Armor)の記載がある場合、原則Global WAFが前提のため、Global WAFに代替できないかを利用者に案内すること(CSP WAFはCyberに相談のうえ必須の場合のみ許容) |
| 対象CSP | 共通 |
| 環境 | DCS共通 |
| 対応するテンプレートの項目 | Azure:15 / AWS:15 / GCP:12 |
| AI判定可否 | ○ |
| チェックタイミング | 1次チェック |
| 判定トリガー | 存在: Azure WAF/AWS WAF/Cloud Armor |
| ネクストアクション | 記載あり → D アーキレビュー票のAzure No.15/AWS No.15/GCP No.12で確認(Global WAFに代替できないかを案内。Cyber相談の要否はEngが判断)<br>記載なし → A 問題なし |
| AI判定方法 | Azure WAF、AWS WAF、Cloud Armorの表記またはアイコンを抽出 |
| AI出力内容 | CSP WAFの記載有無／検出したCSP WAF名／配置先 |
| 備考 | CSP WAFが必須でCyber承認済みの場合のみ、No17〜19(接続元IP制限)に進む |

## No17 Azure WAF利用有無 / 接続元IP制限(変更)

| 列 | 値 |
|---|---|
| チェック観点 | Azure WAF利用有無 / 接続元IP制限<br>※CSP WAFのCyber承認済みの場合にチェック(「※外部公開要件有の場合にチェック」を置き換え) |
| 判断基準 | 変更なし |
| 判定トリガー | 存在: Azure WAF かつ 回答: 「CSP WAFのCyber承認」=済 |
| ネクストアクション | 記載あり → A 問題なし<br>記載なし → B(目視で確認できなければD: アーキレビュー票のAzure No.17で確認)<br>補足: Cyber承認の回答が無い場合は対象外(承認待ち)とし、Engに承認状況を確認 |
| 備考 | Cyber承認はアーキレビュー票(Azure No.17)の回答で確認する。記載が無い場合はEngへ確認 |
| その他の列 | 変更なし(Azure / DCS Disconnected / Azure:17 / ○ / 2次チェック / AI判定方法 / AI出力内容) |

## No18 AWS WAF利用有無 / 接続元IP制限(変更)

No17と同じ変更。読み替え: Azure WAF → AWS WAF、Azure No.17 → AWS No.17。

| 列 | 値 |
|---|---|
| 判定トリガー | 存在: AWS WAF かつ 回答: 「CSP WAFのCyber承認」=済 |
| ネクストアクション | 記載あり → A 問題なし<br>記載なし → B(目視で確認できなければD: アーキレビュー票のAWS No.17で確認)<br>補足: Cyber承認の回答が無い場合は対象外(承認待ち)とし、Engに承認状況を確認 |
| 備考 | Cyber承認はアーキレビュー票(AWS No.17)の回答で確認する。記載が無い場合はEngへ確認 |

## No19 Cloud Armor利用有無 / 接続元IP制限(変更)

No17と同じ変更。読み替え: Azure WAF → Cloud Armor、Azure No.17 → **GCP No.14**。

| 列 | 値 |
|---|---|
| 判定トリガー | 存在: Cloud Armor かつ 回答: 「CSP WAFのCyber承認」=済 |
| ネクストアクション | 記載あり → A 問題なし<br>記載なし → B(目視で確認できなければD: アーキレビュー票のGCP No.14で確認)<br>補足: Cyber承認の回答が無い場合は対象外(承認待ち)とし、Engに承認状況を確認 |
| 備考 | Cyber承認はアーキレビュー票(GCP No.14)の回答で確認する。記載が無い場合はEngへ確認 |

※ 現状のネクストアクションは「GCP No.17」になっているが、対応するテンプレートの項目は GCP:14 なので No.14 に直す。

## No16 WAF記載有無(要判断)

No20(Global WAF)とNo52(CSP WAF)で検出・案内が両方できるため、No16は役割が重なります。推奨は**廃止**(廃止シートへ移動)。残す場合は次のとおり「検出のみ」にする。

| 列 | 値(残す場合) |
|---|---|
| 判断基準 | ※を「※ホスティング事前相談appで「WAFの導入が必要ですか?」=Yes の場合にチェック(検出のみ。案内はNo20・No52で行う)」に置き換え |
| AI判定可否 | ○ |
| チェックタイミング | 1次チェック |
| 判定トリガー | 回答: 「WAFの導入が必要ですか?」=Yes |
| ネクストアクション | 記載あり → A 問題なし<br>記載なし → A 問題なし(Global WAFの案内はNo20、CSP WAFの代替案内はNo52で実施) |

---

## 動きの確認(反映後)

1. 1次: appの出力「WAFの導入が必要ですか?」=Yes → No20でGlobal WAFの記載を確認。無ければ票(Azure:15等)でGlobal WAFを案内。
2. 1次: 構成図にCSP WAFがある → No52で「Global WAFに代替できないか」を票で案内。Cyber相談の要否はEngが判断。
3. 2次: 票の回答に「CSP WAFのCyber承認=済」がある → No17〜19で接続元IP制限を確認。回答が無ければ対象外(承認待ち)として出力し、Engが承認状況を確認。

## あわせて気づいた点(今回の合意とは別。対応は任意)

- AI判定可否に「◎」が残っている行が複数あります(No1、4、5、7〜9、13、15、16、20、31〜33、45〜47 など)。標準は ○/△ の2値なので、◎ → ○ に置き換えが必要です。
- 判定トリガーが「要確認: 回答項目が未定義」のままの行があります(No21、35、36、37、48、49、50)。このままだと常に対象外になるので、票の項目名が決まり次第「回答: 「…」=値」に置き換えが必要です。