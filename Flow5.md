ツール⑤「アーキテクチャレビュー票取得」のStep by Stepです。ファイル名に版数が入っている前提なので、「フォルダ一覧→CSP名で絞り込み→取得」方式で作ります(ファイルは前回どおりOneDriveのAI連携用フォルダにある前提。SharePointサイトの場合の差分は末尾に記載)。

**Step 1: フロー作成とトリガー**

1. Copilot Studio →「フロー」→ 新しいエージェントフロー(旧デザイナー)
2. トリガー「**エージェントがフローを呼び出したとき**」→「+ 入力を追加する」→ テキスト
   - 名前: `CSP` / 説明: `AWS・Azure・GCPのいずれか(ツール①のcsp出力をそのまま渡す)`

**Step 2: フォルダ内のファイル一覧を取得**

1. 「+」→「アクションの追加」→ 検索「**フォルダー内のファイル**」→ OneDrive for Business「**フォルダー内のファイルの一覧**」
2. フォルダー: フォルダアイコンから **AI連携用フォルダ** を選択
3. 表示名を「**票ファイル一覧**」に変更(次の式から参照)

**Step 3: 該当CSPのmdだけに絞る**

1. 「+」→ データ操作「**アレイのフィルター処理**」→ 表示名「**該当CSP票抽出**」
2. From: fx → `body('票ファイル一覧')?['value']`
3. 左辺: fx → 次を貼付(@なし):

```
and(contains(item()?['Name'], concat('アーキテクチャレビュー票(', triggerBody()?['text'], ')')), endsWith(item()?['Name'], '.md'))
```

4. 演算子: 「等しい」/ 右辺: `true`
   - 意味: ファイル名に `アーキテクチャレビュー票(AWS)` を含み、`.md` で終わるもの → AWS版mdだけが残る(Excel雛形は `.xlsx` なので除外)
   - `text` はCSP入力の内部名。入力1つなら `text` です(不安ならコードビューで確認)

**Step 4: 見つからない場合の分岐**

1. 「+」→ コントロール「**条件**」→ 表示名「**判定:該当なし**」
   - 左辺 fx: `length(body('該当CSP票抽出'))` / 等しい / 右辺 `0`
2. **True側**: 「エージェントに応答する」→ 出力2つ(テキスト):

| 出力名 | 説明 | 値 |
|---|---|---|
| `reviewSheetMaster` | 該当CSPのアーキテクチャレビュー票マスタの全文(md) | `該当CSPのアーキテクチャレビュー票mdが見つかりませんでした。CSP指定を確認してください。` |
| `fileName` | 取得したマスタのファイル名(テンプレ版数の確認用) | `なし` |

**Step 5: False側(正常系)**

1. データ操作「**作成**」→ 表示名「**対象ファイルID**」/ 入力 fx: `first(body('該当CSP票抽出'))?['Id']`
2. データ操作「**作成**」→ 表示名「**対象ファイル名**」/ 入力 fx: `first(body('該当CSP票抽出'))?['Name']`
3. OneDrive for Business「**ID を使用したファイル コンテンツの取得**」→ ファイル欄: fx `outputs('対象ファイルID')` → 表示名「**票ファイル取得**」
4. 「エージェントに応答する」→ 出力2つ(**名前・説明はTrue側と一字一句同じ**):

| 出力名 | 値 |
|---|---|
| `reviewSheetMaster` | fx `base64ToString(body('票ファイル取得')?['$content'])` ※ツール②で「ファイル コンテンツ」チップ直指定が正解だった環境なら、ここも同じ方式に |
| `fileName` | fx `outputs('対象ファイル名')` |

**Step 6: 仕上げ**

1. 2つの「エージェントに応答する」→ 設定 → 非同期応答**オフ**
2. 「下書きを保存」→ フロー名 `アーキテクチャレビュー票取得` → **公開**
3. 子C(アーキテクチャレビュー票作成Agent)→ ツール追加 → 説明: 「CSP名(AWS/Azure/GCP)を指定して、該当CSPのアーキテクチャレビュー票マスタ(md全文)とそのファイル名(テンプレ版数)を取得する。編集指示を作成する前に必ず実行する」

**テスト**: Previewで「CSP=AWSでアーキテクチャレビュー票取得を実行し、fileNameと先頭5行を見せて」→ `…(AWS)_v2.4.md` と先頭行(全NN行のヘッダー)が返ればOK。Azure/GCPと、不正値(例: Oracle→該当なし応答)も各1回。

**運用上の注意**: 同じCSPのmdが複数版(v2.4とv2.5)フォルダに残っていると `first()` がどちらを取るか不定になります。**フォルダには現行版のみ**を置き、旧版は別フォルダへ移す運用にしてください。

**SharePointサイトに置いている場合の差分**: Step 2をSharePoint「フォルダー内のファイルの一覧」、Step 5-1の式を `first(body('該当CSP票抽出'))?['{Identifier}']`、Step 5-3をSharePoint「ファイル コンテンツの取得」(識別子=対象ファイルID)に置き換えるだけです。