**Kiroに渡す指示文(そのまま貼れます)**

> pptx-builder スキルを使って社内説明用のパワポを作成してください。用途は配布/ハンドアウトなので白背景。各スライド本文はワーキングエリア内、タイトル24pt、末尾に内部向け免責ページを付け、別名で保存してください。構成は以下。
>
> タイトル:Kiro Agent Hooks の活用提案 — モジュール配置の自動化とクレジット最適化
> 1. 表紙
> 2. アジェンダ:①Hookとは ②現状の課題 ③解決策 ④導入イメージ ⑤注意点と次アクション
> 3. Hookとは:IDEのイベント(ファイル作成/保存/削除・手動 等)で発火する自動トリガー。アクションは「エージェントプロンプト(Ask Kiro)」と「シェルコマンド(Run Command)」の2種類。
> 4. 現状の課題:参照リポジトリからのモジュール一式コピーをsteering(=LLM)にやらせており、コード生成のたびにトークン=クレジットを毎回消費している。
> 5. 解決策(Hook活用):コピーは決定的処理。Run Command(シェル)型Hookに移せばLLMを介さずOSのファイル操作で完結し、消費はほぼゼロ。steeringからは該当ロジックを削減。
> 6. 導入イメージ:トリガーは手動/ファイル作成、アクションは冪等なコピースクリプト。HookのJSONは `.kiro/hooks/` にコミットしてチーム共有。(Before=steeringでLLMがコピー/After=Hookがシェルで配置、の対比図)
> 7. 注意点:①必ずRun Command(Ask Kiroだと削減にならない)②stdoutはコンテキストに入るので大量出力しない③冪等にして編集済みを壊さない④Pre系トリガーは軽く+timeout⑤steeringは整理しつつ「ベースは配置済み、ゼロから作るな」の一文だけ残す
> 8. 次アクション:PoCで1本作成→クレジット差分を計測→横展開
> 9. 免責(内部向け・白)