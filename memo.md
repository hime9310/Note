

**Kiroに貼るプロンプト(HOOK説明パワポ・表紙→目次→サマリ・全11枚)**

> pptx-builder スキルを使って、社内チーム向けの説明用パワポを作成してください。用途は配布/ハンドアウトなので白背景。SKILLのルール(タイトル24pt、本文はワーキングエリア内、コピーライト右下、末尾に内部向け免責ページ)に従い、`Outputs/{要件名}/` に別名で保存してください。テーマは「Kiro Agent Hooks の活用」。全11枚、構成は以下。
>
> 1. 表紙：タイトル「Kiro Agent Hooks の活用提案 — モジュール配置の自動化とクレジット最適化」
> 2. 目次：§5の対応表にある目次レイアウト(白)を使う。項目は「1. Hookとは / 2. 現状の課題 / 3. 解決策 / 4. 導入イメージ / 5. 注意点 / 6. 次アクション」
> 3. サマリ(エグゼクティブサマリ)：モジュールコピーをsteering(LLM)からHook(シェル)へ移すことで、生成のたびのトークン消費をほぼゼロにできる、という結論を先に述べる
> 4. Hookとは：IDEのイベント(ファイル作成/保存/削除・手動)で発火する自動トリガー。アクションは「エージェントプロンプト(Ask Kiro)」と「シェルコマンド(Run Command)」の2種類
> 5. 現状の課題：参照リポジトリからのモジュール一式コピーをsteering(=LLM)にやらせており、コード生成のたびにトークン=クレジットを毎回消費している
> 6. 解決策：コピーは決定的処理。Run Command(シェル)型Hookに移せばLLMを介さずOSのファイル操作で完結し、消費はほぼゼロ。steeringからは該当ロジックを削減
> 7. 導入イメージ：トリガーは手動/ファイル作成、アクションは冪等なコピースクリプト。HookのJSONは `.kiro/hooks/` にコミットしてチーム共有。Before(steeringでLLMがコピー)／After(Hookがシェルで配置)の対比を図示
> 8. 注意点：①必ずRun Command(Ask Kiroだと削減にならない) ②stdoutはコンテキストに入るので大量出力しない ③冪等にして編集済みを壊さない ④Pre系トリガーは軽く＋timeout ⑤steeringは整理しつつ「ベースは配置済み、ゼロから作るな」の一文だけ残す
> 9. 次アクション：PoCで1本作成 → クレジット差分を計測 → 横展開
> 10. (予備)補足Q&A：想定質問への回答。不要なら省略可
> 11. 免責(内部向け・白)


---

はい、その理解で正しいです。この§5の表のTODOを実際の番号で埋める、それが「SKILLの整備」です。基本の並び(表紙→目次→本文、中表紙は必要に応じて)もそのままでOKです。

ただ1点だけ現実的な注意を。**私やあなたが番号を"目視で推測"して埋めるのは避けたほうがいい**です。理由は、番号(index)は実テンプレを開かないと正確に分からず、前のスライド写真(59枚)の見た目の順番とも一致しないからです。ここで適当な数字を入れると、また表紙が崩れます。

なので、**埋める作業自体もKiroにやらせる**のが確実で速いです。手順はこれだけ:

**ステップ1:Kiroに番号を調べさせて表を埋めさせる**

Kiroにこう投げてください。

> `.kiro/skills/pptx-builder/reference/DT Template_16_9_J.potx` を python-pptx で開き、`for i, l in enumerate(prs.slide_layouts): print(i, l.name)` を実行して、レイアウトの index と名前の一覧を出力してください。
> その結果をもとに、SKILL.md §5 の表の TODO を、白背景の「表紙(白)/目次/中表紙/本文/免責(内部向け)」に対応する index で埋めて、SKILL.md を上書き保存してください。判別しづらいレイアウトは、名前と、必要なら各 index で空スライドを1枚生成して見た目を確認してから確定してください。黒背景・外部向けは今回は TODO のままで構いません。

**ステップ2:埋まった§5の表を私に見せる**

Kiroが出した「番号+レイアウト名の一覧」と「埋めた後の表」を、ここに貼ってください。白・内部向けとして妥当か(表紙が外部提案用の枠付きレイアウトを掴んでいないか等)を一緒に確認します。

**ステップ3:表が確定してから、再生成**

§5が埋まってはじめて、前回作った11枚のプロンプトで再生成すると、表紙→目次→本文が正しい型で出ます。プレースホルダー残り(クライアント名・画像枠)も、再生成プロンプトに「初期テキスト・未使用プレースホルダーは削除」を入れておけば消えます。

要するに、順番は「①Kiroに§5を埋めさせる → ②中身を一緒に確認 → ③再生成」。今あなたがやることは、上のステップ1のプロンプトを投げることだけです。まずその出力(index一覧)が出たら、貼ってください。