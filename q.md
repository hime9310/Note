5点、順に回答します。

**1. AI判定可否列の方針 → 賛成(値は「○/△」の2つにし、廃止は行を残さない)**

| 値 | 意味 | 子Bの動き |
|---|---|---|
| ○ | AIに判定してほしい項目 | 判定値(記載あり/なし…)と区分を確定して出す |
| △ | 判定困難と思われる項目 | 判定は確定させず「判定案+根拠+人に確認してほしいこと(質問文)」を出す。私の案の「×(材料提示のみ)」は△に統合で問題ありません(判定案が出せなければ事実だけ書く) |
| 廃止 | チェックをやめる項目 | **mdには入れない**。mdに残すと検算R1の分母と子Bの読解を乱すので、Excelでは「廃止」シートへ移す(履歴用)か行削除 |

「△のとき人間に聞く」の実体だけ補足します。子Bは処理の途中でEngに質問して待つことができない(親経由・1指示=1子)ので、【構成図チェック結果】の「■要人手確認」に質問をまとめて出し、Engが目視または利用者確認のうえ「判定して。○○は有」の訂正・補足で答える、という既存の流れで「聞く」を実現します。

**2. 判定トリガーの「条件」とは**

「型」だけでは子Bが機械的に判定できないので、型ごとに**何を見て成立と判断するか**を書いたものが条件です。

| 型 | 条件に書くこと | 標準表記(1行) | 例(現在の行から) |
|---|---|---|---|
| ①常時 | なし | `常時` | No.-3 リージョン記載有無 |
| ②存在 | 構成図(Mermaidのノードラベル・注釈情報)に**どのサービス/リソース**があれば実施するか。正式名と別名を「/」区切りで列挙 | `存在: RDS/Aurora/Cloud SQL/Azure SQL Database` | No.-1 グローバルサービスの配置場所(「代表サービス一覧のいずれかの利用がある場合」) |
| ③回答 | アーキレビュー票の**どの項目**への回答が**どの値**なら実施するか | `回答: 「DR要件」=有` | No.0 DR構成有無、No.1 外部SaaS連携(`回答: 「外部SaaS連携」=有`)、No.2 CSP外通信経路(`回答: 「通信要件」=記載あり`) |

2つ必要な行は `存在: … かつ 回答: …` と書きます(3つ以上なら行を分ける)。②の対象リストは、論点B-2の「判定用キーワード列」を兼ねるので別列は不要になります。

**3. 環境 → 了解。C-2は取り下げ、「Engが環境を指定する」方式にします**

読み取った前提: Mgmtの判断アプリ結果(スレッド)で環境(Hub/Disconnected)が最初から分かっているため、子Bは「指定環境 or DCS共通」の行だけを実施する。この前提で、Engが「抽出して。環境はHub」のように添え、親が「対象環境」を状態として保持して子B・子Cへ渡す形にします(添えられなければ親が聞き返す)。**確認**: 環境はこの時点で確定扱いでよく、Phase 1cの判定で変わる可能性は無視してよいですか(変わり得るなら、2次チェックで新しい環境の行を実施する扱いにします)。

**4. ネクストアクション標準化プロンプト**

```text
あなたは「構成図チェック観点」表の整備担当です。各行の「ネクストアクション」列(自然言語)を、AIが条件分岐に使える標準表記へ書き換えてください。

# 入力
次の表を渡します(列: No / チェック観点 / 判断基準 / ネクストアクション)。
{ここに表を貼る}

# 標準表記
1行目以降を「判定値 → 区分」の形で書き、最後に「補足:」を1行付けます。
判定値(5つ): 記載あり / 記載なし / 方向不明 / 判定不能 / 対象外
区分(5つ):
- A 問題なし(何もしない)
- B Eng目視確認(AIの判定を人が構成図で確かめる)
- C 利用者へ記載・修正依頼(構成図に追記・修正してもらう)
- D アーキレビュー票の該当項目で確認(票のヒアリング項目にする)
- E 対象外(トリガー不成立・適用外)
多段の場合は「B(目視で確認できなければC: 利用者に○○の記載を依頼)」のように括弧で書く。
Cは「何を記載・修正してもらうか」を必ず具体的に書く。

# ルール
- 元の記述に無い判断・条件を追加しない。元の記述から読み取れない判定値の分岐は書かず、「要確認」列に理由を書く。
- 元の記述の注意書き(例外・推奨条件など)は「補足:」に残す。捨てない。
- 全行を処理する。飛ばさない。出力の最後に「処理行数: N / 入力行数: N」を書き、一致しない場合はやり直す。
- 書き換えに迷った行は、案を出したうえで「要確認」列に「迷った点」を書く。

# 出力(md表)
| No | チェック観点 | 元のネクストアクション | 標準表記 | 補足 | 要確認 |

# 例
元: 「リージョン名が確認できた場合⇒問題無し / 確認できなかった場合⇒一度目視で確認。目視でも確認できない場合は利用者に記載頂くようご案内」
標準表記:
記載あり → A
記載なし → B(目視で確認できなければC: 利用者にリージョン名の記載を依頼)
判定不能 → B
対象外 → E
補足: なし
```

**5. 判定トリガー標準化プロンプト**

```text
あなたは「構成図チェック観点」表の整備担当です。各行の「判定トリガー」列(自然言語)を、AIが機械的に判定できる標準表記へ書き換えてください。

# 入力
次の表を渡します(列: No / チェック観点 / 判断基準 / 1次・2次チェック / 判定トリガー)。
{ここに表を貼る}

# 標準表記(型と条件)
型は次の3つのいずれか。条件は型ごとに決まった形で書く。
- 常時: 条件なし。表記は「常時」
- 存在: 構成図(Mermaidのノードラベル・注釈情報)に対象のサービス/リソースがあれば実施。表記は「存在: 対象A/対象B/…」(正式名と別名を「/」区切り。元の記述にある名称だけを使い、増やさない)
- 回答: アーキテクチャレビュー票の項目への回答が指定の値なら実施。表記は「回答: 「票の項目名」=値」(値は 有 / 無 / 記載あり / 具体値)
条件が2つ必要な行は「存在: … かつ 回答: …」。3つ以上なら「要確認」に「行の分割が必要」と書く。

# ルール
- 元の記述に無い対象・項目・値を追加しない。対象が「下記一覧」など別シート参照になっている場合は、対象欄に「(参照: シート名)」と書き、要確認に「一覧の転記が必要」と書く。
- 1次チェックの行で条件が書かれていないものは「常時」とする。2次チェックの行で条件が読み取れないものは「要確認」に「回答項目が未定義」と書く。
- 「回答」型の票の項目名は、元の記述の言葉をそのまま使う(票側との用語統一は後で行う)。
- 全行を処理する。飛ばさない。出力の最後に「処理行数: N / 入力行数: N」を書き、一致しない場合はやり直す。

# 出力(md表)
| No | チェック観点 | 1次/2次 | 元の判定トリガー | 型 | 条件(標準表記) | 要確認 |

# 例
元: 「アーキレビュー票で利用者に下記をヒアリングし、内容を確認できた場合 ・DR要件利用有」
→ 型: 回答 / 条件: 回答: 「DR要件」=有
元: 「下記リソースいずれかの利用がある場合 Vnet/VPC外に記載する代表サービス一覧」
→ 型: 存在 / 条件: 存在: (参照: Vnet/VPC外に記載する代表サービス一覧) / 要確認: 一覧の転記が必要
元: 「常時実施」 → 型: 常時 / 条件: 常時
```

使い方の注意: 行数が多い場合は30行程度ずつ貼ると欠落しにくいです。出力の「処理行数」が入力と一致することを毎回確認してください。

3の確認(環境の確定扱い)をいただければ、論点一覧を v1.1(AI可否=○/△・廃止行なし、C-2取り下げ→環境指定方式、トリガー条件の定義、区分A〜E)に更新します。