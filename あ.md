承知しました。M365 Copilot Chat にファイルを参照させて、自然言語で直接修正してもらうためのプロンプトです。チャットの入力欄で「/」からExcelファイルを選んで添付し、その後に貼ってください(1つずつ実行)。

**① 判定トリガー**

```text
添付のExcel「構成図チェック観点」シート(2行目が見出し、3行目からデータ)を編集してください。

やること:
「判定トリガー」列のすぐ右に新しい列「判定トリガー(標準)」を追加し、各行の「判定トリガー」の文を次のルールで標準表記に書き換えて入力してください。元の「判定トリガー」列は変更しないでください。

ルール:
・型は3つ: 常時 / 存在 / 回答
・常時 →「常時」
・存在 →「存在: 対象A/対象B」(構成図にそのサービス名・リソース名があれば実施。/区切りで列挙。元の文にある名称だけを使う)
・回答 →「回答: 「票の項目名」=値」(アーキレビュー票の項目への回答が指定の値なら実施。値は 有/無/記載あり/具体値)
・条件が2つ必要な行は「存在: … かつ 回答: …」
・「1次・2次チェック」が1次で、条件が書かれていない行は「常時」
・対象が一覧やシートの参照になっている行は「存在: (参照: 一覧名)」と書き、末尾に「要確認: 一覧の転記が必要」を付ける
・回答の項目が読み取れない行は「要確認: 回答項目が未定義」を付ける
・元の文に無い対象・項目・値を追加しない。説明文は書かず標準表記だけを入力する

「チェック観点」「1次・2次チェック」列も参考にしてください。全行を処理し、最後に「処理した行数」と「要確認を付けた行のNo一覧」を教えてください。
```

**② ネクストアクション**

```text
添付のExcel「構成図チェック観点」シート(2行目が見出し、3行目からデータ)を編集してください。

やること:
「ネクストアクション」列のすぐ右に新しい列「ネクストアクション(標準)」を追加し、各行の「ネクストアクション」の文を次のルールで標準表記に書き換えて入力してください。元の列は変更しないでください。

ルール:
・判定値は5つ: 記載あり / 記載なし / 方向不明 / 判定不能 / 対象外
・区分は5つ: A 問題なし / B Eng目視確認 / C 利用者へ記載・修正依頼 / D アーキレビュー票の該当項目で確認 / E 対象外
・「判定値 → 区分」を判定値ごとにセル内で改行して書く
・Cは「何を記載・修正してもらうか」を括弧内に具体的に書く
・多段の対応は「B(目視で確認できなければC: 利用者に○○の記載を依頼)」の形で書く
・元の文から読み取れない判定値の分岐は書かない
・元の文の注意書き(例外・推奨条件など)は最後に「補足: …」として残す
・元の文に無い判断・条件を追加しない
・迷った行は最後に「要確認: 理由」を1行付ける。説明文は書かず標準表記だけを入力する

「チェック観点」「判断基準」列も参考にしてください。全行を処理し、最後に「処理した行数」と「要確認を付けた行のNo一覧」を教えてください。
```

**もし「ファイルを直接編集できない」と返ってきた場合**は、続けてこう送ってください。

```text
では編集の代わりに、No と 標準表記 の2列の表で全行分を出力してください。私が貼り付けます。処理した行数も教えてください。
```

実行後の確認は、①処理行数が実際の行数と一致しているか ②「要確認」の行を先に目視 ③問題なければ元の列はそのまま残し、md化ではこの「(標準)」列を使う、の3点です。