Step 2 完了です。次は **Step 3: 子A(構成図Mermaid化Agent)を呼ぶノード**です。ここも「最初の1ノードだけ画面で追加 → YAMLを見せてもらう → 残りはコード」で進めます。

**Step 3-0 事前確認(子A側の入力・出力)**

ノードに入力欄が出るのは、子A側に入力・出力が定義されている場合だけなので、先に確認します。

| # | 操作 |
|---|---|
| 1 | 親Agentの上部タブ「**エージェント**」→ `構成図Mermaid化Agent` を開く |
| 2 | 「**入力**」に次の3つがあるか。無ければ「+ 入力を追加する」で追加(データ型: 文字列 / 「必須にする」オフ / 「ユーザーに求める必要がある」**オフ**) |
| 3 | 「**出力**」(「詳細」を展開)に次の3つがあるか。無ければ「+ 出力を追加する」で追加(データ型: 文字列) |
| 4 | 「実行後」は「**応答しない**」のまま → 「保存」 |

| 種別 | 名前 | 説明(そのまま記入) |
|---|---|---|
| 入力 | instruction | 書き起こしの依頼文、または人間からの改修指示。この内容に従って処理する |
| 入力 | previousMermaid | 直前に出力したMermaidコード。改修指示のときに基準にする。初回は空 |
| 入力 | previousAnnotations | 直前に出力した注釈情報。初回は空 |
| 出力 | mermaid | 出力したMermaidコードブロックの中身(```mermaid と ``` の記号は含めない)。質問を返す場合は空 |
| 出力 | annotations | 「## 注釈情報」以下の箇条書き全文。無ければ「なし」 |
| 出力 | message | Mermaidを出力しなかった場合の応答本文(解釈の確認質問、入力不備の説明など)。Mermaidを出力した場合は空 |

**Step 3-1 T1a-1 にノードを追加(画面で1ノードだけ)**

| # | 操作 | 結果 |
|---|---|---|
| 1 | 「トピック」→ `T1a-1` を開く → 一番下(16個目 `Global.KnowledgeDraft`)の下の「**+**」 | ノードメニュー |
| 2 | 「**エージェントを追加する**」→ `構成図Mermaid化Agent` | エージェントのノードが追加される |
| 3 | ノード内の入力で **instruction** に文字列 `添付された構成図の画像またはPDFをMermaidへ書き起こしてください。` を入力(「入力の追加」から選ぶ形の場合は instruction だけ追加。previousMermaid / previousAnnotations は今回は追加しない) | |
| 4 | ノードの出力欄に `Topic.mermaid` `Topic.annotations` `Topic.message` (名前は自動)が出ていることを確認 → 「**保存**」 | |

**Step 3-2 YAMLを見せてください**


Sources: [Add other agents overview - Microsoft Learn](https://learn.microsoft.com/en-us/microsoft-copilot-studio/authoring-add-other-agents), [Add a child agent - Microsoft Learn](https://learn.microsoft.com/en-us/microsoft-copilot-studio/add-agent-child-agent)