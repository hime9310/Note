取得成功おめでとうございます。ご指摘の2点は両方直せます。原因もはっきりしています。

**修正1: 親投稿の本文が空(タイムスタンプと名前だけ)になる**

「追記:親投稿」の式の `plainTextContent` の参照パスが1階層足りていませんでした(旧設計書の式の誤りで、Studio版でも同じ式を使ったため再現)。「追記:親投稿」の値のfx式を、次で**丸ごと差し替え**てください:

```
concat('【親投稿】', outputs('メッセージ詳細を取得する')?['body']?['createdDateTime'], ' ', outputs('メッセージ詳細を取得する')?['body']?['from']?['user']?['displayName'], decodeUriComponent('%0A'), outputs('メッセージ詳細を取得する')?['body']?['body']?['plainTextContent'], if(empty(outputs('メッセージ詳細を取得する')?['body']?['attachments']), '', concat(decodeUriComponent('%0A'), '【添付ファイル】', string(outputs('メッセージ詳細を取得する')?['body']?['attachments']))))
```

(変更点は本文部分が `?['body']?['body']?['plainTextContent']` と body 2階層になったところだけです)

**修正2: 返信がHTMLのままで見にくい → テキストに変換**

返信ループの中に変換アクションを1つ挟みます。

1. 返信ループ(それぞれに適用する)の**中**、「追記:返信」の**前**に「+」→「アクションの追加」→ 検索「**Html**」→ コンテンツ変換の「**Html からテキスト**」を選択
2. 表示名を「**HTML変換**」に変更(この名前は次の式から参照するので正確に)
3. 「コンテンツ」欄: **fx** → `item()?['body']?['content']` → 追加
4. 「追記:返信」の値の式を次で丸ごと差し替え(本文が `body('HTML変換')` に変わっています):

```
concat(decodeUriComponent('%0A%0A'), '【返信】', item()?['createdDateTime'], ' ', item()?['from']?['user']?['displayName'], decodeUriComponent('%0A'), body('HTML変換'), if(empty(item()?['attachments']), '', concat(decodeUriComponent('%0A'), '【添付ファイル】', string(item()?['attachments']))))
```

修正後は「下書きを保存」→「**公開**」→ Previewで再テストです。これで親投稿は投稿とほぼ同じプレーンテキスト、返信もHTMLタグなしのテキストになります。

補足を2つ:

- 太字や色などの装飾は失われますが、Agentの判断材料としては問題ありません。「投稿と完全に同じ見た目」はチャット出力では難しいため、**テキストとして同じ内容**が実用ラインです。
- 画面表示のフォーマット(「1. 対象RITM」「5. 添付ファイル」等のセクション構成)はAgentのInstructions側の仕様なので、フロー修正とは別に調整できます。表示をもっと整えたい場合は、動作確認が終わった後にInstructionsの出力形式を一緒に調整しましょう。

テスト結果(親投稿の本文が入ったか、返信が読みやすくなったか)を教えてください。OKなら残りは「CSPはAzureです」の優先テストと、変更点の設計書への反映です。