①PoCは「後者」(AIが編集指示を出力→人がExcel修正)で設計・構築済み。
 Agent出力は編集指示形式: ①そのまま採用No一覧/②添削(修正後全文)/③対象外(理由)/④新規追加行+検算。「Excel用」指定でタブ区切り再出力可(貼り付けで列分割)。
②前者(AI直接修正)はPhase 2候補。テンプレのテーブル化+Excel Online「表に行を追加」で新規行は可能。既存セルへの追記(記載を消さず)はOfficeスクリプト併用が必要で難度高→PoC評価後に判断。
③必要行を残すロジック=テンプレ由来の標準指摘一覧「全行」を、スレッド情報(ホスティング判断アプリ結果含む)+社内Rule資料と突き合わせて仕分け。構成図チェックAgentのOutput連携はPhase 2。
④テンプレをAIが一から出力する形式は非推奨(現行運用・版管理・人の確認容易性を崩すため)。まず編集指示型で効果検証。


