	•	名前 threadContent → 値: 動的コンテンツまたはfxで変数 threadText(fxなら variables('threadText'))
	•	名前 catalogText…ではなく reviewSheetStandardItems → 値: 変数 reviewSheetStandardItems(fx: variables('reviewSheetStandardItems'))
	•	名前 csp → 値: 動的コンテンツ「CSP確定」の「出力」