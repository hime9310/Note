if(contains(variables('threadText'),'Azure'),'Azure',if(contains(variables('threadText'),'GCP'),'GCP',if(contains(variables('threadText'),'AWS'),'AWS','不明')))

if(empty(coalesce(triggerBody()?['text_1'],'')),outputs('CSP自動判定'),coalesce(triggerBody()?['text_1'],''))

