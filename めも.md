お疲れさまです。アーキテクチャレビューAgentの親Agentの制御方式について、方針を相談させてください。

■ 検討した2案
1. トピック制御: 各フェーズをトピック(画面上のフロー)で固定し、変数で受け渡す方式(MS推奨)
2. Instructions制御: 親の指示文(Instructions)だけで進行を制御する方式

■ トピック制御を実際に作ってみた結果
・必要なトピックが11本、状態を持つ変数が16個になり、画面での設定量がかなり多い
・作成中に設定ミス由来のエラーが複数発生し、原因の切り分けにも時間がかかる
・親のInstructions自体は短くなる一方、業務ロジック(手順・判定条件・文言)を変えるたびにトピックのノードを直す必要があり、保守が難しい

■ 結論(提案)
Instructions制御で進めたいと考えています。
・手順の変更=指示文の修正で済み、Engが読んで直せる
・作成もInstructionsと説明文を貼るだけで、すぐにテストに入れる
・懸念(子Agentの連鎖、結果の要約、状態の判別)はテスト項目にしており、成立しなければトピック制御に戻せるよう、両方の設計書を残しています

まずInstructions制御で通しテストを行い、結果をご報告します。この方針で進めてよろしいでしょうか。



ーーー
お疲れさまです。アーキテクチャレビューAgentの親Agentの制御方式について、方針を相談させてください。

■ 検討した2案
1. トピック制御: 各フェーズをトピック(画面上のフロー)で固定し、変数で受け渡す方式(MS推奨)
2. Instructions制御: 親の指示文(Instructions)だけで進行を制御する方式

■ 補足: トピック制御で必要になるもの
・トピック11本 = レビューの9フェーズ分(1フェーズ=1トピック)+人の確認を繰り返すループ用2本
　- フェーズ用(Engの指示で起動): 「RITMxxxをレビューして」→構成図のMermaid化、「抽出して」→構成図の情報抽出、「判定して」→ホスティング判定、「レビュー票を作成して」→票作成、「回答を確認して」→回答回収 など
　- ループ用(自動で移る): Mermaidの修正→再提示を「完了」まで繰り返す / 編集指示の修正→再提示を「確定」まで繰り返す
・変数16個 = 子Agentの結果を次のフェーズへそのまま持ち回るための入れ物
　- 代表例: RITM番号、確定したMermaid(構成図の書き起こし)、構成図チェック結果、ホスティング判定結果、確定版の編集指示、回答回収結果、ナレッジ文案
・このほか、各子Agentに入力・出力の定義(Mermaid化Agentだけで6件)が必要

■ トピック制御を実際に作ってみた結果
・上記の設定量が多く、画面操作(ノード・変数・入出力の設定)にかなり時間がかかる
・作成中に設定ミス由来のエラーが複数発生し、原因の切り分けにも時間がかかる
・親のInstructions自体は短くなる一方、業務ロジック(手順・判定条件・文言)を変えるたびにトピックのノードと変数を直す必要があり、保守が難しい

■ 結論(提案)
Instructions制御で進めたいと考えています。
・手順の変更=指示文の修正で済み、Engが読んで直せる
・作成もInstructionsと説明文を貼るだけで、すぐにテストに入れる
・懸念(子Agentの連鎖、結果の要約、状態の判別)はテスト項目にしており、成立しなければトピック制御に戻せるよう、両方の設計書を残しています

まずInstructions制御で通しテストを行い、結果をご報告します。この方針で進めてよろしいでしょうか。