# アーキテクチャレビューAgent 全体構成 再整理案

| 項目 | 内容 |
|---|---|
| 版 | v1.2 |
| 更新日 | 2026-08-17 |
| v1.1からの変更 | ①テンプレ読取方式を確定: **公式テンプレは変更不可 → AI連携用専用フォルダにテーブル化済みコピーを格納** ②判定基準一覧の実装を確定: **Eng側でmarkdown化 → ツール②はファイル読取(3アクション)** ③利用者回答は「運用上の前提メモ」へ整理(スレッド返信は人間が実施) ④ナレッジList列構成の確認範囲を明確化(ツール③入力設計のみに影響) ⑤**Skills(構造化指示)はPoCでは不採用**と判断、Phase 2候補へ |
| 位置づけ | 設計書v2.1のスコープ(レビュー票編集指示)に**ホスティング環境判定(Sandbox/Hub/Disconnected)**を追加した全体構成。「ホスティング判断基準一覧」(確定後md化)を判定仕様の一次資料とする |
| 実装先 | Copilot Studio(JP-IT-Int-PAD-Prod)。フローは**AgentFlow(GA)**。Workflow(パブリックプレビュー)・Skills(プレビュー)はPhase 2で評価 |

---

## 1. 全体ワークフロー(確定版)

```mermaid
flowchart TD
    A["Mgmt: 事前相談受付・一次判断<br>判定アプリ出力を含めTeamsスレッド投稿<br>(DCS案件のみEngへ引継ぎ)"] --> B["Eng: Agentへ依頼<br>「RITMxxxxxxxをレビューして」"]

    B --> S1["<b>01 Teamsスレッド内容取得</b>(ツール①)<br>親投稿・返信・添付一覧+標準指摘一覧<br>(利用者回答も返信として取得)"]

    S1 --> S2["<b>02 ホスティング判断</b><br>Gate(No.1〜3)→No.4〜8→SSDLC→ノックアウト<br>Knowledge根拠照合(AI代替可否○△×規則)"]

    S2 --> D{判断結果}

    D -->|"DCS対象外"| F["差し戻し文案出力<br>→ Mgmtが利用者へ連絡・案内"]

    D -->|"情報不足あり<br>(×項目・△未確認)"| G["Agentが追加ヒアリング文案生成<br>→ Engがレビュー票へ記載<br>→ Mgmt経由で利用者へ確認"]
    G --> I["利用者回答(人がスレッドへ反映)"]
    I --> B2["Eng: 再依頼(再判断)<br>※複数回発生しうる"]
    B2 --> S1

    D -->|"必要情報すべて充足<br>(レビュー票=回答受領済み)"| S3["<b>03 仮判定確定(案)</b><br>Sandbox / Hub / Hub(PoC) / Disconnected<br>+レビュー票編集指示①②③④+検算<br>+ナレッジ登録文案の生成"]

    S3 --> V["<b>04 Eng最終確認(人)</b><br>仮判定・レビュー票の妥当性確認"]
    V --> C1["<b>05 クローズ</b><br>スレッド(チャット)クローズ"]
    C1 --> C2["<b>06 技術相談ナレッジへ追記</b><br>Engの明示指示でツール③実行(または手動転記)"]
```

### 責任分界

- Agentが働くのは01〜03(+ヒアリングループ)。**04以降は人のプロセス**
- 「仮判定確定」は全回答が揃った状態でのAgentの最終**案**。確定権限はEngの最終確認(04)
- ナレッジ追記(06)は、Engが内容確認のうえ「登録して」と**明示指示した場合のみ**ツール実行

### 運用上の前提メモ(利用者回答の流れ)

- スレッドへの返信・転記は**すべて人間が行う**(従来どおり)
- 前提はひとつだけ: **回答が最終的にスレッド上のテキストとして残っていること**。ツール①はスレッドしか見ないため、スレッドに文字があればAgentは再判断時に自動で読める
- 回答がExcelレビュー票の中にしか無い場合は、Engがその部分をスレッドへ転記(またはAgentへコピペ提示)してから再依頼する

## 2. ホスティング判定パイプライン(判定基準mdの実装マッピング)

| 段 | 参照(md内セクション) | 判定内容 | 結果 |
|---|---|---|---|
| Gate | DCS利用判定基準 No.1〜3 | 所有者=Deloitte? / 契約主体=Deloitte? / クラウド=Azure・AWS・GCP? | 1つでも不可→**DCS対象外(Mgmt差し戻し)** |
| S1 | 同 No.4 | 90日以内PoCか | Sandbox判定 / Sandbox対象外 |
| S2 | 同 No.5 | 社内NW接続要件有無 × No.4結果 | Sandbox / Hub(PoC) / Disconnected 仮判定(SandboxはデロイトNW接続NG) |
| S3 | 同 No.6〜8 | システム利用者 / クライアントアクセス / クライアント向け提供 × No.5結果 | 仮判定の確定方向(Sandboxは社内利用限定・クライアント提供不可・本番転用不可) |
| S4 | SSDLC(TOM2024) | 目的 / 格納データ(個人・機密・極秘→Sandbox不可)/ 通信 | 仮判定の整合チェック |
| S5 | ノックアウト要件+Hub/Disconnected判断基準 | **Hub第1優先**。7カテゴリ該当時のみDisconnected許容 | Hub / Disconnected 仮判定 |

### AI代替可否フラグの実装規則(Instructions)

| フラグ | Agentの振る舞い |
|---|---|
| ○(高) | 自動判定し、根拠(判定基準ID、スレッド内の記載箇所)を明示 |
| △(中) | 判定案+「**要人手確認**」タグ+確認観点1行 |
| ×(低) | **判定しない**。「追加ヒアリングコメント文案」を生成(→レビュー票経由で利用者確認) |
| 判定不能 | 「判断不可(情報不足)」+質問案でエスカレーション |

### 判定基準md化の要件(Eng作業時のお願い)

- **各判定行にIDを振る**(例: Gate-1、DCS-4、SSDLC-2、KO-①-1)→ Agentの判定経路表示(「Gate-2により不可」)と検算・監査に使用
- **先頭に版数と総行数**を記載(例: `ホスティング判定基準 v1.0 / 全NN項目`)
- 各行にAI代替可否(○△×)と人手確認要否を明記(Excelの列をそのまま踏襲)

## 3. コンポーネント構成

```mermaid
flowchart LR
    Eng["CMS Eng"] -->|"RITM番号で依頼"| Main["メインAgent<br>アーキテクチャレビューAgent<br>【判定+仕分けの頭脳】<br>※コアロジックはInstructions常時適用<br>(Skillsは不採用・§下表)"]
    Main -->|"ツール①"| Flow1["AgentFlow: Teamsスレッド内容取得<br>(改修: 標準指摘一覧をAI連携用コピーから読取)"]
    Main -->|"ツール②(新規)"| Flow2["AgentFlow: ホスティング判定基準取得<br>(md読取・3アクション)"]
    Main -->|"ツール③(新規)"| Flow3["AgentFlow: 技術相談ナレッジ登録<br>(SharePoint List「項目の作成」)"]
    Main -.->|"根拠参照"| KB["Knowledge(01_Knowledge)"]
    Sub1["構成図エージェント(Phase 2)"] -.->|"将来接続"| Main
```

### AI連携用フォルダ(新設・確定)

公式テンプレ・判定基準は変更不可のため、**AI連携専用フォルダ**を新設し、Agentが読むファイルをここに集約する:

| ファイル | 内容 | 改版時の作業 |
|---|---|---|
| アーキテクチャレビュー票(AWS/Azure/GCP)のコピー ×3 | 公式テンプレのコピーに**テーブル(T_標準指摘)を追加**したもの | 新版からコピー作成→テーブル化→差し替え(手順書化・数分) |
| ホスティング判定基準_vX.X.md | 判定基準一覧の確定版をEngがmd化したもの | mdファイル差し替えのみ |

原則: **このフォルダには現行版のみ**を置く(旧版は別フォルダへ)。フロー・Agent・Instructionsは改版時も無修正。

### ツール一覧

| ツール | 実体 | 入出力 | 状態 |
|---|---|---|---|
| ① Teamsスレッド内容取得 | AgentFlow(構築済み・Studio版) | RITM番号(+CSP) → threadContent / reviewSheetStandardItems / csp | **改修**: スイッチ内蔵テキスト→AI連携用コピーの表読取(§4) |
| ② ホスティング判定基準取得 | AgentFlow(新規・3アクション) | 入力なし → hostingCriteria | 新規: トリガー→SharePoint「ファイル コンテンツの取得(パス)」→応答。base64で返る場合は `base64ToString()` を挟む |
| ③ 技術相談ナレッジ登録 | AgentFlow(新規) | Listの列構成に合わせた入力 → 登録結果+リンク | 新規(Eng明示指示時のみ実行)。**List列構成(列名・型・必須・選択肢値)が入力設計の前提** — Listへの変更は不要 |

### 実装形態の使い分け(設計判断)

| 要素 | 実装 | 理由 |
|---|---|---|
| 判定ロジックの骨格(順序・ゲート・○△×・エスカレーション・検算) | メインAgentの**Instructions(常時適用)** | 毎回・必ず・全部適用が要件。条件発動の仕組みに置かない |
| **Skills(構造化指示・プレビュー)** | **PoCでは不採用** | Skillsはオーケストレーターが「関連時に実行」を判断する仕組みのため、必ず適用すべき中核ロジックの置き場所として不安定。状況依存モジュール(Excel用出力・差し戻し文案・ナレッジ文案・報告文生成)のSkill化はPhase 2で発動安定性を比較検証 |
| 判定基準の詳細(md) | **ツール②で全文渡し** | 全行連鎖評価が必要でRAG(Knowledge)不向き |
| レビュー票 標準指摘一覧 | **ツール①でAI連携用コピーから動的取得**(§4) | 全文渡し原則+保守性(内蔵テキスト廃止・公式テンプレ非改変) |
| ルール資料 | **Knowledge** | 根拠を引く用途 |
| ホスティング環境判定アプリ | **連携しない** | 出力がMgmtのスレッド投稿に含まれる(ツール①で取得済み) |
| 技術相談ナレッジ(マスタ) | **SharePoint Listのまま維持** | List書込はCloud Flow経由で可。Excel化は同時編集・保守で劣化するため不採用 |
| 技術相談ナレッジ(Agent参照) | 暫定: 匿名化xlsxスナップショット(週1)→Knowledge / 本命: List直接検索フロー(Phase 2) | KnowledgeにListを直接接続できない制約の回避 |
| レビュー票テンプレのForms化 | **不採用** | 指摘リストは案件ごとに動的でFormsの静的設問と不整合。版管理・承認記録の役割も崩れる |
| 構成図の解析 | PoCでは実装しない(Eng目視) | 画像入力不可の環境制約。Phase 2接続点として予約 |
| サブエージェント分割 | PoCでは分割しない(Instructionsモジュール) | Connected agents未確認。品質劣化時にPhase 2で評価 |

### Knowledgeの役割マップ

| 資料 | 用途 | 扱い |
|---|---|---|
| salus_cloudservices_review_status 1.xlsx | サービス単位のDCS利用可否(Accessed)照合 | 判定根拠に使用可 |
| Salus - Guardrails - v20260805.xlsx | Layer制限等ガードレール適合確認 | 判定根拠に使用可 |
| TOM2024関連 | SSDLC観点・ホスティング判定根拠 | 判定根拠に使用可 |
| AWS-PPA-2026-Eligible-Use.md | AWS特有の制限 | **AWS案件のみ**参照 |
| Microsoft Azure Independence Requirements.md | Azureの商務要件 | **Azure案件のみ**参照 |
| (GCP) | 固有資料なし | AWS/Azure同等扱い+ノックアウト③(GCP必須→基本Disconnected) |
| 技術相談ナレッジ(匿名化スナップショット) | 過去案件の**参考のみ** | **判定根拠にしない**(過去環境・案件特有事情による見かけ上の矛盾がありうる) |

## 4. ツール①改修: 標準指摘一覧の読取(AI連携用コピー方式)

**フロー変更(いいえ側の後半、既存スイッチを置換)**:

```
CSP確定(既存)
 → 条件: CSP確定=「不明」
    はい: reviewSheetStandardItems に「CSPを指定して再実行」案内をセット
    いいえ:
      1. SharePoint「フォルダー内のファイルの一覧」(AI連携用フォルダ)
      2. アレイのフィルター処理: ファイル名に concat('アーキテクチャレビュー票(', outputs('CSP確定'), ')') を含む → 先頭1件
      3. Excel Online (Business)「表内に存在する行を一覧表示」: ファイル=手順2の識別子(動的) / テーブル=T_標準指摘(3ファイル共通名)
      4. ヘッダー追記:【標準指摘一覧】CSP / 版(ファイル名から式で抽出) / 全NN行(length()で算出)
      5. Apply to each: 各行を [No.x] 環境:… / 対応区分:… / カテゴリ:… / 指摘概要:… / 指摘内容:… / 参考資料:… 形式で追記
      6. 末尾に「以上、全NN行」を追記(Agentの検算用)
 → Agent応答(既存のまま。出力名・スキーマ変更なし)
```

- 改版追随: 手順2が部分一致検索のため、版数入りファイル名の変更に自動追随
- フォールバック: Excel読取失敗時もスレッドは返す(reviewSheetStandardItems=「標準指摘一覧の取得に失敗しました。テンプレを直接確認してください」)
- コピーのテーブル化時、**結合セルが含まれる範囲はテーブル化不可** → コピー側なら結合解除やエクスポート用シート追加も自由(公式テンプレ非改変のため)

## 5. Agent出力フォーマット(拡張)

```text
## ホスティング判定サマリ
- RITM: / CSP:
- DCS利用可否(Gate): 可 / 不可(→Mgmt差し戻し文案) / 判断不可
- 環境仮判定: Sandbox / Hub / Hub(PoC) / Disconnected / 判断不可
- 判定経路: 各段の結果と根拠(判定基準ID、スレッド記載箇所)
- ノックアウト該当: なし / あり(カテゴリと理由)
- 要人手確認(△): 一覧
- 追加ヒアリング(×・情報不足): 質問文案(レビュー票転記用)
- 構成図: Eng目視確認が必要

## レビュー票への編集指示
①採用 / ②添削 / ③対象外 / ④新規追加 + 検算(①+②+③=全NN行)

## 技術相談ナレッジ登録文案(クローズ時用)
- タイトル(RITM)/ 相談概要 / 判定結果 / 根拠 / 特記事項

※本出力はすべてAgentの案。判定の確定・承認・利用者連絡・ナレッジ登録の実行判断は人が行う。
```

## 6. 段階導入ロードマップ

| フェーズ | 内容 |
|---|---|
| PoC(現行) | ツール①+レビュー票仕分け。結合テスト完了・品質評価(進行中) |
| PoC拡張(本再整理) | AI連携用フォルダ新設 / ツール①改修 / ツール②③新規 / Instructions v3.0 / Knowledge整備 / テスト3パターン追加(DCS対象外・Sandbox・ノックアウト→Disconnected) |
| Phase 2 | 構成図エージェント / Connected agents分割 / **状況依存モジュールのSkill化+発動安定性の比較検証** / Workflow(GA後)による自動起点+Human Review / Search past cases(List直接検索)本採用 / レビュー票xlsx自動転記 |

## 7. 実装タスク(PoC拡張)

- [ ] **AI連携用フォルダ新設**(格納・権限・現行版のみ運用)
- [ ] テンプレコピー3ファイル作成+テーブル化(T_標準指摘)+手順書化
- [ ] ツール①改修(§4)+フォールバック+結合テスト(100秒制限内の実測含む)
- [ ] **判定基準一覧の確定→md化(Eng)** ※ID付与・版数・総行数・○△×を含める(§2)
- [ ] ツール②「ホスティング判定基準取得」作成(3アクション・非同期応答オフ・発行)
- [ ] 技術相談ナレッジListの列構成確認(列名・型・必須・選択肢値)→ ツール③作成
- [ ] Instructions v3.0 作成・貼付
- [ ] Knowledge追加(salus_cloudservices / Guardrails / 匿名化ナレッジスナップショット)
- [ ] テストケース追加3パターンのスレッド作成(Mgmt協力)

## 8. 確認事項(残り)

| # | 確認内容 | 確認先 | 状態 |
|---|---|---|---|
| 1 | ~~テンプレへのテーブル追加可否~~ → **変更不可と確定。AI連携用フォルダ方式で解決** | — | 解決済み |
| 2 | AI連携用フォルダの場所・権限(フローの接続アカウントが読めること) | Eng | 未 |
| 3 | 技術相談ナレッジListの列構成(ツール③入力設計に使用。List変更は不要) | ナレッジOwner | 未 |
| 4 | ~~スレッド返信ルールの運用依頼~~ → **運用上の前提メモに変更**(§1)。回答をスレッドに残すことのみ徹底 | — | 解決済み |
| 5 | 判定基準一覧の内容確定(Excel内コメントの論点解消)→ md化 | 基準Owner / Eng | 進行中 |

---

## 付録: Instructionsモジュール構成案(v3.0の骨格)

```text
# 役割(拡張): ホスティング環境判定+レビュー票編集指示+ナレッジ登録文案
# 進め方
1. RITM特定 → ツール①「Teamsスレッド内容取得」実行(利用者回答の返信も含めて読む)
2. ツール②「ホスティング判定基準取得」実行
3. Gate判定(No.1〜3)→ 不可ならMgmt差し戻し文案を出力して終了
4. 環境判定パイプライン(No.4→5→6-8→SSDLC→ノックアウト)を判定基準IDの順に全件評価
   - ○=自動判定+根拠(ID引用) / △=判定案+要人手確認 / ×=判定せず追加ヒアリング文案
5. 標準指摘一覧の全行仕分け(①②③④+検算)
6. 統合出力(§5フォーマット)。全回答受領済みなら仮判定確定(案)+ナレッジ登録文案も出力
7. Engから「ナレッジに登録して」と明示指示があった場合のみ、文案の最終確認を取ってツール③実行
# 厳守事項(追加分)
- 判定は常に「仮判定(案)」。確定・承認・利用者連絡・ナレッジ登録実行の判断は人
- Hub第1優先。ノックアウト該当時のみDisconnected
- 技術相談ナレッジは参考のみ。判定根拠にしない
- AWS-PPAはAWS案件のみ、Azure IndependenceはAzure案件のみ。GCPはAWS/Azure同等+ノックアウト③
- 判定不能は「判断不可(情報不足)」+質問案でエスカレーション
```
