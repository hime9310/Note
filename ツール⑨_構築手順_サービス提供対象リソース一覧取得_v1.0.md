# ツール⑨ サービス提供対象リソース一覧取得 — 構築手順(共通パターン版)

| 項目 | 内容 |
|---|---|
| 版 | v1.0(2026-09-14) |
| 元になる設計 | ツール一覧 v2.3 の「0.1 参照データ取得ツールの共通パターン」と「⑨ サービス提供対象リソース一覧取得」 |
| 作るもの | Copilot Studio のエージェント フロー(名前: `サービス提供対象リソース一覧取得`)。入力なし。出力 `resourceList`(md全文+先頭行に全行数)と `count`(全行数) |
| 前提 | AI連携用フォルダに `サービス提供対象リソース一覧.md` がある(1ファイル1表。表以外の行は `\|` で始めない。列: カテゴリ / サービス種類 / サービス名 / 利用者による動作検証必要) |

---

## 1. フローの全体像(上から順に7アクション)

```
エージェントがフローを呼び出したとき(入力なし)
  ↓
パスによるファイル コンテンツの取得(SharePoint)   … 作成済み
  ↓
作成: md文字列
  ↓
作成: 行の配列
  ↓
アレイのフィルター処理: 表の行
  ↓
作成: 全行数
  ↓
エージェントに応答する(resourceList / count)
```

アクションを追加したら、必ずタイトルを右の名前(`md文字列` など)に変えてください。あとの式がその名前で参照します(タイトルをクリックすると名前を変えられます)。

---

## 2. 各アクションの設定

### 2-1. パスによるファイル コンテンツの取得(作成済み)

| 項目 | 値 |
|---|---|
| サイトのアドレス | JP CMS - https://jpdeloitte.sharepoint.com/sites/Cloud681 |
| ファイル パス | …/Agent連携用/サービス提供対象リソース一覧.md(ピッカーで選択) |
| コンテンツ タイプを推測する | はい |

### 2-2. 作成: `md文字列`

「作成」を追加し、名前を `md文字列` にする。「入力」に次のどちらかを入れる。

- まず動的なコンテンツから「ファイル コンテンツ」を入れて、一度テスト実行する。出力が読めるmdの文字列ならそのままでよい。
- 出力が `{"$content-type": "...", "$content": "base64…"}` の形なら、「入力」を式に切り替えて次を入れる(③④⑤の文字列化と同じ。アクション名の空白は `_` になる)。

```
base64ToString(body('パスによるファイル_コンテンツの取得')?['$content'])
```

### 2-3. 作成: `行の配列`

「作成」を追加し、名前を `行の配列` にする。「入力」は式で次を入れる(改行コードの違いを吸収して1行ずつの配列にする)。

```
split(replace(outputs('md文字列'), decodeUriComponent('%0D'), ''), decodeUriComponent('%0A'))
```

### 2-4. アレイのフィルター処理: `表の行`

「アレイのフィルター処理」を追加し、名前を `表の行` にする。

| 項目 | 値 |
|---|---|
| 差出人(元の配列) | 式 `outputs('行の配列')` |
| 条件の左辺 | 式 `item()` |
| 演算子 | 次の値で始まる |
| 条件の右辺 | 半角の縦棒 `\|` を1文字だけ(前後に空白を入れない) |

詳細モードで書く場合は次の1行。

```
@startsWith(item(), '|')
```

### 2-5. 作成: `全行数`

「作成」を追加し、名前を `全行数` にする。「入力」は式で次を入れる(見出し行と区切り行の2行を引いた数)。

```
length(skip(body('表の行'), 2))
```

### 2-6. エージェントに応答する

「出力を追加する」→「テキスト」を2回。値はどちらも式で入れる。

| 名前 | 値(式) | 説明欄(あれば) |
|---|---|---|
| `resourceList` | `concat('資料: サービス提供対象リソース一覧.md / 全行数: ', string(outputs('全行数')), decodeUriComponent('%0A'), outputs('md文字列'))` | サービス提供対象リソース一覧の全文(md表)。先頭行に資料名と全行数 |
| `count` | `string(outputs('全行数'))` | 表の全行数(数字の文字列) |

応答アクションの「…」→「設定」にある「非同期応答」はオフのまま。

---

## 3. テスト

1. 保存 → テスト(手動)→ 実行。
2. `count` が、mdファイルの表の行数(見出し行と区切り行を除く)と一致することを目視で確認する。
3. `resourceList` の1行目が「資料: サービス提供対象リソース一覧.md / 全行数: NN」、2行目以降がmdの全文になっていることを確認する。
4. 一致しないときの見どころ: (a) mdに表以外で `\|` から始まる行がないか(表は1ファイル1つ) (b) セル内改行が生の改行になっていないか(`<br>` にする) (c) `md文字列` の出力が本当に文字列か(base64のままなら2-2の式に切り替える)。

---

## 4. 公開とAgentへの追加

1. フローを公開する。
2. アーキテクチャレビュー票作成Agent → ツール → 追加 → フロー `サービス提供対象リソース一覧取得` を選ぶ。
3. ツールの説明欄に Instructions集 v8.3「アーキテクチャレビュー票作成Agent」のツール表にある⑨の説明文を貼る。入力パラメーターはなし。

---

## 5. 同じ作りで③④⑤を改修するとき

③ホスティング判定基準取得 / ④Salus Status一覧取得 / ⑤アーキテクチャレビュー票テンプレート取得も、2-2〜2-5の4アクションを「ファイル コンテンツの取得」と「応答」の間に挟み、応答に `count` を足すだけです(出力名は ③ `criteria` / ④ `salusStatus` / ⑤ `template`+`fileName`。⑤はCSPでファイルを選ぶ分岐が先頭にある)。②は加えて「列位置で絞り込む」フィルターが入るので、ツール一覧 v2.3 の②の項を参照してください。
