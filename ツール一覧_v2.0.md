# アーキテクチャレビューAgent ツール一覧(名前・用途・設計)

## 0. 一覧

| No | ツール名 | 用途 | 持ち主(呼ぶAgent) | 状況 |
|---|---|---|---|---|
| ① | Teamsスレッド内容取得 | RITM番号でスレッドを特定し、親投稿・全返信・添付一覧・CSP判定を返す | ホスティング判定Agent / 構成図チェックAgent / アーキテクチャレビュー票作成Agent | 実装済み |
| ② | 構成図チェック観点取得 | 観点表(Excel)から対象環境・CSP・1次/2次に該当する観点を絞り、適用行数を付けて返す | 構成図チェックAgent | **要改修**(v2.0設計) |
| ③ | ホスティング判定基準取得 | ホスティング判定基準.md(ノックアウト要件)の全文を返す | ホスティング判定Agent | 実装済み |
| ④ | Salus Status一覧取得 | Salus_Status一覧.mdの全文を返す | ホスティング判定Agent | **要改修**(検証一覧の出力を外す) |
| ⑤ | アーキテクチャレビュー票テンプレート取得 | CSP別テンプレートmd(全NN行・No付き)の全文を返す | アーキテクチャレビュー票作成Agent | 実装済み |
| ⑥ | アーキテクチャレビュー票Excel作成 | 雛形をコピーし、行データJSONをテーブルへ書き込み、件数とリンクを返す | アーキテクチャレビュー票作成Agent | 実装済み(No.の扱いはAgent側の変更のみ) |
| ⑦ | 回答済みアーキテクチャレビュー票取得 | チャネルフォルダから回答済みExcelを特定し、全行をmd表で返す | アーキテクチャレビュー票作成Agent | 実装済み |
| ⑧ | 技術相談ナレッジ追記 | 承認済み文案16項目をSharePoint Listへ登録し、リンクを返す | ホスティング判定Agent | **実装未定**(未実装の間はEngが手動登録) |
| ⑨ | サービス提供対象リソース一覧取得 | サービス提供対象リソース一覧.md(動作検証必要○)の全文を返す | アーキテクチャレビュー票作成Agent | **新設・未実装** |

共通の作り方(全ツール): トリガー「エージェントがフローを呼び出したとき」/ 応答「エージェントに応答する」(非同期応答オフ)/ 同じ200応答が2つある場合は出力スキーマを完全一致 / 条件・フィルターは文字列同士で比較 / SharePointのパスはピッカーで選択 / 公開後にAgentへ追加し、説明文はInstructions集v8.0の各Agentの表を設定。

---

## ① Teamsスレッド内容取得

| 項目 | 内容 |
|---|---|
| 用途 | 共通_Engineering標準化チャネルからRITM番号でスレッド(親投稿+全返信)を特定し、本文・添付一覧と、本文から判定したCSPを返す。ホスティング判定Agent(判定・再判定のたびに最新を読む)、構成図チェックAgent(判定トリガー「回答:」の材料として利用者回答を読む)、アーキテクチャレビュー票作成Agent(案件情報とExcelファイル名用のCSP)が使う |
| 入力 | RITM番号(テキスト・必須)/ CSP(テキスト・任意。指定時はその値を優先) |
| 処理 | ①チャネルのメッセージを検索(件名・本文にRITM番号を含む親投稿)→ ②親投稿の返信を全件取得 → ③HTML→テキスト変換 → ④添付ファイル一覧を整形 → ⑤本文の語句からCSP(Azure/AWS/GCP/不明)を判定(入力CSPがあれば優先)→ ⑥結合して応答 |
| 出力 | threadText(親投稿+全返信の本文。投稿者・日時付き)/ attachments(添付ファイル名の一覧)/ csp(Azure / AWS / GCP / 不明)/ subject(件名) |
| 参照先 | Teams: 共通_Engineering標準化チャネル |
| 例外 | スレッドが見つからない場合は「該当スレッドなし」を返す(Agentは RITM番号の誤りとしてEngへ確認) |
| 状況 | 実装済み。v2.0での変更なし(持ち主に構成図チェックAgentを追加) |

## ② 構成図チェック観点取得(v2.0設計)

| 項目 | 内容 |
|---|---|
| 用途 | 構成図チェック観点表(Excel)から、対象環境・CSP・処理(1次/2次)に該当する観点だけを取り出し、**適用行数**と観点表の版を先頭に付けて「1観点=1ブロック」のテキストで返す。構成図チェックAgentが観点の実行仕様と検算R1の分母として使う。行数の注記や md への変換を人が行わずに済むようにする |
| 入力 | 環境(テキスト: Hub / Disconnected)/ CSP(テキスト: AWS / Azure / GCP)/ 処理(テキスト: 1次 / 2次) |
| 処理 | ①Excel Online (Business)「表内に存在する行を一覧表示」で観点表テーブルの全行を取得 → ②アレイのフィルター処理: 環境列が入力環境を含む or 「共通」 → ③フィルター: 対象CSP列が入力CSPを含む or 「共通」 → ④フィルター: 処理=1次なら「1次・2次チェック」列が1次の行のみ、2次なら全行 → ⑤ `length()` で件数 → ⑥「選択」で1行を次の形に整形 → ⑦ヘッダー(版・条件・適用行数)+ブロックを改行で結合して応答 |
| 1観点のブロック形式 | `### No.{No} {チェック観点}` / `- 大分類:` / `- 判断基準:` / `- 対象CSP:` / `- 環境:` / `- 1次・2次:` / `- 判定トリガー(標準):` / `- ネクストアクション(標準):` / `- AI判定:` / `- AI判定方法:` / `- AI出力内容:` / `- テンプレート対象No:` / `- AI注意事項:`(備考のうちAI向けの列がある場合) |
| 出力 | checkpoints(テキスト: 「観点表の版: … / 条件: 環境=… CSP=… 処理=… / 適用行数: NN」+ブロック群)/ count(適用行数。テキストで返す) |
| 参照先 | AI連携用フォルダの構成図チェック観点.xlsx(テーブル名を固定。列: No / 大分類 / チェック観点 / 判断基準 / 対象CSP / 環境 / 1次・2次チェック / 判定トリガー / 判定トリガー(標準) / ネクストアクション / ネクストアクション(標準) / AI判定 / AI判定方法 / AI出力内容 / テンプレート対象No / 備考)。**廃止した観点は「廃止」シートへ移し、テーブルには残さない** |
| 例外 | 適用行数が0の場合は「適用行数: 0」を返す(Agentは条件の誤りとしてEngへ確認)。テーブルが見つからない場合はエラーメッセージを返す |
| 注意 | Excelの日付・改行の扱いはツール⑦と同じ(セル内改行はそのまま保持してよい。縦棒(半角のパイプ記号)は全角「｜」に置換)。列名にピリオドや記号があると内部名が変わる(例: `No.`→`No_x002e_`)ので、式では内部名を使う |
| 状況 | **要改修**(現行はmd全文を返すだけ)。改修までは現行のmd全文版を使い、構成図チェックAgentが自分で絞り込む(検算R1はmd先頭の内訳で代用) |

## ③ ホスティング判定基準取得

| 項目 | 内容 |
|---|---|
| 用途 | ホスティング判定基準.md(ノックアウト要件のみ。基準ID・版数・総行数付き)の全文を返す。ホスティング判定Agentが要件を1件ずつ照合する |
| 入力 | なし |
| 処理 | SharePoint「ファイル コンテンツの取得」→ 文字列化 → 応答 |
| 出力 | criteria(md全文) |
| 参照先 | AI連携用フォルダのホスティング判定基準.md(正式版は作成中) |
| 状況 | 実装済み。変更なし |

## ④ Salus Status一覧取得(旧: サービス照合資料取得)

| 項目 | 内容 |
|---|---|
| 用途 | Salus_Status一覧.mdの全文を返す。ホスティング判定Agentが利用予定サービスのSalus status(Assessed/Deprecated等)を照合する。サービス検証状況(→サービス提供対象リソース一覧)は判定では使わないため、本ツールから外す |
| 入力 | なし |
| 処理 | SharePoint「ファイル コンテンツの取得」→ 文字列化 → 応答 |
| 出力 | salusStatus(md全文) |
| 参照先 | AI連携用フォルダのSalus_Status一覧.md |
| 状況 | **要改修**: 現行の2出力(salusStatus / serviceVerification)から serviceVerification を削除し、ツール名を変更。ツール名を変えるとAgent側で再追加が必要 |

## ⑤ アーキテクチャレビュー票テンプレート取得(旧: アーキテクチャレビュー票取得)

| 項目 | 内容 |
|---|---|
| 用途 | CSPに応じたアーキテクチャレビュー票テンプレートmd(先頭に版数・全NN行。各行にNo)の全文とファイル名を返す。アーキテクチャレビュー票作成Agentが編集指示の分母(検算R2)と、Noを維持した流用・添削の元にする |
| 入力 | CSP(テキスト: AWS / Azure / GCP) |
| 処理 | CSPで固定ファイル名を選択 → SharePoint「ファイル コンテンツの取得」→ 応答 |
| 出力 | template(md全文)/ fileName |
| 参照先 | AI連携用フォルダのアーキテクチャレビュー票テンプレート(AWS).md / (Azure).md / (GCP).md |
| 状況 | 実装済み。変更なし(名称のみ「テンプレート」を明示) |

## ⑥ アーキテクチャレビュー票Excel作成

| 項目 | 内容 |
|---|---|
| 用途 | RITM番号・CSP・行データJSONを受け取り、雛形xlsxをコピーして送付用Excelを作成し、テーブルへ行を追加して書き込み件数とファイルリンクを返す。アーキテクチャレビュー票作成Agentが確定版編集指示から送付用Excelを作る(検算R3) |
| 入力 | RITM番号 / CSP / 行データ(JSON配列。キー: No. / ステータス / 環境 / 対応区分 / カテゴリ / 指摘概要 / 指摘内容 / 対応ガイドライン・参考資料) |
| 処理 | ①雛形xlsxを取得 → ②送付フォルダに「【案件名】(RITMxxxxxxx)アーキテクチャレビュー票(CSP).xlsx」を作成 → ③JSON解析 → ④Officeスクリプト「アーキテクチャレビュー_行追加」でテーブル `T_アーキテクチャレビュー票` へ行追加 → ⑤件数+リンクを応答 |
| 出力 | result(書き込み件数)/ fileLink |
| 参照先 | AI連携用フォルダの雛形xlsx / 送付フォルダ / OneDrive「Documents/Office スクリプト」 |
| v2.0での扱い | **「No.」はJSONの値をそのまま転記**(現行の作りのまま)。連番への振り直しはAgent側で行わない。④不要の行はJSONに含めない(=Excelに書かない)。**将来**: 不要行を「非表示」にしたい場合は、Officeスクリプトに「非表示にするNoの配列」を渡して行を非表示にする処理を追加する |
| 状況 | 実装済み。フロー変更なし |

## ⑦ 回答済みアーキテクチャレビュー票取得

| 項目 | 内容 |
|---|---|
| 用途 | 利用者がスレッドへ添付返送した回答済みExcelをRITM番号だけで特定し、記入内容をmd表で返す。アーキテクチャレビュー票作成Agentが全行を[回答済み]/[未回答]/[要確認]に仕分ける(検算R4)。Engが手で追加した行も含めて返す |
| 入力 | RITM番号(テキスト) |
| 処理 | ①SharePoint「フォルダーの一覧」(共通_Engineering標準化 直下)→ ②フィルター: Name 含む RITM番号 → ③フィルター: `if(endsWith(Name,'.xlsx'), Name, '')` 含む アーキテクチャレビュー票 → ④件数0なら未添付メッセージを応答 → ⑤`last(sort(配列,'LastModified'))` で最新1件 → ⑥Excel「表内に存在する行を一覧表示」(ファイル=対象ファイル.Path、テーブル=T_アーキテクチャレビュー票)→ ⑦「選択」で1行を md表の1行「No ／ ステータス ／ 指摘概要 ／ 指摘内容 ／ 回答内容 ／ 回答者 ／ 回答日」(区切りは半角パイプ)に変換(改行→`<br>`、半角パイプ→全角「｜」、日付シリアル値→yyyy/MM/dd)→ ⑧「対象ファイル: / 全行数: NN件 / md表」を応答 |
| 出力 | answerSheet(対象ファイル+全行数+md表。未添付時は固定メッセージ)/ fileName(未添付時「なし」) |
| 参照先 | JP CMS > ドキュメント > 共通_Engineering標準化 |
| 運用ルール | 返送はスレッドへ添付。ファイル名のRITM部分と「アーキテクチャレビュー票」を変えない。テーブルの行を削除しない(追加は可) |
| 状況 | 実装済み。変更なし。各アクションの設定・式・テスト観点は **ツール設計書_v1.1 §⑦** を参照 |

## ⑧ 技術相談ナレッジ追記(実装未定)

| 項目 | 内容 |
|---|---|
| 用途 | Engが承認したナレッジ文案(16項目)を技術相談ナレッジListへ1件登録し、登録リンクを返す。ホスティング判定Agentが親からの「登録実行」の指示でのみ実行する |
| 入力 | List列に対応した16項目(選択肢列はListの定義値と完全一致) |
| 処理 | SharePoint「項目の作成」→ リンクを応答 |
| 出力 | itemLink |
| 参照先 | 技術相談ナレッジList |
| 状況 | **実装未定**。未実装の間は、ホスティング判定Agentが作った文案を親が提示し、Engが手動でListへ登録する(Instructions集v8.0 親の手順16) |

## ⑨ サービス提供対象リソース一覧取得(新設)

| 項目 | 内容 |
|---|---|
| 用途 | サービス提供対象リソース一覧.md(旧: サービス検証状況.md)の全文を返す。アーキテクチャレビュー票作成Agentが、利用予定サービスのうち「利用者による動作検証必要=○」のものを申し送り事項として票へ追記する |
| 入力 | なし |
| 処理 | SharePoint「ファイル コンテンツの取得」→ 文字列化 → 応答(③④と同じ作り) |
| 出力 | resourceList(md全文。列: カテゴリ / サービス種類 / サービス名 / 利用者による動作検証必要) |
| 参照先 | AI連携用フォルダのサービス提供対象リソース一覧.md |
| 状況 | **新設・未実装**。ツール④から serviceVerification を切り出して作る |

---

## 変更時の注意(全ツール)

- ツール名・ファイル名・テーブル名・列名を変えたら、Instructions集v8.0の説明文と各AgentのInstructionsの名称も合わせて更新する
- 説明文に検算(R1〜R4)を書いているのは、Instructions側の記載が読み飛ばされた場合の二重防止。設計原則・共通ルール集§2を改訂したときは説明文も見直す
- 同じHTTP 200を返す応答が2つあるツール(⑦など)は、出力スキーマの完全一致を保つ(コードビューで揃える)
