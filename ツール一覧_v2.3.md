# アーキテクチャレビューAgent ツール一覧(名前・用途・設計)

| 項目 | 内容 |
|---|---|
| 版 | v2.3 |
| 更新日 | 2026-09-14 |
| 適用 | 設計書v8.3 / Instructions集v8.3 / 設計原則・共通ルール集v1.1。ツールはすべて Copilot Studio の**エージェント フロー**(GA)で作る |
| v1.x(ツール設計書v1.1)からの変更 | ②を「観点表(Excel)を直接読み、環境・CSP・1次/2次で絞って件数付きで返す」設計に変更 / ④を Salus Status一覧のみに変更(検証一覧を外す)/ ⑥は「No.」を編集指示の番号のまま転記(フロー変更なし)/ ⑦の持ち主はアーキテクチャレビュー票作成Agentのまま / ⑧は実装未定 / **⑨サービス提供対象リソース一覧取得を新設** |
| 詳細設計 | ⑦の各アクション設定・式・テスト観点は「ツール設計書_v1.1」を参照。他のツールは本書の設計欄が現時点の設計 |
| **v2.2からの変更(v2.3)** | **参照データをmd表に統一**(設計原則11・共通ルール集§6)。ツール②③④⑤⑨を共通パターン「ファイル取得 → 表の行を数える → (②は列位置で絞り込み) → 件数を付けて返す」に揃えた。②はExcel直読み案(v2.0〜v2.2)を取り下げ、md表を列位置で絞る設計に変更。③④⑤に「全行数」の出力を追加(③はID列の追加を前提)。⑨は新設(共通パターン)。⑥⑦⑧①は変更なし |
| v2.1からの変更(v2.2) | ③の用途に「ノックアウト要件一覧=DCS Hubで使えない条件(1行でも該当→Disconnected、すべて非該当→Hub)」と、サービス名の突き合わせをAgentが柔軟に判断する旨を追記(説明文のみ) |
| v2.0からの変更(v2.1) | ③の参照ファイルを実物の「ノックアウト要件一覧.md」(列: No.(要因番号)/ 要因 / 具体的なケース / 判断区分 / 備考)に統一し、ホスティング判定Agentが【構成図一覧】のサービスをCSPサービス起因の行と突き合わせる使い方を明記。ツールの作りは変更なし(説明文のみ更新) |

---

## 0. 一覧

| No | ツール名 | 用途 | 持ち主(呼ぶAgent) | 状況 |
|---|---|---|---|---|
| ① | Teamsスレッド内容取得 | RITM番号でスレッドを特定し、親投稿・全返信・添付一覧・CSP判定を返す | ホスティング判定Agent / 構成図チェックAgent / アーキテクチャレビュー票作成Agent | 実装済み |
| ② | 構成図チェック観点取得 | 観点表(md表)から対象環境・CSP・1次/2次に該当する行を列位置で絞り、更新日時と適用行数を付けて返す | 構成図チェックAgent | **要改修**(v2.3設計: 行数カウント+列位置フィルター) |
| ③ | ホスティング判定基準取得 | ノックアウト要件一覧.md(ID/No./要因/具体的なケース/判断区分/備考)の全文と全行数を返す | ホスティング判定Agent | **要改修**(行数カウントを追加。md側にID列を追加) |
| ④ | Salus Status一覧取得 | Salus_Status一覧.mdの全文と全行数を返す | ホスティング判定Agent | **要改修**(検証一覧の出力を外し、行数カウントを追加) |
| ⑤ | アーキテクチャレビュー票テンプレート取得 | CSP別テンプレートmd(No付き)の全文と全行数を返す | アーキテクチャレビュー票作成Agent | **要改修**(行数カウントを追加。md先頭の「全NN行」は不要に) |
| ⑥ | アーキテクチャレビュー票Excel作成 | 雛形をコピーし、行データJSONをテーブルへ書き込み、件数とリンクを返す | アーキテクチャレビュー票作成Agent | 実装済み(No.の扱いはAgent側の変更のみ) |
| ⑦ | 回答済みアーキテクチャレビュー票取得 | チャネルフォルダから回答済みExcelを特定し、全行をmd表で返す | アーキテクチャレビュー票作成Agent | 実装済み |
| ⑧ | 技術相談ナレッジ追記 | 承認済み文案16項目をSharePoint Listへ登録し、リンクを返す | ホスティング判定Agent | **実装未定**(未実装の間はEngが手動登録) |
| ⑨ | サービス提供対象リソース一覧取得 | サービス提供対象リソース一覧.md(動作検証必要○)の全文と全行数を返す | アーキテクチャレビュー票作成Agent | **新設・未実装**(共通パターン) |

共通の作り方(全ツール): トリガー「エージェントがフローを呼び出したとき」/ 応答「エージェントに応答する」(非同期応答オフ)/ 同じ200応答が2つある場合は出力スキーマを完全一致 / 条件・フィルターは文字列同士で比較 / SharePointのパスはピッカーで選択 / 公開後にAgentへ追加し、説明文はInstructions集v8.3の各Agentの表を設定。

---

## 0.1 参照データ取得ツールの共通パターン(②③④⑤⑨。md統一)

参照データはすべて「設計原則・共通ルール集v1.1 §6 参照データ(md表)の約束」に従うmdファイル(1ファイル1表)。ツールは次の同じ並びで作る。件数はツールが数え、Agentはその値を検算の分母に使う(人がmdに行数を書かない)。

| 手順 | アクション | 設定・式 |
|---|---|---|
| 1 | SharePoint「ファイル コンテンツの取得」 | サイト: JP CMS / ファイル: AI連携用フォルダの対象md(ピッカーで選択) |
| 2 | 「作成」= md文字列 | ファイル内容を文字列化(現行ツールと同じ。例: `base64ToString(body('ファイル_コンテンツの取得')?['$content'])`。テキストで返る場合はそのまま) |
| 3 | 「作成」= 行の配列 | `split(replace(outputs('md文字列'), decodeUriComponent('%0D'), ''), decodeUriComponent('%0A'))` |
| 4 | 「アレイのフィルター処理」= 表の行 | 元: 行の配列 / 条件(詳細モード): `@startsWith(item(), '\|')` |
| 5 | 「作成」= データ行 | `skip(body('表の行'), 2)`(見出し行と区切り行を除く) |
| 6 | 「作成」= 全行数 | `length(outputs('データ行'))` |
| 7 | (②のみ)「アレイのフィルター処理」= 適用行 | 元: データ行 / 条件は②の項を参照(列位置で判定)→「作成」= 適用行数 `length(body('適用行'))` |
| 8 | (任意) SharePoint「ファイルのメタデータの取得」 | `Modified` を「更新日時」として応答に含める(版の手入力を不要にする) |
| 9 | 「エージェントに応答する」 | `text`(見出し行「資料: ファイル名 / 更新日時: … / 全行数(または適用行数): NN」+改行+md表の全文、②は見出し行+区切り行+適用行だけ)/ `count`(件数。文字列で返す) |

注意: 本書の表の中では縦棒が `\|` と表示されることがあるが、式に書くのは半角の縦棒1文字。ファイルに表が2つ以上あると全行数がずれる(1ファイル1表)。表以外の行を `|` で始めない。セル内改行は `<br>`、セル内の縦棒は全角「｜」。行数が多い場合(数百行)は「作成」→「選択」の組み合わせでも処理時間は数秒程度。フローの「アレイのフィルター処理」で列を参照する式は `trim(split(item(), '|')?[k])`(k=列番号。先頭の `|` の前が空要素になるため1列目がk=1)。

---

## ① Teamsスレッド内容取得

| 項目 | 内容 |
|---|---|
| 用途 | 共通_Engineering標準化チャネルからRITM番号でスレッド(親投稿+全返信)を特定し、本文・添付一覧と、本文から判定したCSPを返す。ホスティング判定Agent(判定・再判定のたびに最新を読む)、構成図チェックAgent(判定トリガー「回答:」の材料として利用者回答を読む)、アーキテクチャレビュー票作成Agent(案件情報とExcelファイル名用のCSP)が使う |
| 入力 | RITM番号(テキスト・必須)/ CSP(テキスト・任意。指定時はその値を優先) |
| 処理 | ①チャネルのメッセージを検索(件名・本文にRITM番号を含む親投稿)→ ②親投稿の返信を全件取得 → ③HTML→テキスト変換 → ④添付ファイル一覧を整形 → ⑤本文の語句からCSP(Azure/AWS/GCP/不明)を判定(入力CSPがあれば優先)→ ⑥結合して応答 |
| 出力 | threadText(親投稿+全返信の本文。投稿者・日時付き)/ attachments(添付ファイル名の一覧)/ csp(Azure / AWS / GCP / 不明)/ subject(件名) |
| 参照先 | Teams: 共通_Engineering標準化チャネル |
| 例外 | スレッドが見つからない場合は「該当スレッドなし」を返す(Agentは RITM番号の誤りとしてEngへ確認) |
| 状況 | 実装済み。v2.0での変更なし(持ち主に構成図チェックAgentを追加) |

## ② 構成図チェック観点取得(v2.3設計・md統一)

| 項目 | 内容 |
|---|---|
| 用途 | 構成図チェック観点表(md表)から、対象環境・CSP・処理(1次/2次)に該当する行だけを取り出し、更新日時と**適用行数**を先頭に付けて返す。構成図チェックAgentが観点の実行仕様と検算R1の分母として使う。絞り込みと件数はフローが行う(Agentは数え直さない) |
| 入力 | 環境(テキスト: Hub / Disconnected)/ CSP(テキスト: AWS / Azure / GCP)/ 処理(テキスト: 1次 / 2次) |
| 処理 | 共通パターン1〜6 → ⑦「アレイのフィルター処理」(元: データ行)で次の3条件をすべて満たす行を残す: (a)環境列(k=6)が入力環境を含む または「共通」を含む / (b)対象CSP列(k=5)が入力CSPを含む または「共通」を含む / (c)処理が「2次」なら無条件、「1次」なら1次・2次チェック列(k=7)が「1次」を含む → 適用行数=`length(...)` → 応答 |
| フィルター条件の式(詳細モード) | `@and(or(contains(trim(split(item(),'\|')?[6]), triggerBody()?['text_1']), contains(trim(split(item(),'\|')?[6]), '共通')), or(contains(trim(split(item(),'\|')?[5]), triggerBody()?['text_2']), contains(trim(split(item(),'\|')?[5]), '共通')), or(equals(triggerBody()?['text_3'], '2次'), contains(trim(split(item(),'\|')?[7]), '1次')))`(text_1=環境 / text_2=CSP / text_3=処理。実際のトリガー入力名に合わせる) |
| md表の列順(固定・k=列番号) | 1 No / 2 大分類 / 3 チェック観点 / 4 判断基準 / 5 対象CSP / 6 環境 / 7 1次・2次チェック / 8 判定トリガー(標準)/ 9 ネクストアクション(標準)/ 10 AI判定 / 11 AI判定方法 / 12 AI出力内容 / 13 テンプレート対象No / 14 備考(AI向けの注意)。**列を足すときは15列目以降に追加し、順番を変えない**(式が列番号を参照するため) |
| 出力 | checkpoints(テキスト: 「観点表: ファイル名 / 更新日時: … / 条件: 環境=… CSP=… 処理=… / 適用行数: NN」+改行+見出し行+区切り行+適用行のmd表)/ count(適用行数。文字列) |
| 参照先 | AI連携用フォルダの構成図チェック観点.md(業務側がExcelで編集 → 標準化列を含めてmd表へ変換して配置。廃止した観点は表に含めない) |
| 例外 | 適用行数が0の場合は「適用行数: 0」を返す(Agentは条件の誤りとしてEngへ確認)。ファイルが無い場合はエラーメッセージを返す |
| 注意 | 環境・対象CSP・1次・2次チェックの列の値は標準表記に揃える(Hub / Disconnected / 共通、AWS / Azure / GCP / 共通、1次 / 2次)。表記が揺れると絞り込みから落ちる。セル内改行は `<br>`、縦棒は全角「｜」 |
| 状況 | **要改修**(現行はmd全文を返すだけ)。改修までは現行のmd全文版を使い、構成図チェックAgentが自分で絞り込む(検算R1の分母はAgentが数える。暫定) |

## ③ ホスティング判定基準取得(v2.3: 全行数を追加)

| 項目 | 内容 |
|---|---|
| 用途 | ノックアウト要件一覧.md(「DCS Hubで使えない条件」の一覧。1行でも該当すればDisconnected、すべて非該当ならHub)の全文と**全行数**を返す。ホスティング判定Agentが全行を行順に1件ずつ照合し、全行数を検算R5の分母にする。判断区分に具体的なサービス名・機能・構成パターンがある行(要因④「CSPサービス起因: DCS Hubで利用できないマネージドサービスを利用」など)は【構成図一覧】の■サービス一覧・■NW情報と突き合わせて該当の有無を決める(表記ゆれ・機能名での言及はAgentがクラウドサービスの知識で柔軟に同一判断。根拠必須、不確かなら「候補(要確認)」)。それ以外の行(NW要件・GCP利用・構成・アセット特性・SLA)はスレッドと■NW情報を根拠に判定する。備考のAI向け指示・例外条件はAgentがそのまま従う |
| 入力 | なし |
| 処理 | 共通パターン1〜6、8、9(絞り込みなし) |
| md表の列順(固定) | 1 ID(**新設**: KO-01, KO-02…。行を一意に指すため)/ 2 No.(要因番号①〜⑦)/ 3 要因 / 4 具体的なケース / 5 判断区分 / 6 備考 |
| 出力 | criteria(テキスト: 「資料: ノックアウト要件一覧.md / 更新日時: … / 全行数: NN」+改行+md全文)/ count(全行数。文字列) |
| 参照先 | AI連携用フォルダのノックアウト要件一覧.md |
| md側の作業 | 先頭にID列を追加する(KO-01から連番。廃止した行のIDは再利用しない)。AI向けの指示・条件は現状どおり備考に書く。表以外の行(タイトル・版)は `\|` で始めない |
| 状況 | **要改修**(現行はmd全文のみ。共通パターンの3〜6・8を追加し、応答に全行数を追加)。改修までは現行版を使い、Agentが行数を数える(暫定) |

## ④ Salus Status一覧取得(v2.3: 全行数を追加。旧: サービス照合資料取得)

| 項目 | 内容 |
|---|---|
| 用途 | Salus_Status一覧.md(md表)の全文と全行数を返す。ホスティング判定Agentが【構成図一覧】のサービス全件を1件ずつ突き合わせ、Salus status(Assessed/Deprecated等)を確認する。照合の分母は■サービス一覧の件数(全行数は参考) |
| 入力 | なし |
| 処理 | 共通パターン1〜6、8、9(絞り込みなし) |
| 出力 | salusStatus(テキスト: 「資料: Salus_Status一覧.md / 更新日時: … / 全行数: NN」+改行+md全文)/ count(全行数。文字列) |
| 参照先 | AI連携用フォルダのSalus_Status一覧.md(列順は現行のまま固定。サービス名の列を先頭に) |
| 状況 | **要改修**: 現行の2出力(salusStatus / serviceVerification)から serviceVerification を削除し、ツール名を変更。共通パターンの行数カウントを追加。ツール名を変えるとAgent側で再追加が必要 |

## ⑤ アーキテクチャレビュー票テンプレート取得(v2.3: 全行数を追加。旧: アーキテクチャレビュー票取得)

| 項目 | 内容 |
|---|---|
| 用途 | CSPに応じたアーキテクチャレビュー票テンプレートmd(md表。各行にNo)の全文と**全行数**とファイル名を返す。アーキテクチャレビュー票作成Agentが全行数を検算R2の分母にし、Noを維持した流用・添削の元にする。md先頭の「全NN行」の手書きは不要になる |
| 入力 | CSP(テキスト: AWS / Azure / GCP) |
| 処理 | CSPで固定ファイル名を選択(条件分岐または `if()` でファイルパスを決める)→ 共通パターン1〜6、8、9(絞り込みなし) |
| md表の列順(固定) | 1 No / 2 ステータス / 3 環境 / 4 対応区分 / 5 カテゴリ / 6 指摘概要 / 7 指摘内容 / 8 対応ガイドライン・参考資料(ツール⑥の行データJSONのキーと同じ並び) |
| 出力 | template(テキスト: 「資料: ファイル名 / 更新日時: … / 全行数: NN」+改行+md全文)/ count(全行数。文字列)/ fileName |
| 参照先 | AI連携用フォルダのアーキテクチャレビュー票テンプレート(AWS).md / (Azure).md / (GCP).md |
| 状況 | **要改修**(現行は全文とファイル名のみ。共通パターンの行数カウントを追加) |

## ⑥ アーキテクチャレビュー票Excel作成

| 項目 | 内容 |
|---|---|
| 用途 | RITM番号・CSP・行データJSONを受け取り、雛形xlsxをコピーして送付用Excelを作成し、テーブルへ行を追加して書き込み件数とファイルリンクを返す。アーキテクチャレビュー票作成Agentが確定版編集指示から送付用Excelを作る(検算R3) |
| 入力 | RITM番号 / CSP / 行データ(JSON配列。キー: No. / ステータス / 環境 / 対応区分 / カテゴリ / 指摘概要 / 指摘内容 / 対応ガイドライン・参考資料) |
| 処理 | ①雛形xlsxを取得 → ②送付フォルダに「【案件名】(RITMxxxxxxx)アーキテクチャレビュー票(CSP).xlsx」を作成 → ③JSON解析 → ④Officeスクリプト「アーキテクチャレビュー_行追加」でテーブル `T_アーキテクチャレビュー票` へ行追加 → ⑤件数+リンクを応答 |
| 出力 | result(書き込み件数)/ fileLink |
| 参照先 | AI連携用フォルダの雛形xlsx / 送付フォルダ / OneDrive「Documents/Office スクリプト」 |
| v2.0での扱い | **「No.」はJSONの値をそのまま転記**(現行の作りのまま)。連番への振り直しはAgent側で行わない。④不要の行はJSONに含めない(=Excelに書かない)。**将来**: 不要行を「非表示」にしたい場合は、Officeスクリプトに「非表示にするNoの配列」を渡して行を非表示にする処理を追加する |
| 状況 | 実装済み。フロー変更なし |

## ⑦ 回答済みアーキテクチャレビュー票取得

| 項目 | 内容 |
|---|---|
| 用途 | 利用者がスレッドへ添付返送した回答済みExcelをRITM番号だけで特定し、記入内容をmd表で返す。アーキテクチャレビュー票作成Agentが全行を[回答済み]/[未回答]/[要確認]に仕分ける(検算R4)。Engが手で追加した行も含めて返す |
| 入力 | RITM番号(テキスト) |
| 処理 | ①SharePoint「フォルダーの一覧」(共通_Engineering標準化 直下)→ ②フィルター: Name 含む RITM番号 → ③フィルター: `if(endsWith(Name,'.xlsx'), Name, '')` 含む アーキテクチャレビュー票 → ④件数0なら未添付メッセージを応答 → ⑤`last(sort(配列,'LastModified'))` で最新1件 → ⑥Excel「表内に存在する行を一覧表示」(ファイル=対象ファイル.Path、テーブル=T_アーキテクチャレビュー票)→ ⑦「選択」で1行を md表の1行「No ／ ステータス ／ 指摘概要 ／ 指摘内容 ／ 回答内容 ／ 回答者 ／ 回答日」(区切りは半角パイプ)に変換(改行→`<br>`、半角パイプ→全角「｜」、日付シリアル値→yyyy/MM/dd)→ ⑧「対象ファイル: / 全行数: NN件 / md表」を応答 |
| 出力 | answerSheet(対象ファイル+全行数+md表。未添付時は固定メッセージ)/ fileName(未添付時「なし」) |
| 参照先 | JP CMS > ドキュメント > 共通_Engineering標準化 |
| 運用ルール | 返送はスレッドへ添付。ファイル名のRITM部分と「アーキテクチャレビュー票」を変えない。テーブルの行を削除しない(追加は可) |
| 状況 | 実装済み。変更なし。各アクションの設定・式・テスト観点は **ツール設計書_v1.1 §⑦** を参照 |

## ⑧ 技術相談ナレッジ追記(実装未定)

| 項目 | 内容 |
|---|---|
| 用途 | Engが承認したナレッジ文案(16項目)を技術相談ナレッジListへ1件登録し、登録リンクを返す。ホスティング判定Agentが親からの「登録実行」の指示でのみ実行する |
| 入力 | List列に対応した16項目(選択肢列はListの定義値と完全一致) |
| 処理 | SharePoint「項目の作成」→ リンクを応答 |
| 出力 | itemLink |
| 参照先 | 技術相談ナレッジList |
| 状況 | **実装未定**。未実装の間は、ホスティング判定Agentが作った文案を親が提示し、Engが手動でListへ登録する(Instructions集v8.0 親の手順16) |

## ⑨ サービス提供対象リソース一覧取得(新設・共通パターン)

| 項目 | 内容 |
|---|---|
| 用途 | サービス提供対象リソース一覧.md(旧: サービス検証状況.md)の全文と全行数を返す。アーキテクチャレビュー票作成Agentが、【構成図一覧】のサービスのうち「利用者による動作検証必要=○」のものを申し送り事項として票へ追記する(照合の分母は■サービス一覧の件数。全行数は参考) |
| 入力 | なし |
| 処理 | 共通パターン1〜6、8、9(絞り込みなし) |
| md表の列順(固定) | 1 カテゴリ / 2 サービス種類 / 3 サービス名 / 4 利用者による動作検証必要 |
| 出力 | resourceList(テキスト: 「資料: サービス提供対象リソース一覧.md / 更新日時: … / 全行数: NN」+改行+md全文)/ count(全行数。文字列) |
| 参照先 | AI連携用フォルダのサービス提供対象リソース一覧.md |
| 状況 | **新設・未実装**。ツール④から serviceVerification を切り出し、共通パターンで作る |

## 移行手順(md統一への切り替え。人の作業)

| # | 作業 | 対象 | 備考 |
|---|---|---|---|
| 1 | 観点表: 業務側のExcel(標準化列を含む)を選択してコピーし、md表に変換して `構成図チェック観点.md` として配置 | ② | 変換はM365 Copilot Chat(「この表をMarkdownの表にして」)やVS Codeの拡張で可。列順は②の項のとおり。廃止行は含めない。セル内改行は `<br>` に |
| 2 | ノックアウト要件一覧.md の先頭に ID列(KO-01…)を追加 | ③ | 既存の No.(要因番号)列はそのまま残す |
| 3 | Salus_Status一覧.md / テンプレートmd×3 / サービス提供対象リソース一覧.md を §6の約束(1ファイル1表・表以外の行は `\|` で始めない・行数を書かない)に合わせて確認 | ④⑤⑨ | テンプレートmd先頭の「全NN行」は削除してよい(ツールが数える) |
| 4 | フロー改修: ③④⑤に共通パターンの3〜6・8・9を追加、②に列位置フィルターを追加、⑨を新規作成 | ②③④⑤⑨ | 応答の出力名(text/count)は本書の各項に合わせる。同じ200応答が2つある場合は完全一致 |
| 5 | Agentへの追加と説明文の設定(Instructions集v8.3の表) | 全Agent | ②は構成図チェックAgentに追加(旧: 構成図情報抽出Agentからは外す)。⑨はアーキテクチャレビュー票作成Agentに追加 |
| 6 | テスト: 各ツールを単体で実行し、「全行数(適用行数)」が目視の行数と一致すること、②で条件を変えると件数が変わることを確認 | ②③④⑤⑨ | セル内改行・「｜」を含む行で件数がずれないことも確認 |

---

## 変更時の注意(全ツール)

- ツール名・ファイル名・列名・列順を変えたら、Instructions集v8.3の説明文と各AgentのInstructionsの名称も合わせて更新する(②は列番号を参照する式も直す)
- 説明文に検算(R1〜R5)を書いているのは、Instructions側の記載が読み飛ばされた場合の二重防止。設計原則・共通ルール集§2を改訂したときは説明文も見直す
- 同じHTTP 200を返す応答が2つあるツール(⑦など)は、出力スキーマの完全一致を保つ(コードビューで揃える)
