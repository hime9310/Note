# ツール構築手順書 — ⑥アーキテクチャレビュー票Excel作成 / ⑧技術相談ナレッジ追記

| 項目 | 内容 |
|---|---|
| 版 | v1.0 |
| 更新日 | 2026-09-01 |
| 対象 | Copilot Studio のエージェントフロー(Power Automate)を初めて触る方 |
| 表記 | `【要差し替え】` はご自身の環境の値に置き換えてください |

---

## 0. はじめに: 共通の操作と用語

初めての方向けに、この手順書で繰り返し出てくる操作をまとめます。

### 0.1 用語

| 用語 | 意味 |
|---|---|
| **トリガー** | フローの始まり。今回は「エージェントがフローを呼び出したとき」= Agentがこのツールを使ったら動き出す、という意味 |
| **アクション** | フローの中の1つ1つの処理(ファイルをコピーする、Listに登録する、など)。上から順に実行される |
| **動的コンテンツ** | 前のアクションの結果を、次のアクションで使うための「差し込み」。稲妻⚡アイコンから選ぶ |
| **fx(式)** | 計算や文字の組み立てをするための関数。`concat(...)` などを書く。**先頭に @ は付けない** |
| **表示名** | アクションの名前。後の式から `body('表示名')` の形で参照するため、**変更したら式も直す**必要がある |

### 0.2 よく使う操作

**アクションを追加する**: フロー上の「+」→「アクションの追加」→ 検索欄にキーワードを入力 → 目的のアクションをクリック

**アクションの名前を変える**: アクションのタイトル部分をクリックして編集(または「…」→「名前の変更」)

**fx(式)を入れる**: 入力欄をクリック → 右に出る **fx** アイコンをクリック → 式を入力 → 「追加」

**動的コンテンツを入れる**: 入力欄をクリック → **稲妻⚡** アイコン → 一覧から選択

**保存と公開**: 画面右上の「下書きを保存する」→ その後「**公開**」。**公開しないとAgentから使えません**

### 0.3 必ず守るポイント(トラブルの9割はこれ)

1. **「エージェントに応答する」が複数ある場合、出力の名前・説明を1文字も違わず揃える**(違うと公開時にエラー)
2. **「エージェントに応答する」の設定 →「ネットワーク」→「非同期応答」を必ずオフ**(オンだとAgentが結果を受け取れない)
3. **アクションの表示名を後から変えたら、それを参照している式も直す**
4. 条件分岐(True/False)の**片方の中にあるアクションを、もう片方から参照できない**

---

## 1. ツール⑥ アーキテクチャレビュー票Excel作成

### 1.1 このツールがやること

```
Agentから: RITM番号 + CSP + 行データ(JSON)を受け取る
    ↓
① 雛形Excelをコピーして、案件名付きのファイルを作る
    ↓
② 受け取ったJSONを解析して、1行ずつExcelの表に書き込む
    ↓
Agentへ: 書き込んだ件数 + ファイルのリンクを返す
```

### 1.2 事前準備(フローを作る前に必要)

| # | 準備物 | 内容 |
|---|---|---|
| P1 | **雛形Excel** | `【案件名】(RITMxxxxxxx)アーキテクチャレビュー票(CSP名).xlsx`。中に**テーブル**を作り、テーブル名を `T_アーキテクチャレビュー票` に設定。回答列以外はシート保護 |
| P2 | **雛形の置き場所** | Agent連携用フォルダ(既存のmdと同じ場所でOK) |
| P3 | **送付フォルダ** | 生成したExcelの保存先。`【要差し替え】`(例: Agent連携用フォルダ配下に「送付用」フォルダを作る) |

> **テーブルの作り方**(Excel): データ範囲を選択 → 「挿入」タブ →「テーブル」→「先頭行をテーブルの見出しとして使用する」にチェック → OK → 「テーブルデザイン」タブの左端「テーブル名」を `T_アーキテクチャレビュー票` に変更

### 1.3 Step 1: フローを新規作成する

1. Copilot Studio を開く → 右上の環境が **JP-IT-Int-PAD-Prod** であることを確認
2. 左メニュー「**フロー**」→「**新しいフロー**」→「**エージェント フロー**」
3. トリガーの検索欄に「エージェント」と入力 →「**エージェントがフローを呼び出したとき**」を選択

### 1.4 Step 2: トリガーに入力を3つ追加する

トリガーのカード内「**+ 入力を追加する**」→「**テキスト**」を選び、以下を3回繰り返します(左が名前、右が説明)。

| 入力名 | 説明(Agentに見える文) |
|---|---|
| `RITM番号` | RITMで始まる番号。例: RITM1234567 |
| `CSP` | AWS・Azure・GCP のいずれか |
| `行データ` | アーキテクチャレビュー票に書き込む行データ。JSON配列で渡す |

### 1.5 Step 3: 雛形Excelをコピーする

1. トリガーの下の「+」→「アクションの追加」→ 検索「**ファイルのコピー**」→ **SharePoint** の「**ファイルのコピー**」を選択
2. 表示名を「**雛形コピー**」に変更
3. パラメータを設定:

| 項目 | 設定値 |
|---|---|
| 現在のサイトアドレス | `【要差し替え】`(例: JP CMS - https://jpdeloitte.sharepoint.com/sites/Cloud681) |
| ファイルを移動する | フォルダアイコンから**雛形Excel**を選択(手打ちしない) |
| 移動先サイトのアドレス | 同じサイト |
| 宛先フォルダー | フォルダアイコンから**送付フォルダ**を選択 |
| 別のファイルが既に存在する場合 | **置換** |

4. コピー後のファイル名を指定するため、「**詳細パラメーターを表示**」に「**新しい名前**」欄があれば、そこに **fx** で次を入力:

```
concat('【案件名】(', triggerBody()?['text'], ')アーキテクチャレビュー票(', triggerBody()?['text_1'], ').xlsx')
```

> **解説**: `triggerBody()?['text']` = 1番目の入力(RITM番号)、`text_1` = 2番目の入力(CSP)。「【案件名】」の部分はそのまま残し、送付時に人が書き換えます。
> **注意**: 内部名(`text` / `text_1`)は環境によって変わることがあります。保存後に「コードビュー」で確認するか、**fxエディタの「動的コンテンツ」タブから入力を挿入**すると確実です。
> 「新しい名前」欄が無い場合は、Step 3のあとに SharePoint「**ファイル名の変更**」アクションを足して同じ式を使ってください。

### 1.6 Step 4: 受け取ったJSONを解析する

1. 「+」→「アクションの追加」→ 検索「**JSON**」→ データ操作の「**JSON の解析**」
2. 表示名を「**行データ解析**」に変更
3. 「コンテンツ」欄: **稲妻⚡** → トリガーの「**行データ**」を選択
4. 「スキーマ」欄: 下の「**サンプルから生成する**」ボタンをクリックし、次のサンプルJSONを貼り付けて「完了」:

```json
[
  {
    "No": 1,
    "環境": "DCS共通",
    "対応区分": "回答必須",
    "カテゴリ": "ネットワーク",
    "指摘概要": "サンプル概要",
    "指摘内容": "サンプル内容",
    "参考資料": "サンプルURL"
  }
]
```

> **解説**: スキーマは「どんな形のデータが来るか」の設計図です。サンプルを貼れば自動生成されます。**雛形Excelの列名と、このJSONのキー名を一致させてください**(列名が違う場合はサンプルJSONの方を実際の列名に書き換える)。

### 1.7 Step 5: 1行ずつExcelに書き込む

1. 「+」→「アクションの追加」→ 検索「**それぞれに適用**」→ コントロールの「**それぞれに適用する**」
2. 表示名を「**行の書き込みループ**」に変更
3. 「前のステップから出力を選択します」: **稲妻⚡** →「**行データ解析**」の「**本文**」を選択
4. ループの**中**の「+」→ 検索「**表に行を追加**」→ **Excel Online (Business)** の「**表に行を追加**」
5. 表示名を「**票へ1行追加**」に変更
6. パラメータ:

| 項目 | 設定値 |
|---|---|
| 場所 | `【要差し替え】`(SharePointサイトを選択) |
| ドキュメント ライブラリ | `【要差し替え】`(例: ドキュメント) |
| ファイル | **fx** で `body('雛形コピー')?['Id']` ※コピーしたファイルを指す |
| テーブル | `T_アーキテクチャレビュー票` ※ファイルが動的なので手入力(「カスタム値の入力」を選択) |

7. テーブルの各列の欄が出てくるので、**稲妻⚡** →「行データ解析」の各項目(No / 環境 / 対応区分 / …)を割り当てる
8. 「票へ1行追加」の「**…**」→「**設定**」→「**再試行ポリシー**」を「**既定**」のままにする(コピー直後はファイルがロックされていることがあり、自動で再試行されます)

### 1.8 Step 6: Agentへ結果を返す

1. ループの**外**(下)に「+」→ 検索「**エージェントに応答**」→「**エージェントに応答する**」
2. 「**+ 出力を追加する**」→「**テキスト**」で2つ追加:

| 出力名 | 説明 | 値 |
|---|---|---|
| `result` | 書き込み結果と件数 | **fx**: `concat('アーキテクチャレビュー票Excelを作成しました。書き込み件数: ', string(length(body('行データ解析'))), '件')` |
| `fileLink` | 作成したExcelのリンク | **fx**: `body('雛形コピー')?['{Link}']` |

3. このアクションの「**…**」→「**設定**」→「**ネットワーク**」→「**非同期応答**」を**オフ**

### 1.9 Step 7: 保存・テスト・公開

1. 「**下書きを保存する**」
2. 右上「**テスト**」→「手動」→「テスト」→ 入力欄に:
   - RITM番号: `RITM9999999`
   - CSP: `AWS`
   - 行データ: 次を貼り付け

```json
[{"No":1,"環境":"DCS共通","対応区分":"回答必須","カテゴリ":"NW","指摘概要":"テスト概要","指摘内容":"テスト内容","参考資料":"-"}]
```

3. 実行 → 送付フォルダに `【案件名】(RITM9999999)アーキテクチャレビュー票(AWS).xlsx` ができ、中に1行入っていればOK(テストファイルは後で削除)
4. 「**公開**」

### 1.10 Step 8: Agentにツールとして追加

1. Copilot Studio → 子C「**アーキテクチャレビュー票作成Agent**」を開く
2. 「**ツール**」→「**ツールを追加する**」→ カテゴリ「フロー」→ `アーキテクチャレビュー票Excel作成` を選択
3. 説明を設定:

> RITM番号・CSP・行データ(JSON配列)を渡して、利用者送付用のアーキテクチャレビュー票Excelを生成する。行データの各項目は雛形テーブルの列構成に一致させること。

4. 保存

---

## 2. ツール⑧ 技術相談ナレッジ追記

### 2.1 このツールがやること

```
Agentから: ナレッジList16項目の値を受け取る
    ↓
① SharePoint Listに1件登録する
    ↓
Agentへ: 登録完了メッセージ + アイテムのリンクを返す
```

### 2.2 事前準備

| # | 準備物 | 内容 |
|---|---|---|
| P1 | 技術相談ナレッジList | 既存の「技術相談ナレッジ(Cons)」。**新規作成は不要** |
| P2 | 選択肢の値の確認 | 下表のとおり(変更されていないか確認) |

**選択肢列の値(完全一致で渡す必要あり)**

| 列 | 選択肢 |
|---|---|
| Hosting環境 | DCS Hub / DCS Disconnect / DCS Sandbox |
| CSP | Azure / AWS / GCP |
| 環境利用用途 | PRD / STG / NPD |
| ノックアウトファクター | なし / 技術的制約(GCP利用) / 技術的制約(NW要件要因) / 技術的制約(オートスケール起因) / 技術的制約(CSPサービス起因) / アセットの特性による制約 / DCS HubのSLAによる制約 / ビジネス要件 |

### 2.3 Step 1: フローを新規作成する

1. 左メニュー「**フロー**」→「**新しいフロー**」→「**エージェント フロー**」
2. トリガー: 「**エージェントがフローを呼び出したとき**」

### 2.4 Step 2: トリガーに入力を16個追加する

「**+ 入力を追加する**」→「**テキスト**」を16回繰り返します。**すべてテキスト型**でOKです(選択肢列も文字列で渡します)。

| # | 入力名 | 説明 |
|---|---|---|
| 1 | `Title` | 案件タイトル。【案件名】(RITM番号) の形式 |
| 2 | `Hosting環境` | DCS Hub / DCS Disconnect / DCS Sandbox のいずれか(完全一致) |
| 3 | `CSP` | Azure / AWS / GCP のいずれか(完全一致) |
| 4 | `環境利用用途` | PRD / STG / NPD のいずれか(完全一致) |
| 5 | `利用者` | 利用者名・部署 |
| 6 | `アセット名` | 対象アセット名 |
| 7 | `利用サービス` | 利用予定のクラウドサービス |
| 8 | `利用者要求事項` | 利用者からの要求事項(要件) |
| 9 | `利用者確認事項` | 利用者へ確認した事項・確認結果 |
| 10 | `Cons技術レビュー` | 技術レビューの内容 |
| 11 | `判断軸判断根拠` | 判定の判断軸・根拠(基準ID・資料名を含む) |
| 12 | `申し送り事項` | 次工程・運用への申し送り |
| 13 | `ノックアウトファクター` | 選択肢のいずれか(完全一致)。該当なしは「なし」 |
| 14 | `ノックアウトファクター2` | ノックアウト該当の詳細・補足 |
| 15 | `Teams` | 対象Teamsスレッドのリンク |
| 16 | `その他記入事項` | 補足事項 |

> **入力が多くて大変な場合**: 1つずつ追加する以外に方法はありませんが、名前と説明をこの表からコピペすれば早く済みます。

### 2.5 Step 3: Listに登録するアクションを追加

1. 「+」→「アクションの追加」→ 検索「**項目の作成**」→ **SharePoint** の「**項目の作成**」
2. 表示名を「**ナレッジ登録**」に変更
3. パラメータ:

| 項目 | 設定値 |
|---|---|
| サイトのアドレス | `【要差し替え】`(技術相談ナレッジListがあるサイト) |
| リスト名 | `技術相談ナレッジ(Cons)` ※ドロップダウンから選択 |

4. サイトとリストを選ぶと、**Listの列がそのまま入力欄として下に並びます**。各欄に **稲妻⚡** から対応するトリガー入力を割り当てます。

| List列(表示名) | 割り当てるトリガー入力 |
|---|---|
| タイトル | Title |
| Hosting環境 | Hosting環境 |
| CSP | CSP |
| 環境利用用途 | 環境利用用途 |
| 利用者 | 利用者 |
| アセット名 | アセット名 |
| 利用サービス | 利用サービス |
| 利用者要求事項(要件) | 利用者要求事項 |
| 利用者確認事項 | 利用者確認事項 |
| Cons技術レビュー | Cons技術レビュー |
| 判断軸/判断根拠 | 判断軸判断根拠 |
| 申し送り事項 | 申し送り事項 |
| ノックアウトファクター | ノックアウトファクター |
| ノックアウトファクター2 | ノックアウトファクター2 |
| Teams | Teams |
| その他記入事項 | その他記入事項 |
| アーキレビュー着手日 / 完了予定日 / SLA遵守 / 未達理由 | **空のまま**(人が後から記入) |

> **選択肢列(Hosting環境・CSP・環境利用用途・ノックアウトファクター)の注意**
> これらの欄はドロップダウンになっていて、そのままでは動的コンテンツを入れられません。欄の右端にある「**カスタム値の入力**」(または鉛筆アイコン)をクリックして自由入力モードに切り替えてから、稲妻⚡でトリガー入力を挿入してください。

### 2.6 Step 4: Agentへ結果を返す

1. 「+」→ 検索「**エージェントに応答**」→「**エージェントに応答する**」
2. 「**+ 出力を追加する**」→「**テキスト**」で1つ:

| 出力名 | 説明 | 値 |
|---|---|---|
| `result` | 登録結果とアイテムのリンク | **fx**: `concat('技術相談ナレッジに登録しました。 ', body('ナレッジ登録')?['{Link}'])` |

3. 「**…**」→「**設定**」→「**ネットワーク**」→「**非同期応答**」を**オフ**

### 2.7 Step 5: 保存・テスト・公開

1. 「**下書きを保存する**」
2. 右上「**テスト**」→「手動」→ 入力欄に最低限:
   - Title: `【テスト】(RITM9999999)`
   - Hosting環境: `DCS Hub`
   - CSP: `AWS`
   - 環境利用用途: `NPD`
   - ノックアウトファクター: `なし`
   - 他は適当な文字列でOK
3. 実行 → Listに1件登録されていればOK(**テスト行は必ず削除**)
4. 「**公開**」

### 2.8 Step 6: Agentにツールとして追加

1. 子B「**ホスティング判定Agent**」を開く →「ツール」→「ツールを追加する」→ フロー → `技術相談ナレッジ追記`
2. 説明を設定:

> 技術相談ナレッジListへレビュー結果を1件登録する。**Engの承認を得て親Agentから登録実行を指示された場合のみ実行する**。選択肢項目は必ず完全一致の値を渡すこと(Hosting環境: DCS Hub/DCS Disconnect/DCS Sandbox、CSP: Azure/AWS/GCP、環境利用用途: PRD/STG/NPD、ノックアウトファクター: なし/技術的制約(GCP利用)/技術的制約(NW要件要因)/技術的制約(オートスケール起因)/技術的制約(CSPサービス起因)/アセットの特性による制約/DCS HubのSLAによる制約/ビジネス要件)。

3. 保存

---

## 3. うまくいかないときのチェックリスト

| 症状 | 原因 | 対処 |
|---|---|---|
| 公開時に「スキーマが一致しない」系のエラー | 複数の「エージェントに応答する」で出力名・説明が違う | 両方の**コードビュー**を開いて `description` まで一字一句揃える |
| Agentがツールを呼んでも結果が返ってこない | 非同期応答がオン | 「エージェントに応答する」→設定→ネットワーク→非同期応答をオフ |
| Agentのツール一覧にフローが出てこない | フローが未公開 | フローを開いて「公開」を実行 |
| 「〜は定義されていません」エラー | 式で参照しているアクションの表示名が違う | `body('表示名')` の表示名と実際のアクション名を一致させる |
| ファイルが見つからない(404) | パスを手打ちした | 一度クリアして**フォルダアイコン(ファイルピッカー)から選び直す** |
| Excelへの書き込みが失敗する | コピー直後のファイルロック | 「表に行を追加」の設定で再試行ポリシーを既定にする(自動リトライ) |
| 選択肢列に値が入らない | ドロップダウンのまま動的コンテンツを入れようとした | 「カスタム値の入力」に切り替えてから挿入 |
| 日付列でエラー | 空文字を日付列に入れた | fxで `if(empty(入力), null, 入力)` に変更 |

---

## 4. 完成後の状態(確認用)

| ツール | 持ち主Agent | トリガー入力 | 主なアクション |
|---|---|---|---|
| ⑥ アーキテクチャレビュー票Excel作成 | 子C: アーキテクチャレビュー票作成Agent | RITM番号 / CSP / 行データ | 雛形コピー → 行データ解析 → 行の書き込みループ(表に行を追加)→ 応答 |
| ⑧ 技術相談ナレッジ追記 | 子B: ホスティング判定Agent | 16項目 | ナレッジ登録(項目の作成)→ 応答 |
