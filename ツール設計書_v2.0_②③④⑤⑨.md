# ツール設計書 v2.0 — 参照データ取得ツール ②③④⑤⑨(アクション単位)


## 0. 全ツール共通の設定値

| 項目 | 値 |
|---|---|
| トリガー | 「エージェントがフローを呼び出したとき」。入力は各ツールの項のとおり(無いものは「入力を追加する」を押さない) |
| SharePoint「パスによるファイル コンテンツの取得」 | サイトのアドレス: `JP CMS - https://jpdeloitte.sharepoint.com/sites/Cloud681` / ファイル パス: フォルダーアイコンから対象ファイルを選ぶ(`/Shared Documents/共通_Engineering標準化/00.共通/08.案件外タスク/FY27施策_アーキテクチャレビュー高度化/02_実装/02_PoC/Agent連携用/…md`)/ コンテンツ タイプを推測する: はい |
| 「エージェントに応答する」 | 「出力を追加する」→「テキスト」。名前は半角英字(下表)。値は動的なコンテンツ「ファイル コンテンツ」(§0.1)。「…」→設定の「非同期応答」はオフ |
| 簡易モード(プレビュー) | オフのまま |
| 保存後 | 公開 → 持ち主のAgentの「ツール」に追加 → 説明文は Instructions集 v8.3 の各Agentのツール表の文をそのまま貼る |

### 0.1 「ファイル コンテンツ」がそのまま使えるかの確認(最初の1回だけ)

「エージェントに応答する」の値に動的なコンテンツ「ファイル コンテンツ」を入れてテスト実行し、出力を見る。

| 出力の見え方 | 対応 |
|---|---|
| mdの文章がそのまま読める | そのままでよい(③④⑤で動いているものと同じ) |
| `{"$content-type":"…","$content":"SGVsbG8…"}` のように見える | 値を「式」に切り替えて `base64ToString(body('パスによるファイル_コンテンツの取得')?['$content'])` を入れる(アクション名の空白は `_` に変わる) |

---

## 1. 行数カウント(②③⑤に後から足す共通の4アクション)

「パスによるファイル コンテンツの取得」と「エージェントに応答する」の間に、上から順に追加する。追加したらタイトルを右の名前に変える(式がその名前で参照する)。

| # | アクション | 名前 | 設定値 |
|---|---|---|---|
| 1 | 作成 | `md文字列` | 入力: §0.1で確認した値(「ファイル コンテンツ」または base64ToString の式) |
| 2 | 作成 | `行の配列` | 入力(式): `split(replace(outputs('md文字列'), decodeUriComponent('%0D'), ''), decodeUriComponent('%0A'))` |
| 3 | アレイのフィルター処理 | `表の行` | 差出人: 式 `outputs('行の配列')` / 条件: 左辺 式 `item()`、演算子「次の値で始まる」、右辺 半角の縦棒1文字(詳細モードなら `@startsWith(item(), '\|')`) |
| 4 | 作成 | `全行数` | 入力(式): `length(skip(body('表の行'), 2))`(見出し行と区切り行を引く) |

応答の出力に `count`(テキスト)を追加し、値は式 `string(outputs('全行数'))`。本文の出力(criteria など)は、先頭に全行数を付けたい場合だけ式 `concat('全行数: ', string(outputs('全行数')), decodeUriComponent('%0A'), outputs('md文字列'))` にする(付けなくても `count` があれば検算はできる)。

テスト: `count` が、mdの表の行数(見出し・区切りを除く)と一致すること。ずれるときは、mdに表以外で `|` から始まる行が無いか、表が2つ無いかを確認する。

---

## ② 構成図チェック観点取得

| 項目 | 内容 |
|---|---|
| 持ち主 | 構成図チェックAgent(旧: 構成図情報抽出Agent。外す) |
| 参照ファイル | `構成図チェック観点.md`(業務側のExcelを標準化列ごとmd表に変換して配置。列順はツール一覧v2.3 ②の項) |
| 段階 | **現行(3アクション)**: 入力なし、全文を返す。Agentが自分で絞る(暫定) / **改修版**: 入力3つ、列位置で絞って適用行数を返す |

### 現行(3アクション)の設定値

| アクション | 設定値 |
|---|---|
| トリガー | 入力なし |
| パスによるファイル コンテンツの取得 | ファイル: 構成図チェック観点.md |
| エージェントに応答する | 出力 `checkpoints`(テキスト)= 「ファイル コンテンツ」 |

### 改修版の設定値(§1の4アクションを入れたあとに追加)

| # | アクション | 名前 | 設定値 |
|---|---|---|---|
| 0 | トリガー | — | 入力を3つ追加(テキスト): `環境`(Hub / Disconnected)、`CSP`(AWS / Azure / GCP)、`処理`(1次 / 2次)。式で参照するときは動的なコンテンツから選ぶ(内部名は追加順に `text` / `text_1` / `text_2`) |
| 5 | アレイのフィルター処理 | `適用行` | 差出人: 式 `skip(body('表の行'), 2)` / 条件(詳細モード)は下の式。列の位置: 対象CSP=5列目、環境=6列目、1次・2次チェック=7列目 |
| 6 | 作成 | `適用行数` | 入力(式): `length(body('適用行'))` |
| 7 | 作成 | `見出し2行` | 入力(式): `join(take(body('表の行'), 2), decodeUriComponent('%0A'))` |
| 応答 | エージェントに応答する | — | `checkpoints`(テキスト)= 式 `concat('観点表: 構成図チェック観点.md / 条件: 環境=', triggerBody()?['text'], ' CSP=', triggerBody()?['text_1'], ' 処理=', triggerBody()?['text_2'], ' / 適用行数: ', string(outputs('適用行数')), decodeUriComponent('%0A'), outputs('見出し2行'), decodeUriComponent('%0A'), join(body('適用行'), decodeUriComponent('%0A')))` / `count`(テキスト)= 式 `string(outputs('適用行数'))` |

`適用行` の条件(詳細モード。`text`/`text_1`/`text_2` は環境/CSP/処理の内部名。表の中では縦棒が `\|` と表示されるが、式には半角の縦棒1文字を書く):

```
@and(
  or(contains(trim(split(item(),'|')?[6]), triggerBody()?['text']),   contains(trim(split(item(),'|')?[6]), '共通')),
  or(contains(trim(split(item(),'|')?[5]), triggerBody()?['text_1']), contains(trim(split(item(),'|')?[5]), '共通')),
  or(equals(triggerBody()?['text_2'], '2次'), contains(trim(split(item(),'|')?[7]), '1次'))
)
```

テスト: 環境=Hub, CSP=AWS, 処理=1次 で実行 → `count` が「環境がHubまたは共通、対象CSPがAWSまたは共通、1次の行」の数と一致すること。処理=2次にすると件数が増える(全行が対象)こと。

---

## ③ ホスティング判定基準取得

| 項目 | 内容 |
|---|---|
| 持ち主 | ホスティング判定Agent |
| 参照ファイル | `ノックアウト要件一覧.md`(先頭にID列 KO-01… を追加。列: ID / No. / 要因 / 具体的なケース / 判断区分 / 備考) |
| 現行(3アクション) | トリガー(入力なし)→ ファイル コンテンツの取得(ノックアウト要件一覧.md)→ 応答 `criteria`(テキスト)= 「ファイル コンテンツ」 |
| 改修(検算R5の分母) | §1の4アクションを追加し、応答に `count` を追加。`criteria` は先頭に全行数を付けた式にしてもよい |

---

## ④ Salus Status一覧取得

| 項目 | 内容 |
|---|---|
| 持ち主 | ホスティング判定Agent |
| 参照ファイル | `Salus_Status一覧.md` |
| 現行 | 応答が2つ(salusStatus / serviceVerification)ある旧作り |
| 改修 | 応答の出力を `salusStatus`(テキスト)= 「ファイル コンテンツ」の1つだけにし、ツール名を「Salus Status一覧取得」に変更する(名前を変えるとAgent側で再追加が必要)。行数カウントは任意(分母はサービス一覧件数なので無くてよい) |

---

## ⑤ アーキテクチャレビュー票テンプレート取得

| 項目 | 内容 |
|---|---|
| 持ち主 | アーキテクチャレビュー票作成Agent |
| 参照ファイル | `アーキテクチャレビュー票テンプレート(AWS).md` / `(Azure).md` / `(GCP).md`(列: No / ステータス / 環境 / 対応区分 / カテゴリ / 指摘概要 / 指摘内容 / 対応ガイドライン・参考資料) |
| 入力 | `CSP`(テキスト: AWS / Azure / GCP) |
| 現行(3アクション+分岐) | トリガー(入力 CSP)→ CSPでファイルを選ぶ → ファイル コンテンツの取得 → 応答 `template`(テキスト)= 「ファイル コンテンツ」、`fileName`(テキスト)= 選んだファイル名 |
| ファイルの選び方(分岐を作らない書き方) | 「パスによるファイル コンテンツの取得」のファイル パスを式にする: `if(equals(triggerBody()?['text'], 'AWS'), '<AWSのmdのパス>', if(equals(triggerBody()?['text'], 'Azure'), '<Azureのmdのパス>', '<GCPのmdのパス>'))`(パスは一度ピッカーで選んで表示された文字列をコピーする)。現行が「条件」の3分岐で動いているなら、そのままでよい |
| 改修(検算R2の分母) | §1の4アクションを追加し、応答に `count` を追加。mdの先頭に人が書いていた「全NN行」は不要になる(残っていても害はない) |

---

## ⑨ サービス提供対象リソース一覧取得(新設)

| 項目 | 内容 |
|---|---|
| 持ち主 | アーキテクチャレビュー票作成Agent |
| 参照ファイル | `サービス提供対象リソース一覧.md`(列: カテゴリ / サービス種類 / サービス名 / 利用者による動作検証必要) |
| 作り(3アクション) | トリガー(入力なし)→ パスによるファイル コンテンツの取得(サービス提供対象リソース一覧.md、コンテンツ タイプを推測する=はい)→ エージェントに応答する |
| 応答の設定値 | 出力を1つ追加(テキスト)。名前 `resourceList`、値 = 動的なコンテンツ「ファイル コンテンツ」(§0.1でbase64だった場合だけ式に切り替える) |
| 行数カウント | 任意(分母はサービス一覧件数なので無くてよい)。付ける場合は§1の4アクション+ `count` |
| テスト | 実行して `resourceList` にmdの表が全文入っていること |
| Agentへの登録 | 公開 → アーキテクチャレビュー票作成Agent → ツール → 追加 → 説明文は Instructions集 v8.3 の⑨の文 |

---

## 付録 A. 式の書き方の約束

- アクション名の空白は式の中では `_` になる(例: `body('パスによるファイル_コンテンツの取得')`)。
- 「作成」の出力は `outputs('名前')`、「アレイのフィルター処理」の出力は `body('名前')`。
- トリガーの入力は動的なコンテンツから選ぶのが確実。式に書く場合の内部名は追加した順に `text` / `text_1` / `text_2`(テキスト型の場合)。
- 改行は `decodeUriComponent('%0A')`、復帰は `decodeUriComponent('%0D')`。
- 本書の表の中では縦棒が `\|` と表示されることがあるが、式に書くのは半角の縦棒1文字。
