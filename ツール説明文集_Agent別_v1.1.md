# ツール説明文集(Agent別)


## この文書の使い方

Copilot Studio では、親Agentが子Agentを選ぶときも、子Agentがツールを選ぶときも、**説明文だけを読んで判断**します。処理内容が同じツールでも、**使うAgentが違えば「いつ・何のために呼ぶか」が違う**ため、Agentごとに説明文を出し分けます。

貼り付け先は次の2か所です。

| 貼り付け先 | 場所 |
|---|---|
| ツールの説明 | 該当Agent → ツール → 対象ツールを開く → 「説明」欄 |
| 入力パラメーターの説明 | 同画面の各入力パラメーター → 「説明」欄 |

### Agent名の対応

設計書上の呼び名と、Copilot Studio 上の実際のAgent名の対応です。構成は**子Agent方式**(親の中で子を新規作成)です。

| 設計書上の呼び名 | Copilot Studio 上のAgent名 | リレーションシップ |
|---|---|---|
| 親: オーケストレーターAgent | `DTG_アーキテクチャレビュー親Agent` | 親 |
| 子A: 構成図情報抽出Agent | `構成図情報抽出Agent` | 子 |
| 子B: ホスティング判定Agent | `ホスティング判定Agent` | 子 |
| 子C: アーキテクチャレビュー票作成Agent | `アーキテクチャレビュー票作成Agent` | 子 |
| 子D: 構成図修正Agent | `構成図修正Agent` | 子 |

**注意**: ツール⑥⑦の動作テストは `DTG_ReviewSheetGenerator` という別Agentで行いました。そちらと `アーキテクチャレビュー票作成Agent`(子C)が別物の場合、**ツール①⑤⑥⑦は子C側にも改めて接続する必要があります**。子Cのツール一覧を確認してください。

---

## 親: オーケストレーターAgent

ツールは持ちません(設計原則1: 親は連携専任)。

親に設定するのは**子Agentの説明文**(Connected Agents)であり、ツールの説明文ではありません。子Agentの説明文は「Instructions集_旧UI版_v4.5」を参照してください。

---

## 子A: 構成図情報抽出Agent

### ② 構成図チェック観点取得

#### 説明文

```text
構成図チェック観点.mdの全文を取得して返す。構成図の画像を受け取ったら、チェックを開始する前に必ず呼び出すこと。返される観点は共通観点とCSP別観点に分かれており、対象となる観点を1件も飛ばさずに順に判定すること。md冒頭の内訳件数は検算R1の分母として使用する。記憶や一般知識で観点を代用してはならない。
```

#### 入力パラメーター

| パラメーター | 説明文 |
|---|---|
| (なし) | — |

---

## 子B: ホスティング判定Agent

### ① Teamsスレッド内容取得

#### 説明文

```text
RITM番号を指定して、共通_Engineering標準化チャネルの該当スレッドから親投稿・全返信・添付ファイル一覧を取得し、本文から判定したCSPとあわせて返す。ホスティング判定を始める前に必ず最初に呼び出し、案件情報・ホスティング判断アプリの結果・利用者からの回答を読み取ること。利用者回答を受けた再判定のときも、最新の返信を読むため毎回呼び出すこと。スレッドが見つからない場合はRITM番号の誤りである可能性が高いのでEngへ確認すること。
```

#### 入力パラメーター

| パラメーター | 説明文 |
|---|---|
| RITM番号 | 対象案件のRITM番号。例: RITM1234567 |
| CSP | AWS / Azure / GCP のいずれか。指定した場合はその値を優先し、空の場合はスレッド本文から自動判定する。通常は空のままにする |

### ③ ホスティング判定基準取得

#### 説明文

```text
ホスティング判定基準.md(ノックアウト要件)の全文を取得して返す。Hub / Disconnected の判定を行う前に必ず呼び出すこと。返された要件を1件ずつ順に照合し、判定理由には必ず基準IDを添えること。記憶や一般知識で基準を代用してはならない。
```

#### 入力パラメーター

| パラメーター | 説明文 |
|---|---|
| (なし) | — |

### ④ サービス照合資料取得

#### 説明文

```text
Salus_Status一覧.mdとサービス検証状況.mdの2ファイルの全文を取得し、salusStatus(Salus上の利用可否)とserviceVerification(社内での検証状況)に分けて返す。利用予定サービスの照合を行う前に必ず呼び出すこと。各サービスについて両方を必ず照合し、片方だけで結論を出さないこと。どちらの一覧にも存在しないサービスは「未掲載」として要人手確認に分類すること。
```

#### 入力パラメーター

| パラメーター | 説明文 |
|---|---|
| (なし) | — |

### ⑧ 技術相談ナレッジ追記 ※未実装

#### 説明文

```text
技術相談ナレッジのSharePoint Listへ1件を登録し、登録先リンクを返す。Engの承認を得たうえで、親Agentから明示的に登録の指示を受けたときだけ呼び出すこと。文案を作成しただけの段階や、自己判断では絶対に呼び出さないこと。登録内容は承認された文案のとおりとし、選択肢項目はListで定義された値と一字一句同じ文字列を渡すこと。定義外の値を渡すと登録が失敗する。
```

#### 入力パラメーター

| パラメーター | 説明文 |
|---|---|
| (List列に対応した16項目) | 各項目の説明文はList構築時に確定させる。選択肢列には「Listで定義された値のいずれかを完全一致で指定する。例: ○○ / △△ / □□」と明記する |

---

## 子C: アーキテクチャレビュー票作成Agent

### ① Teamsスレッド内容取得

#### 説明文

```text
RITM番号を指定して、共通_Engineering標準化チャネルの該当スレッドから親投稿・全返信・添付ファイル一覧を取得し、本文から判定したCSPとあわせて返す。アーキテクチャレビュー票の作成を始める前に必ず最初に呼び出し、案件情報と、後続でアーキテクチャレビュー票Excelのファイル名に使うCSPを確定させること。CSPが「不明」で返った場合は推測せず、Engに確認すること。
```

#### 入力パラメーター

| パラメーター | 説明文 |
|---|---|
| RITM番号 | 対象案件のRITM番号。例: RITM1234567 |
| CSP | AWS / Azure / GCP のいずれか。指定した場合はその値を優先し、空の場合はスレッド本文から自動判定する。通常は空のままにする |

### ⑤ アーキテクチャレビュー票取得

#### 説明文

```text
CSPを指定して、そのCSPのアーキテクチャレビュー票マスタ(md)の全文を取得して返す。編集指示を作成する前に必ず呼び出すこと。返されたmdの先頭にある全行数を検算R2の分母とし、全行を①流用 / ②添削 / ④不要のいずれかに必ず仕分けること(③追記は新規行のため分母に含めない)。記憶や過去案件の内容でマスタを代用してはならない。
```

#### 入力パラメーター

| パラメーター | 説明文 |
|---|---|
| CSP | AWS / Azure / GCP のいずれか。ツール①が返したCSPをそのまま渡す |

### ⑥ アーキテクチャレビュー票Excel作成

#### 説明文

```text
RITM番号・CSP・確定した行データ(JSON)を渡すと、アーキテクチャレビュー票の雛形をコピーして利用者送付用のExcelを作成し、書き込み件数とファイルリンクを返す。Engが編集指示を確定させた後にのみ呼び出すこと。行データJSONのキーはExcelの見出しと完全一致させること(No. / ステータス / 環境 / 対応区分 / カテゴリ / 指摘概要 / 指摘内容 / 対応ガイドライン・参考資料。「No.」は末尾のピリオドまで含める)。返された書き込み件数と、渡した行数(①流用+②添削+③追記)を突き合わせる検算R3を必ず行い、不一致の場合はEngへ報告すること。
```

#### 入力パラメーター

| パラメーター | 説明文 |
|---|---|
| RITM番号 | 対象案件のRITM番号。ファイル名に使用される。例: RITM1234567 |
| CSP | AWS / Azure / GCP のいずれか。ファイル名に使用される |
| 行データ | アーキテクチャレビュー票に書き込む行のJSON配列。キーはExcelの見出しと完全一致させる |

### ⑦ 回答済みアーキテクチャレビュー票取得

#### 説明文

```text
RITM番号を指定して、Teamsチャネルのファイルフォルダから該当案件の回答済みアーキテクチャレビュー票Excelを特定し、記入済みの内容をmd表形式で返す。利用者からの回答を受領した後、内容を確認・整理する前に必ず呼び出すこと。返されるmdの先頭に対象ファイル名と全行数があるので、回答済みと未回答の仕分け件数の合計を全行数と突き合わせ、不一致のまま出力しないこと。該当ファイルが見つからない場合は未添付である旨が返るので、そのときは回答が未着であるとEngへ報告すること。
```

#### 入力パラメーター

| パラメーター | 説明文 |
|---|---|
| RITM番号 | 対象案件のRITM番号。例: RITM1234567 |

---

## 子D: 構成図修正Agent

ツールは持ちません。親から渡される3つの材料(子Aの抽出結果 / 子Bの判定結果 / Eng確定版の編集指示)のみで構成図修正指示の文案を作成します。

---

## 補足: 同じツールで説明文を出し分けている箇所

| ツール | 子B向けの主眼 | 子C向けの主眼 |
|---|---|---|
| ① Teamsスレッド内容取得 | 判定材料(判断アプリ結果・利用者回答)を読むため。**再判定のたびに毎回呼ぶ**ことを強調 | アーキテクチャレビュー票Excelの**ファイル名に使うCSPを確定**させるため。CSP不明時はEng確認を強調 |

②③④⑤⑥⑦⑧は単一Agentのみに接続するため、出し分けは不要です。

## 更新時の注意

- ツール名やファイル名を変えた場合、説明文中の名称も合わせて更新すること
- 説明文に検算ルール(R1〜R3)を書いているのは、Instructions側の記載が読み飛ばされた場合の二重防止のため。設計書§1.1を改訂したときはこちらも見直すこと
