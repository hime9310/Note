
論点を全部まとめました。列の説明シートの定義も§1に取り込んでいます(判定トリガーが「リソースの存在」「アーキレビュー票のヒアリング」の2種、備考に「Guardrails・社内ルール」等が混在、など)。

**構成**: §1 列の理解(業務側の定義+設計側の使い方+確認したいこと)/ §2 論点 27件(A〜Jの10テーマ。各行に 背景・選択肢・推奨・決める人・回答欄)/ §3 推奨案で決めた場合の子Bの処理の流れ / §4 決定後に更新する成果物。

**先に決めたい★9件**

| ID | 論点 | 推奨 |
|---|---|---|
| A-1 | 2次チェックはいつ・何を材料に | 回答回収後、修正構成図で再Mermaid化→子Bが2次実行(材料: 新Mermaid+回答回収結果) |
| A-2 | 2次のとき1次も再実行するか | 全件再実行(図が変わるため) |
| B-1 | 判定トリガーの標準化 | 3型(常時 / 構成図に存在 / 回答で確認)+条件 |
| C-2 | 環境未確定のときの環境別行 | 全部判定して「(Hub想定時)」等を注記 |
| D-1 | ネクストアクションの標準化 | 5区分(A問題なし / B Eng目視 / C利用者へ依頼 / D票で確認 / E対象外) |
| E-1 | 画像前提の判定方法(アイコン等) | 子Bに共通の読み替え表(アイコン→ノードラベル、境界線→subgraph、矢印→接続)を置き、特殊な行だけmdを書き直す |
| F-1 | 出力の共通フォーマット | 共通8項目+観点固有の出力内容 |
| G-1 | AIが判定できない行 | 「AI可否」列(○/△/×)を追加(ホスティング判定基準の○△×と同じ考え方) |
| I-1 | mdの形式 | 1観点=1ブロック(Excelから機械変換) |




ーーーー

# 構成図チェック観点 論点一覧(業務確認用 v1.0)

| 項目 | 内容 |
|---|---|
| 目的 | 「構成図チェック観点」表(Excel)を構成図情報抽出Agent(子B)が実行できるmdに落とす前に、業務側と設計側で決めておくべき論点を一覧にする |
| 対象 | 観点表の列: No / 大分類 / チェック観点 / 判断基準 / 対象CSP / 環境 / 1次・2次チェック / 判定トリガー / ネクストアクション / AI判定方法 / AI出力内容 / 備考 |
| 前提(設計方針) | ロジックの置き場は3層: **観点md=観点ごとのロジック(データ)** / **子BのInstructions=列の読み方と汎用手順(絞込→トリガー判定→判定→出力→検算R1)** / **ツール②=mdを返すだけ**。観点を追加・変更するときはmdの行だけを直し、子BのInstructionsとツールは変えない。新しいツールは作らない(本番化時に絞込をツール②へ移す選択肢のみ) |
| 使い方 | §2の各論点の「回答欄」に決定またはコメントを記入する。★は**mdを作る前に決めないと進めない**論点、☆は後回しにできる論点 |
| 決定後に変わるもの | 観点md(形式・列)/ 子BのInstructions(v7.2)/ 親のInstructions(2次チェックの指示語)/ 設計書(§3.1 P5、§4.3 Phase 1b・7)/ ツール②(本番化時のみ) |

---

## 1. 列の理解(確認したいこと付き)

| 列 | 業務側の定義(観点表の説明シートより) | 設計側の理解・使い方 | 確認したいこと |
|---|---|---|---|
| No | 各チェック観点を識別する管理番号。確認項目ごとに付与 | 出力の識別子。検算R1の対象単位 | マイナスのNo(-3〜0)の意味(→H-2)。欠番・追番のルール(→H-3) |
| 大分類 | 観点のカテゴリ(全体構成、通信、インターネットアクセス、VPC/VNet、サブネット、VM、PaaS、DB、DevOps/CI/CD など) | 出力のグルーピング単位 | 適用判定にも使うか(大分類=DBの行はDBが図に無ければ対象外、など)(→H-1) |
| チェック観点 | 構成図で確認するテーマ・項目名 | 出力の見出し | 1行に複数観点が入っている行(例:「外部SaaS連携有無 / 外部SaaS記載有無」)を分割するか(→F-3) |
| 判断基準 | 観点を満たすと判断するために構成図上で何が確認できる必要があるか(サービス・リソース・配置・接続線・通信経路・注記) | 子Bが「何をもって記載ありとするか」を理解する文。判定の正 | 「妥当性」など評価を含む基準の扱い(→G-2)。取り消し線・赤字など編集中の表記の確定(→I-5) |
| 対象CSP | AWS / Azure / GCP / 複数CSP / 共通 | 適用フィルタ(推定CSPと一致 or 共通) | 「複数CSP」の表記ルール(→C-1)。CSPが推定できないときの扱い(→C-3) |
| 環境 | DCS Hub / DCS Disconnected / 共通 | 適用フィルタ | 抽出時点では環境が未確定。環境別の行をどう扱うか(→C-2) |
| 1次/2次チェック | 1次=初回の構成図確認、2次=利用者へのヒアリング後の再確認 | 実行タイミングのフィルタ(親が依頼文でフェーズを指定) | 2次の実行タイミング・入力材料(→A-1〜A-3) |
| 判定トリガー | そのチェックを実施する条件(特定サービス・リソースが図に存在する場合 / アーキレビュー票のヒアリングで該当要件が確認された場合 など) | 行ごとの「対象外」判定 | 型の標準化と表記(→B-1〜B-3) |
| ネクストアクション | 結果に応じた対応(問題なしとする条件 / 目視での再確認 / 利用者への追記・修正依頼 / アーキレビュー票の該当項目の案内) | 子Bは区分を出力し、後続(子C・子E・Eng)が使う | 区分の標準化と実行者(→D-1〜D-3) |
| AI判定方法 | AIが構成図から抽出・確認する情報と手順(サービス名・リソース名・アイコン・CIDR・境界線・通信線・矢印・配置関係・プロトコル・ポート) | 子Bの判定手順。**Mermaid+注釈情報だけを読む**前提に合わせる必要あり | 画像前提の表現(アイコン等)の書き換え方針(→E-1〜E-3) |
| AI出力内容 | 判定結果として出力する項目(記載有無・検出サービス名・配置先・通信元/先・接続方式・CIDR・判定結果・情報不足項目) | 子Bの出力項目 | 共通フォーマット+観点固有項目の切り分け(→F-1〜F-2) |
| 備考 | 補足・注意点(構成図だけでは判定困難な内容、利用者確認が必要な項目、環境別の推奨・必須条件、例外申請、Guardrails・社内ルールとの関係) | 一部は子Bに読ませる(判定困難の注意)、一部は人向け | どこまで子Bに読ませるか(→I-4)。AI可否の列を分けるか(→G-1) |

---

## 2. 論点一覧

記入ルール: 「推奨」は設計側の案。「決める人」は 業務=業務に詳しい方 / 設計=Agent設計側 / 両方。

### A. 実行タイミング(1次/2次)

| ID | 論点 | 背景 | 選択肢 | 推奨(設計側の案) | 決める人 | 回答欄 |
|---|---|---|---|---|---|---|
| ★A-1 | **2次チェックはいつ・何を材料に実行するか** | 2次のトリガーは「アーキレビュー票のヒアリングで要件が確認された場合」。回答はPhase 7(回答回収)で集まり、利用者が構成図を修正して再提出することがある | (a) 回答回収後、修正された構成図で「レビューして」→Mermaid化→**2次チェックを実行**(材料: 新しい確定Mermaid+注釈+回答回収結果) (b) Phase 2(再判断)のたびに実行(材料: 元のMermaid+スレッドの回答) (c) 2次チェックは子B以外(子C=判定、子E=修正指示)の中で行う | **(a)** 子Bが実行。回答回収結果がトリガーの材料、修正後のMermaidが判定の材料。構成図が再提出されない場合は「図が更新されていない」前提で元のMermaidで実行 | 両方 | |
| ★A-2 | 2次チェックのとき、1次の観点も再実行するか | 修正後の構成図では1次の結果も変わり得る。一方で出力量が増える | (a) 全件(1次+2次)を再実行 (b) 2次の行のみ実行し、1次は前回結果を流用 (c) 2次の行+前回「記載なし」だった1次の行 | **(a)** 全件。図が変わっているため。出力は「前回からの変化」を付記して読みやすくする | 業務 | |
| A-3 | 「ヒアリング」の定義 | 「利用者へのヒアリング後」が、Phase 2の追加ヒアリング(スレッド)を指すのか、アーキレビュー票の回答(Phase 7)を指すのかで実行タイミングが変わる | (a) アーキレビュー票の回答(Phase 7) (b) Phase 2の追加ヒアリングも含む (c) 両方 | **(a)** 列の説明文が「アーキレビュー票のヒアリング」となっているため。(b)も含める場合はA-1の(b)を併用 | 業務 | |
| A-4 | 2次チェックを親に依頼する指示語 | Engが「抽出して」と言ったとき、1次か2次かを親が判別する必要がある | (a) 別の指示語「2次チェックして」を設ける (b) 回答回収結果が会話にあれば自動で2次 (c) Engが「抽出して(2次)」のように添える | **(a)** 明示的な指示語。親は「2次」と回答回収結果を依頼文に載せて子Bを呼ぶ | 設計 | |

### B. 判定トリガー

| ID | 論点 | 背景 | 選択肢 | 推奨(設計側の案) | 決める人 | 回答欄 |
|---|---|---|---|---|---|---|
| ★B-1 | **トリガーの型を標準化するか** | 自由記述のままだと子Bの解釈がぶれる | (a) 3型に標準化: ①常時 ②構成図に対象リソースが存在する場合(対象: サービス名・ノードの種類) ③回答・ヒアリングで要件が確認された場合(対象: 回答の項目と値) (b) 自由記述のまま(子Bが読解) | **(a)** 「型 + 条件」の2要素で書く。例: `②存在: DB系サービス(RDS/Aurora/Cloud SQL/Azure SQL)` / `③回答: DR要件=有` | 両方 | |
| B-2 | 「対象リソースが存在する」の判定材料 | 子BはMermaidのノードラベル(サービス名)と注釈情報しか見ない。表記ゆれ(EC2 / Amazon EC2 / 仮想サーバ)がある | (a) 行ごとに「判定用キーワード」列を追加(該当するサービス名・別名を列挙) (b) 子Bの一般知識で同義判定(推測を許容) (c) 子Aの書き起こしルールで正式名に統一し、キーワードは正式名のみ | **(a)+(c)** キーワード列を追加し、子Aが正式名で書き起こす前提にする。C2(推測しない)と両立させる | 両方 | |
| B-3 | トリガー不成立の行の扱い | 全件処理(C3)と検算R1の分母に関わる | (a) 「対象外(トリガー不成立)」として出力に残す(分母に含める) (b) 出力しない(分母から除く) | **(a)** 出力に残す。「なぜ対象外か(存在しない/回答なし)」を1行で書く。R1=出力行数=適用行数で機械的に検算できる | 設計 | |
| B-4 | ③型(回答で要件が確認された場合)の回答の読み方 | 回答回収結果はNo付きの表(指摘概要/回答内容)。どの回答項目を見るかを行ごとに指定する必要がある | (a) 観点mdに「参照する票の項目(指摘概要のキーワード)」を書く (b) 子Bが回答全文から該当箇所を探す | **(a)** 例: `③回答: 票の「DR要件」への回答が「有」`。票側の指摘概要の文言と揃える(票mdと観点mdで用語を統一) | 業務 | |

### C. 適用フィルタ(CSP・環境)

| ID | 論点 | 背景 | 選択肢 | 推奨(設計側の案) | 決める人 | 回答欄 |
|---|---|---|---|---|---|---|
| C-1 | 「複数CSP」の表記 | 列の説明に「複数CSP」がある | (a) `AWS/Azure` のようにスラッシュ区切り (b) 行を複製してCSPごとに1行 (c) 共通に寄せる | **(a)** 区切り文字を1つに固定(スラッシュ)。子Bは「推定CSPを含む or 共通」で絞る | 業務 | |
| ★C-2 | 環境(Hub/Disconnected)が未確定のときの扱い | 抽出(Phase 1b)は判定(Phase 1c)より前に走るため、環境が決まっていない | (a) 環境別の行も全部判定し、「(Hub想定時)」「(Disconnected想定時)」を付ける (b) Hub想定で絞る(DCS Hub優先の判定方針に合わせる) (c) 環境別の行は2次へ回す(判定後に実行) | **(a)** 全部判定して注記。判定後にEngが該当環境の行だけ見ればよい。出力量が問題なら(b) | 両方 | |
| C-3 | CSPが推定できない(不明)ときの扱い | 図にCSP固有のサービス名が無い場合がある | (a) 共通の行だけ実行し、CSP別の行は「CSP未確定のため保留」と出す。EngがCSPを訂正して再実行 (b) 3CSP分すべて実行 (c) 中断してEngに確認 | **(a)** Phase 1cで「判定して。CSPはAWS」と訂正できる流れが既にあるため | 設計 | |

### D. ネクストアクション

| ID | 論点 | 背景 | 選択肢 | 推奨(設計側の案) | 決める人 | 回答欄 |
|---|---|---|---|---|---|---|
| ★D-1 | **ネクストアクションの区分を標準化するか** | 自由記述(「確認できた場合⇒問題無し / できなかった場合⇒目視、目視でも駄目なら利用者へ案内」)を子Bがそのまま扱うと出力がぶれる | (a) 区分を固定: **A 問題なし** / **B Eng目視確認** / **C 利用者へ記載・修正依頼** / **D アーキレビュー票の該当項目で確認(ヒアリング)** / **E 対象外**。行には「判定結果→区分」の対応と補足だけ書く (b) 自由記述のまま | **(a)** 子Bは行ごとに区分を出力。多段(目視→利用者依頼)は「B(目視で確認できなければC)」と書く | 両方 | |
| D-2 | 区分ごとの実行者と後続Agentへの受け渡し | 子Bは判定するだけで、行動は人か他の子が行う | — | A: 何もしない / B: Engが目視(子Bの出力に「目視ポイント」を添える) / C: 子E(構成図修正指示)と子C(ヒアリング文案)の材料にする / D: 子D(票作成)の指摘候補にする。親は子Bの出力をそのまま子C・D・Eに渡す(既存の流れで足りる) | 設計 | |
| D-3 | 「目視で確認できない場合」の判断は誰がいつ行うか | Agentは目視できない。B→Cへの昇格は人の判断 | (a) Engが目視後に「判定して」の訂正・補足として伝える(既存の流れ) (b) 子Bが「記載なし」をすべてCとして扱う | **(a)** 既存の「訂正・補足」で吸収。子Bは B/C の両方を提示しない(区分は1つ) | 業務 | |

### E. AI判定方法(Mermaid前提への書き換え)

| ID | 論点 | 背景 | 選択肢 | 推奨(設計側の案) | 決める人 | 回答欄 |
|---|---|---|---|---|---|---|
| ★E-1 | **画像前提の表現をどう扱うか** | 「アイコンを抽出」「境界線」「矢印」などは画像の言葉。子BはMermaid(ノード/subgraph/接続/ラベル)と注釈情報だけを読む | (a) mdの「AI判定方法」をMermaid用語で書き直す (b) 子BのInstructionsに共通の読み替え表を置く(アイコン→ノードのラベル / 境界線→subgraph / 矢印→接続の向き / 注記→注釈情報 / 配置関係→subgraphの包含) (c) 両方 | **(b)を先に**(mdの書き直しコストを抑える)。判定方法が特殊な行だけ(a)で個別に書く | 設計 | |
| E-2 | Mermaidで表せない情報の扱い | 色・線の太さ・細かい位置関係・図中の小さな注記は、子Aの注釈情報に書かれていなければ子Bは見えない | (a) 子Aの書き起こしルール(凡例・注釈)で拾う項目を観点側から指定する (b) 判定不能として出す (c) 画像を子Bにも読ませる(設計原則8に反する) | **(a)+(b)** 観点mdの備考に「注釈情報に必要な記載」を書き、無ければ「判定不能(注釈情報に記載なし)」で出す | 両方 | |
| E-3 | 判定結果の値 | 現状は「記載あり / 記載なし / 方向不明」 | (a) 3値のまま (b) 「判定不能(注釈情報に記載なし)」「対象外」を加えた5値 | **(b)** 5値。ネクストアクション区分と1対1で対応させる(あり→A、なし→B/C、不能→B、対象外→E) | 設計 | |

### F. AI出力内容

| ID | 論点 | 背景 | 選択肢 | 推奨(設計側の案) | 決める人 | 回答欄 |
|---|---|---|---|---|---|---|
| ★F-1 | **出力の共通フォーマット** | 観点ごとに出力項目が違うと、後続の子C・子Eが読みにくい | (a) 全行共通の固定項目: No / 大分類 / 観点 / 判定(5値) / 根拠(ノードID・ラベル・接続) / 観点固有の出力内容 / ネクストアクション区分 / 補足 (b) 観点ごとに自由 | **(a)** 共通8項目+観点固有の「AI出力内容」を1項目に収める | 設計 | |
| F-2 | 事実抽出系の行(利用サービス一覧抽出、リージョン記載有無 等)の置き場 | これらは「判定」ではなく【構成図チェック結果】の「■サービス一覧」「■NW情報」そのもの | (a) 観点表から外し、子Bの固定出力(サービス一覧・NW情報)として扱う (b) 観点表に残し、判定=「抽出できた/できない」とする | **(b)** 表に残す(業務側が一覧で管理できる)。ただし出力はサービス一覧・NW情報のセクションに書き、観点別チェックでは「→■サービス一覧を参照」とする | 業務 | |
| F-3 | 1行に複数の観点が入っている行の分割 | 例:「外部SaaS連携有無 / 外部SaaS記載有無」「AWS WAF利用有無 / 接続元IP制限」 | (a) 1観点=1行に分割 (b) そのまま(子Bが両方判定) | **(a)** 判定・区分が1つに定まる。R1の件数も明確になる | 業務 | |

### G. 人間が判断する項目

| ID | 論点 | 背景 | 選択肢 | 推奨(設計側の案) | 決める人 | 回答欄 |
|---|---|---|---|---|---|---|
| ★G-1 | **AIが判定できない行をどう表すか** | 「構成図だけでは判定困難」「利用者確認が必要」が備考に混在。子Bが判定してよい行と、材料提示だけの行を分けたい | (a) 「AI可否」列を追加: ○=AIが判定 / △=AIが判定案+要人手確認 / ×=AIは材料提示のみ(判定しない) (b) 備考の文言で子Bが判断 | **(a)** ホスティング判定基準の「AI代替可否フラグ(○△×)」と同じ考え方に揃える | 業務 | |
| G-2 | 「妥当性」「利用可能性」など評価を含む行 | 例: CIDR妥当性、SQL Server利用可能性。子Bは「評価しない(記載の有無だけ)」が原則(C2・個別厳守事項) | (a) 子Bは事実(CIDR原文・OS種別)を抽出し、妥当性はEng(または子Cの判定)が行う → AI可否=△ (b) 子Bに評価も許す (c) 観点表から外す | **(a)** 原則を守る。行の「AI出力内容」を「抽出する事実」に書き換える | 業務 | |

### H. 大分類・No

| ID | 論点 | 背景 | 選択肢 | 推奨(設計側の案) | 決める人 | 回答欄 |
|---|---|---|---|---|---|---|
| H-1 | 大分類を適用判定に使うか | 大分類=DBの行は、図にDBが無ければすべて対象外にしたい場合がある | (a) 出力のグルーピングのみ(対象外は各行のトリガーで表す) (b) 大分類ごとの存在条件を持つ | **(a)** トリガー(B-1の②型)で行ごとに書く方が単純 | 設計 | |
| H-2 | マイナスのNo(-3〜0)の意味 | 例の行: -3 リージョン記載有無 / -2 利用サービス一覧抽出 / -1 グローバルサービスの配置場所 / 0 DR構成有無 | (a) 「最初に実行する事実抽出」の意味 → 実行順として使う (b) 番号の都合(意味なし) | 確認したい。(a)なら「実行順=Noの昇順」を子Bのルールにする | 業務 | |
| H-3 | 観点の増減時の採番 | 欠番・追番が出るとR1の分母(件数)の管理に影響 | (a) 追番のみ(欠番は残す) (b) 振り直し | **(a)** 追番のみ。R1は「行数」で数えるのでNoの連続性には依存しない | 業務 | |

### I. mdの形式・ツール②・検算

| ID | 論点 | 背景 | 選択肢 | 推奨(設計側の案) | 決める人 | 回答欄 |
|---|---|---|---|---|---|---|
| ★I-1 | **mdの形式** | 12列の幅広い表は、セル内の長文・改行があるとLLMが列を取り違えやすい | (a) 表のまま (b) **1観点=1ブロック**(`### No.X 観点名` の下に「判断基準:」「対象CSP:」…を1行ずつ) (c) 両方(表は人向け、ブロックはAI向け) | **(b)** AI向けはブロック形式。Excelからの変換は機械的にできる(変換スクリプトを用意) | 設計 | |
| I-2 | 検算R1の分母 | 「実施すべき観点数」の定義が要る | (a) CSP・環境・フェーズで絞った後の適用行数(トリガー不成立の行も含む) (b) 絞る前の全行数 | **(a)** mdの先頭に内訳(CSP別×環境別×フェーズ別の件数)を書き、子Bはそこから分母を出す | 設計 | |
| I-3 | 絞込をどこで行うか | LLMに絞込をさせると漏れ・重複の余地がある | (a) PoC: 子BのInstructions(mdの全行を読んで絞る) (b) 本番: ツール②にパラメータ(CSP・環境・フェーズ)を追加し、適用行と件数だけを返す | **(a)→(b)** PoCは(a)。R1不一致が続く場合や観点が100行を超える場合に(b)へ | 設計 | |
| I-4 | 備考列をどこまで子Bに読ませるか | 備考には人向け(例外申請・社内ルール)とAI向け(判定困難の注意)が混在 | (a) 全部読ませる (b) AI向けの注意だけ別列(「AI注意事項」)に分け、備考は読ませない (c) G-1のAI可否列で代替 | **(b)** 分ける。備考は人向けに残す | 業務 | |
| I-5 | 編集中の表記(取り消し線・赤字)の確定 | mdにすると取り消し線や色は消える | (a) md化の前に本文を確定(取り消し線の文は削除、赤字は本文に反映) (b) 取り消し線を「旧:」として残す | **(a)** 確定してから変換 | 業務 | |
| I-6 | 観点mdの版管理 | 観点を変えたときに、どの版で判定したかを残したい | (a) mdの先頭に版数・更新日、子Bの出力に版数を書く (b) 不要 | **(a)** ヘッダーに「版数 / 更新日 / 全NN行 / 内訳」。出力の1行目に版数を出す | 設計 | |

### J. 他Agentとの連携

| ID | 論点 | 背景 | 選択肢 | 推奨(設計側の案) | 決める人 | 回答欄 |
|---|---|---|---|---|---|---|
| J-1 | 子Bの出力を子C(ヒアリング文案)・子D(票)・子E(修正指示)がどう使うか | ネクストアクション区分C・Dの行が、後続の材料になる | — | 子C: 区分Dの行→追加ヒアリング文案の候補 / 子D: 区分C・Dの行→票の③追記候補 / 子E: 2次で「記載なし」の行→修正指示。親は【構成図チェック結果】全文をそのまま渡す(現行どおり) | 設計 | |
| J-2 | 2次チェックの結果と子E(構成図修正指示)の重複 | 子Eも「決まったことのうち図に無いもの」を出す。2次チェックと役割が重なる | (a) 2次チェック=事実(記載なし)の検出、子E=それを修正指示の文面にする、と役割を分ける (b) 2次チェックを子Eに統合 | **(a)** 分ける(1Agent=1責務)。子Eの材料1に2次の【構成図チェック結果】を使う | 設計 | |
| J-3 | 票(アーキテクチャレビュー票md)と観点表の用語統一 | B-4・D-1の区分Dは票の指摘概要を参照する | — | 票mdの「指摘概要」と観点mdの「参照する票の項目」の文言を一致させる(どちらかを正にする) | 業務 | |

---

## 3. 推奨案で決めた場合の子Bの処理の流れ(イメージ)

| 順 | 処理 | 使う列 |
|---|---|---|
| 1 | 親の依頼文から受け取る: RITM / 確定Mermaid / 注釈情報 / フェーズ(1次 or 2次) / 2次のときは回答回収結果 | — |
| 2 | Mermaidから要素を列挙(ノード・subgraph・接続・ラベル・注釈)。CSPを推定 | — |
| 3 | ツール②で観点mdを取得。ヘッダーの版数・内訳を読む | ヘッダー |
| 4 | 絞込: 対象CSP(推定CSP or 共通)× 環境(全環境。未確定は注記)× フェーズ(1次 / 1次+2次) → 適用行数=R1の分母 | 対象CSP / 環境 / 1次・2次 |
| 5 | 行ごとにトリガー判定: ①常時→実施 / ②存在→キーワードがMermaidに有れば実施、無ければ「対象外」 / ③回答→回答回収結果の該当項目が条件を満たせば実施、無ければ「対象外」 | 判定トリガー / 判定用キーワード |
| 6 | AI可否が×の行は材料提示のみ。○△の行は「AI判定方法」(Mermaid読み替え表を適用)に従って判定: 記載あり / 記載なし / 方向不明 / 判定不能 / 対象外 | AI可否 / AI判定方法 / 判断基準 |
| 7 | 行ごとに出力: No / 大分類 / 観点 / 判定 / 根拠(ノードID・ラベル・接続) / AI出力内容 / ネクストアクション区分(A〜E) / 補足(△は「要人手確認」) | AI出力内容 / ネクストアクション |
| 8 | 事実抽出系の行は「■サービス一覧」「■NW情報」に書き、観点別チェックでは参照のみ | — |
| 9 | 検算R1: 出力行数 = 適用行数。一致しなければ出力せずやり直し | ヘッダーの内訳 |
| 10 | 【構成図チェック結果】(版数・RITM・CSP・想定環境・フェーズ付き)を応答し、同じ全文を出力 result に設定 | — |

---

## 4. 決定後に更新する成果物

| 成果物 | 変更内容 |
|---|---|
| 構成図チェック観点.md(P5) | 列の追加(判定用キーワード / AI可否 / AI注意事項)、トリガーとネクストアクションの標準表記、1観点=1ブロック形式、ヘッダー(版数・全NN行・内訳)。Excel→md変換スクリプト |
| 子B Instructions(Instructions集 v7.2 §2) | 列の読み方(トリガー3型・区分A〜E・AI可否・Mermaid読み替え表)、フェーズ(1次/2次)の受け取り、出力フォーマットの共通8項目、判定5値 |
| 親 Instructions(v7.2 §A-2) | 2次チェックの指示語と依頼文(フェーズ=2次+回答回収結果を渡す)。「依頼の判定」に1行追加 |
| 設計書(v7.2) | §3.1 P5の内容、§4.3 Phase 1b(1次)・Phase 7以降(2次)の追加、§1.1 R1の分母の定義、§2.5 指示語一覧 |
| ツール② | PoCでは変更なし。本番化時に絞込パラメータ(I-3) |
| アーキテクチャレビュー票md | 観点md側の「参照する票の項目」と用語を一致(J-3) |