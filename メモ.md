いい疑問です。結論は「**はい、同じものを全Agentに書くのが現実解**」ですが、違和感の正体と、それでも問題ない理由を整理します。

**なぜ「同じものを5回書く」しかないのか**

Copilot Studioの制約が理由です。①Instructionsに外部ファイルを参照する仕組みがない、②Connected Agentsでは**子は親のInstructionsを継承しない**(各Agentは完全に独立して動く)。つまり「1箇所に書いて全員に効かせる」手段がプラットフォーム上存在しないので、全員に守らせたいルールは全員のInstructionsに物理的に存在させるしかありません。ID4の「共通化」は**実行環境上の共通化ではなく、文面の単一ソース管理**(設計書§1が正、各Agentはそのコピー)という意味です。

**「自分に関係ない行が混ざる」問題は起きないか**

これが違和感のもう半分だと思いますが、共通ブロックは意図的に**条件付きの書き方**にしてあります。例えばC3は「**該当する**検算ルール(R1〜R3)を実施し」— 子B・子Dには該当する検算がないので、この行は実行時に何も要求しません(無害に不発)。C4の「画像の解析は構成図情報抽出Agentのみ」も、子A自身にとっては許可、他Agentにとっては禁止として両立します。つまり同一文面でも各Agentで正しく機能するよう書いてあります。分量も6行なのでトークンコストは無視できます。

**残るリスクと対策**

唯一の実リスクは「誰かが1体だけ書き換えて版ズレする」ことです。対策は運用ルール1つ: **共通ブロックは各Agent画面で直接編集しない。直すときは必ず設計書§1→5体へ一括貼り直し**。これをブロックの1行目に書いておくとより安全です:

```
# 共通厳守事項(全Agent共通・設計書§1が正。このブロックは個別に編集せず、設計書更新時に全Agentへ一括貼り直すこと)
```

代替案(共通ルールmdをツールで毎回取得)も一度検討しましたが、全Agentへのツール追加+毎回の取得コスト+Instructions直書きより遵守が弱くなる、で見送った経緯です。Phase 2でCopilot StudioのSkills(構造化指示の再利用機能)がGAし、常時適用を強制できるようになれば、そこが本当の共通部品化の受け皿になります — 設計書の将来検討に1行足しておく価値はあります。

上の1行目の文言修正、設計書に反映しておきますか?それともお手元で直しますか?