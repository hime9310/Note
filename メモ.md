4点、実装レベルで整理します。

## 1. Knowledgeソース：何を・どこへ

**格納場所は専用SharePointライブラリ1つに集約**するのが本番向きです（例：JPサイト配下に「AgentKnowledge」ライブラリ）。PoC初期はCopilot Studioへの直接アップロードでも動きますが、SharePoint参照にしておくと**担当者がファイルを差し替えるだけでAgentの知識が更新される**運用になります。

フォルダ構成と中身の例：

- **01_判定基準**：T2024判定フロー_決定表.docx（PDFフロー図の書き起こし）、構成図チェック観点.docx（整理でき次第）
- **02_参考規定**：Azure独立性要件.docx、AWS_PPA_EligibleUse解説.docx
- **03_過去案件**：技術相談ナレッジ抜粋.xlsx（代表30〜50件、列順維持）
- **04_様式**：アーキテクチャレビュー票.docx（質問生成の種）、用語集.docx（DCS/Hub/PPA等の表記揺れ吸収）

注意：SharePoint参照はエージェント認証が「Microsoftで認証する」であること＋利用者全員にサイトの閲覧権限が必要です。

## 2. 判定ロジックの実装

**三層で組みます。**

- **インストラクション（指示文）**：役割、処理手順、出力フォーマットを記述。チェック1（PPA Eligible Use）は決定表が小さいので**表ごと指示文に直接書きます**。「判定は必ず軸A（実施可否）と軸B（環境選択）に分け、根拠として決定表の行番号・設問番号を引用せよ」と縛るのが肝
- **Knowledge**：チェック2〜4（T2024決定表）は指示文に入りきらないのでKnowledge側に置き、指示文で「ホスティング判定はナレッジ『T2024決定表』の設問に沿って順に評価せよ」と参照を強制
- **出力フォーマット**：①ホスティング判定（軸A／軸B＋根拠）②一次レビュー所見 ③追加ヒアリング質問 ④要確認事項（未整備領域はここへ逃がす）

LLMに自由判定させず「決定表の適用と根拠引用」をさせる構図です。基準が固まったらv0.2でトピックの条件分岐ノードに移して完全決定化できます。

## 3. Power Automate（エージェントフロー）の使い道と作り方

役割は**データの取り込み口**だけです。PoCで作るのは1本：

**フローA「スレッド取得」**
1. Copilot Studio →「ツール」→「新規」→「エージェントフロー」
2. トリガー「エージェントがフローを呼び出すとき」＋入力変数 `RITM番号`（テキスト）
3. Teamsコネクタ「メッセージ一覧取得」（チーム＝Mgmt、チャネル＝事前相談）
4. RITM番号を含むスレッドを絞り込み →「メッセージへの返信一覧取得」
5. HTMLタグ除去・時系列連結で1本のテキストに整形
6. 「エージェントに応答する」で返す
7. エージェント側でツール登録し、説明欄に「RITM番号を受け取り事前相談スレッド全文を返す」と書く（この説明を見てオーケストレーターが呼び出し判断します）

v0.2で添付構成図の取得（チャネル添付は裏のSharePointに保存されるのでそこから取得）、将来はServiceNowコネクタ（プレミアムだがエージェントフロー内ならCopilot Studio課金でカバー）を追加、という積み上げです。

## 4. 会話の場所（GUI）

既製のチャットUIがあり、自作は不要です。

- **開発中**：Copilot Studio画面右の**テストパネル**。作りながらその場で会話して調整
- **チーム展開**：「Teams + Microsoft 365 Copilot」チャネルで公開 → **Teamsの1対1チャット**がそのままGUIになります（担当者がTeamsでAgentに「RITM1234567レビューして」と話しかける形）。冒頭で確認した認証設定・管理者承認がここで必要
- **承認待ちの間のデモ**：デモ用Webサイトリンクを共有すればブラウザで会話可能

将来形は資料§4.1の構想どおり「案件スレッド内で@メンション応答」ですが、これはフェーズ2です。

ここまでの設計（Knowledge構成・指示文ドラフト・フロー手順・画面）を1本の実装設計書ファイルにまとめることもできます。必要なら次のターンで作ります。