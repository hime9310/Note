結果の読み解きです。想定どおりの部分と想定外の部分が混ざっています。

| # | 画面に出たもの | 判定 | 説明 |
|---|---|---|---|
| 1 | Mermaidコード+注釈情報(3項目) | **想定どおり** | 親経由で画像が子Aに届き、書き起こしができた(検証①クリア)。これは子Aの応答がチャットにそのまま表示されたもの |
| 2 | 「RITM1234567 レビュー結果」(構成の流れ・OIDCは妥当・確認ポイント…) | **想定外・あるべきでない** | 子Aが依頼外の「レビュー」まで作った。子は親の会話文脈を受け取るため、Engの「レビューして」に引きずられた(子AのInstructionsには「レビューしない」の禁止が無い) |
| 3 | 親の「構成図をMermaidへ書き起こしました。」の中身が英語1文(Reviewed the provided…) | **想定外(原因は明確)** | 子の**出力定義を消したため、親には子の「要約1文」しか返らない**(MS仕様: 出力を定義しない子は自然言語の要約だけを返す)。親はMermaid本文を持っていないので、後続で子Bに渡せない |
| 4 | 親が子B・子Cを続けて呼ばずに停止 | **想定どおり** | 1指示=1子は守られた(検証③クリア) |

つまり②は「子Aの逸脱」、③は「私の設計ミス(出力まで消した)」です。入力は消したままでよく(聞き返しの原因は入力)、**出力だけ定義し直す**のが正解でした。

**直し方(3点)**

| # | 場所 | 変更 |
|---|---|---|
| 1 | 子A「出力」 | mermaid / annotations / message の3つを再作成(説明は前回の表と同じ、String)。**入力は作らない** |
| 2 | 子B〜E「出力」 | `result`(String、説明:「応答した成果物の全文」)を1つずつ追加。後続フェーズで親が判定結果などを子へ渡すために必要 |
| 3 | 親Instructions | 手順2・3を下記に差し替え(子Aに「レビュー不要」を明示し、親はMermaidを再掲せず案内だけにする) |

```text
2. 子Agent「構成図Mermaid化Agent」を呼び出し、次の依頼文だけを渡す:「添付された構成図の画像またはPDFをMermaidへ書き起こしてください。レビュー・評価・確認ポイントの作成は不要です。出力はMermaidコードと注釈情報だけにしてください。」(画像は会話の添付として子Aが読む)
3. 子Aの出力 mermaid / annotations を下書きとして原文のまま保持する。子Aの応答はチャットに直接表示されるので再掲しない。次の案内だけをEngへ送って停止する:「上のMermaidコードをMermaid Live(https://mermaid.live)またはVS Codeのプレビューに貼り付けて描画し、原図と照合してください。修正がある場合は修正内容をそのまま入力、問題ない場合は「完了」と入力してください。」mermaid が空(子Aが質問を返した)場合は、message を原文のまま提示して停止する。
```

あわせて子Aの説明(Description)の末尾に「レビュー・評価・確認ポイントは作成しない。」を足してください。これでも②のレビューが出る場合は、子AのInstructionsに禁止の1行を足す相談になります。

