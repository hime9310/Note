
# 自動更新 検証結果まとめ

## 結論

V のアプリケーションオブジェクトで「SNI Certificate」にチェックを入れて Push すれば、ALB の SNI リスナー証明書にも新証明書が追加され、対象 URL へのアクセス時には新証明書が適用されることを検証環境で確認した。

「Default SNI Certificate」はリスナーのデフォルト証明書を置き換える設定であるため、本番のホストヘッダー用ドメイン（8 件）には**チェックを入れない**。チェックを入れるのは、リスナーのデフォルト証明書を担うドメインのみとする。

## 検証で確認できたこと

0.SNI含めて更新する場合、ポリシーの追加が必要になる

1. **SNI Certificate チェックあり**で Push すると、AWS コンソール上の「SNI のリスナー証明書」に新証明書が追加される（新規 ARN として ACM にインポートされ、リスナーの SNI リストに登録される）。

2. **Default SNI Certificate チェックあり**で Push すると、SNI リストへの追加に加えて、リスナーの**デフォルト証明書も新証明書に置き換わる**ことを確認した。

3. Renew（更新 Push）後、旧証明書は SNI リストに残るが、実際にクライアントへ配信されるのは**新証明書**であることを実測で確認した。

## 正常性確認（Push 後に URL アクセス時に参照される証明書）

- **検証環境（）**: VPC 内 CloudShell から SNI 付きで接続し、配信された証明書のシリアル・有効期限が Renew で発行された新証明書と**完全一致**することを確認。旧証明書が SNI リストに残った状態でも、新証明書が選択・配信された。

- **本番環境の想定**: Push 後に各ホストヘッダー用 FQDN へアクセスした際も、**新証明書のシリアル・有効期限が返る想定**。Push 後、以下で 8 ドメイン分を確認する。

```
echo | openssl s_client -connect <本番ALBのDNS名>:443 \
  -servername <対象FQDN> 2>/dev/null \
  | openssl x509 -noout -serial -enddate
```

（ACM 側の新証明書シリアルと一致していれば適用済みと判定）


**Default SNI Certificate を 8 ドメインに入れない理由:**

- Default SNI Certificate は「Push のたびにリスナーのデフォルト証明書を自ドメインの証明書で上書きする」設定である。
- 8 ドメイン全部に入れると、最後に Push されたドメインの証明書がデフォルト証明書となり、更新順序によってデフォルト証明書が毎回変わる不安定な状態になる。
- デフォルト証明書は「SNI を送らないクライアント」や「どのホスト名にも一致しないアクセス」への応答に使われるため、別ドメインの証明書が座ると監視・ヘルスチェック等 SNI を送らない経路で証明書エラーとなり、正常性確認ができなくなる。
- なお、リスナールール（ホストヘッダーによるターゲットグループ振り分け）は証明書設定とは独立しており、どちらの設定でもルーティング自体には影響しない。

## 残る運用上の確認事項

1. **旧証明書の残留**: Renew 後も旧証明書は SNI リストに残る。V の GenerationalCleanup（2 世代前を削除）が次回 Renew 時に旧証明書を自動削除するかを検証環境で経過確認する。削除されない場合は、更新後に旧証明書をリスナーから外す手順を運用に追加する（SNI 証明書はALB あたり上限 25 枚のため放置不可）。

2. **本番初回 Push 後の確認**: 上記 openssl コマンドで 8 ドメイン分のシリアル一致を確認する。



