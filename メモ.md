固定値は飛ばして、最初から動的版でいきましょう。詳細手順です。

**A. 「標準指摘一覧取得」の改修(フィルタークエリを動的に)**

1. 「標準指摘一覧取得」カードをクリック
2. 「**詳細パラメーターを表示**」(すべて表示)を開き、「**フィルター クエリ**」欄を出す
3. 欄をクリック → **fx**アイコン → 次の式をそのままコピペ → 「追加」:

```
concat('CSP eq ''', outputs('CSP確定'), '''')
```

   - クォートの数に注意: `eq ''',` の部分は**シングルクォート3つ**、末尾は `''''`(**4つ**)です。実行時に `CSP eq 'AWS'` のように展開されます
   - ※`CSP` の部分はList列の内部名。列を半角英字「CSP」で作成していればこのままでOK。実行して0件/エラーになったら内部名ズレなので、そのとき直します
4. 「…」→ 設定 → 「**改ページ**」オン・しきい値 `1000`(未設定なら)

**B. 条件「判定:CSP不明」の設定**

1. 「標準指摘一覧取得」の下の「+」→「アクションの追加」→ コントロール「**条件**」
2. 表示名を「**判定:CSP不明**」に変更
3. 条件行の設定:
   - 左の「値の選択」: クリック → **fx** → `outputs('CSP確定')` → 追加
   - 演算子: 「**次の値に等しい**」
   - 右の「値の選択」: そのまま文字で `不明` と入力(fx不要)

**C. はい側(CSP不明のとき)**

1. はい側の「+」→ 検索「文字列変数」→「**文字列変数に追加**」
2. 名前: `reviewSheetStandardItems` / 値(そのままコピペ):

```
【アーキテクチャレビュー票一覧未適用】スレッドからCSPを特定できませんでした。EngにCSP(AWS/Azure/GCP)を確認し、CSPを指定して再度このツールを実行してください。
```

**D. いいえ側(確定CSPの一覧を整形)**

1. 「**文字列変数に追加**」(reviewSheetStandardItems)→ 表示名「**追記:一覧ヘッダー**」/ 値(fx):

```
concat('【アーキテクチャレビュー票 標準指摘一覧】CSP: ', outputs('CSP確定'), ' / 全', string(length(body('標準指摘一覧取得')?['value'])), '行')
```

2. コントロール「**それぞれに適用する**」→ 表示名「**標準指摘ループ**」/ 対象(fx): `body('標準指摘一覧取得')?['value']`
3. ループ内に「**文字列変数に追加**」(reviewSheetStandardItems)→ 表示名「**追記:指摘行**」/ 値は**次の形の式**になります(〈〉が列の内部名):

```
concat(decodeUriComponent('%0A'), '---', decodeUriComponent('%0A'), '[No.', string(item()?['〈No列〉']), '] 環境: ', string(item()?['〈環境列〉']), ' / 対応区分: ', string(item()?['〈対応区分列〉']), ' / カテゴリ: ', string(item()?['〈カテゴリ列〉']), decodeUriComponent('%0A'), '指摘概要: ', string(item()?['〈指摘概要列〉']), decodeUriComponent('%0A'), '指摘内容: ', string(item()?['〈指摘内容列〉']), decodeUriComponent('%0A'), '対応ガイドライン・参考資料: ', string(item()?['〈参考資料列〉']))
```

4. ループの**外・下**に「**文字列変数に追加**」→ 表示名「**追記:一覧末尾**」/ 値(fx):

```
concat(decodeUriComponent('%0A'), '以上、全', string(length(body('標準指摘一覧取得')?['value'])), '行。')
```

D-3の〈内部名〉だけは実物確認が必要です。A〜Cまで組んだら一度保存→テスト実行し、実行履歴の「標準指摘一覧取得」の**出力のvalue先頭1件のJSON**を見せてください(値はマスク可)。キー名を埋めた**完成版の式**を作って返します。選択肢列は `{"Value":"AWS"}` 形式で返ることがあり、その場合は `item()?['CSP']?['Value']` の形に直す必要があるので、そこもJSONを見て確定させます。