

## Venafi証明書自動更新で発覚した問題のご報告と、今後の運用方針のご相談

お時間をいただきありがとうございます。本日は、HRの**SSL証明書の自動更新（Venafi連携）**についてご報告とご相談をさせてください。

先週8/27展開作業を行った際、**証明書の一部が切り替わらない事象**が発生しました。その後の調査で原因の特定と解決方法の確認までは完了しているのですが、あわせて**今後の運用に関わる問題**も見つかったため、状況を共有した上で、対応の方向性についてみなさんのご意見を伺いたい、というのが本日の趣旨です。

説明は10分ほどで、「そもそもHR Webの証明書構成がどうなっているか」という前提からお話しします。

### はじめに（前提）: HR Webは証明書の構成が「特殊」です

通常のシステムは、入口（ロードバランサー＝ALB）1台に証明書1枚という構成です。この構成であれば、Venafiの標準的な自動更新設定（デフォルト証明書の置き換え）で問題なく動作し、全社のVenafi展開もこの標準構成を前提に検証されてきました。

一方HR Webは、**1台の入口に9つのサイトが同居**しており、証明書も9枚あります。内訳は、代表の「デフォルト証明書」1枚と、サイトごとの「SNI証明書」8枚です（SNI＝アクセスされたサイト名に応じて対応する証明書を自動で出し分けるしくみ）。**このSNI証明書の自動更新は、全社標準の検証範囲に含まれておらず、公式には未検証の領域でした。**

本日のご報告は、この未検証部分が本番作業で顕在化した話になります。

### 結論

8/27に実施したHR 向けの証明書自動更新（Venafi連携）の本番展開で、**9枚中1枚（デフォルト証明書）しか切り替わらず、残り8枚（SNI証明書）は古い証明書が参照され続ける**事象が発生しました。原因は上記の特殊構成に起因するもので既に特定済み、Venafi側の設定1つ（SNI Certificateチェック）で解消できることも検証環境で確認済みです。

ただし、その設定を入れて解決とするには**もう1つ問題**があります。SNI証明書は古い世代が自動では削除されず、更新のたびに溜まり続け、AWSの上限（25枚）に達すると**それ以降の更新がエラー通知もなく静かに効かなくなる**ことが検証でわかりました。この恒久対応の方向性について、本日みなさんの知見をお借りしたいと思っています。

### 発覚の経緯（8/27の本番作業）

作業担当の方からの報告が発端です。手順どおり、①自動更新設定の導入 → ②Venafi上でリソースと証明書の紐づけ → ③VenafiからPush（配信）→ ④正常性確認、と進めたところ、③までは正常に見えたものの、④で想定外の状態が確認されました。

- AWSの証明書保管庫（ACM）には、各サイト分の新証明書が**正しく届いていた**（各FQDNに新旧2枚が存在する状態）
- しかし入口（ALB）側は、デフォルト証明書の1枚だけが新しくなり、**残り8サイトは古い証明書に紐づいたまま**
- 利用者への影響: サイトへのアクセスや暗号化通信自体は問題なし。ただし古い証明書が使われ続けており、**このままではその証明書の期限が切れた時点でブラウザ警告が出る**状態
- 現在はVenafiの自動更新を一時停止し、原因調査に切り替え

### 調査でわかったこと

**原因**: Venafiには「証明書をALBのどこに紐づけるか」を決める設定があり、今回の設定は「デフォルト証明書の置き換え」のみが有効でした。SNI証明書8枚分をALBに紐づけるには「**SNI Certificate**」というチェックを追加する必要がありますが、標準の展開手順にはこの観点がありませんでした（冒頭のとおり未検証領域だったため）。

**検証結果（解決すること）**: 検証環境でこのチェックを入れてPushしたところ、新証明書がALBのSNI証明書として登録され、サイトアクセス時に新証明書が使われることを確認できました。事象そのものはこの設定で解消します。

**検証結果（新たにわかった問題＝「世代」の問題）**: ただし、SNI証明書への反映は「置き換え」ではなく「**追加**」です。更新するたびに新しい世代の証明書が1枚増え、**古い世代は自動では削除されません**。検証環境で更新を繰り返しても、一度も自動削除されないことを確認しました。AWSの仕様でSNI証明書は**25枚が上限**のため、HR Webは8サイト分が毎回増えると、**更新3回目で24枚に達し、4回目から新証明書の登録に失敗**します。しかもエラーで止まるのではなく、見かけ上は正常のまま古い証明書が配られ続けるため、気づかないうちに期限切れ事故へ向かう、たちの悪い壊れ方をします。

### 対応案（2案）

**案A: 現構成のまま、古い世代を自動で掃除するしくみを追加する**

SNI Certificateチェックを入れて自動更新を再開し、あわせて更新のたびに古い世代を自動削除するプログラム（AWS Lambda）を追加します。各サイトの構成は一切変わらないため影響はゼロですが、掃除のしくみを新規に作り、保守し続ける必要があります。

**案B: 証明書を1枚に集約する（構成のシンプル化）**

9枚の証明書をやめ、全サイトをカバーする証明書1枚（ワイルドカード証明書）をデフォルト証明書として持つ構成に変更します。「追加」の動きをするSNI部分を使わなくなるため、世代が溜まる問題そのものが消滅し、以後は全社標準と同じ「デフォルト証明書の置き換えのみ」というシンプルな構成になります。一方、1枚の証明書を全サイトで共有する形になるため、各サイトご担当への事前確認が必要です（後述）。

事務局としては、問題の根を断ち構成を標準に寄せられる**案Bを本命**と考えていますが、移行時期などの事情によっては案Aで当面をしのぐ組み合わせもあり得ると考えています。

### みなさんに伺いたいこと

1. 対応の方向性（案A/案B、または他の観点）についてのご意見
2. **【各サイトご担当に特に伺いたい】** サイトに接続するシステム・アプリ・バッチ連携の中に、「特定の証明書であること」を前提にした実装（証明書ピンニング）がないか。ある場合、案Bの証明書差し替えでそのシステムだけ接続不能になるため、事前の把握が必須です
3. 証明書1枚を全サイトで共有すること（案B）への、セキュリティポリシー上・運用上の懸念の有無
4. 案Bでは全サイトの証明書更新日が同一になります。リリース凍結期間など更新タイミングに制約があるサイトの有無

なお、当面の暫定対応として、現在参照されている旧証明書の有効期限までは時間的猶予があります。期限と残日数は別途一覧でお示しします。

---



