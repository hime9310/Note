# 全体ワークフロー(サブAgent統合版)


```mermaid
flowchart TD
    A["Mgmt: 事前相談受付・一次判断→<br>スレッド投稿(アプリ結果含む)→<br>Engへ引継ぎ(DCS案件のみ)"] --> SUB1

    subgraph SUB_IN["構成図チェックAgent【抽出モード】(構成図がある場合)"]
        SUB1["Eng: 構成図画像をアップ<br>「RITMxxxxxxxの構成図をチェックして」"]
        SUB1 --> SUB2["CSP推定→Engに確認(ワンカット)<br>→ツールS2で観点取得<br>→適用観点(共通+該当CSP)を全件判定"]
        SUB2 --> SUB3["【構成図チェック結果】<br>サービス一覧・NW情報・不足情報リスト<br>→Eng確認後「投稿して」<br>→ツールS1でスレッドへ返信投稿"]
    end

    SUB3 --> B["Eng: 親Agentへ依頼<br>「RITMxxxxxxxをレビューして」"]
    A -.->|"構成図なし/後日提出"| B

    B --> T1["ツール① Teamsスレッド内容取得<br>親投稿・返信・添付一覧+対象CSPのレビュー票全文<br>※構成図チェック結果の投稿もここで自動取得"]
    T1 --> T2["ツール② ホスティング判断<br>ノックアウト+全体基準<br>Knowledge根拠照合<br>※サービス一覧をSalus statusと照合"]

    T2 --> D{判断結果}

    D -->|"DCS対象外"| F["Mgmtへ差し戻し文案出力<br>Mgmtが利用者へ連絡・案内"]

    D -->|"情報不足あり<br>(×項目・△未確認)"| G["Agentが追加ヒアリング文作成<br>→Mgmt経由で利用者へ確認"]
    G --> I["利用者回答<br>(Mgmtがスレッドへ反映)"]
    I -->|"回答状況により複数回発生しうる"| B

    D -->|"必要情報すべて充足<br>(レビュー票=回答受領済み)"| R["レビュー票作成・Mgmt経由利用者へ連携<br>レビュー票編集指示(①流用・②添削・③追記・④不要)<br>Sandbox / Hub / Disc.+構成図等のやり取り"]

    R --> Q{"質疑・修正依頼<br>解消?"}
    Q -->|"あり(解消まで繰返し)"| R
    Q -->|"構成・前提の変更あり<br>(再判断が必要)"| B

    Q -->|"なし(合意)"| SUB4

    subgraph SUB_OUT["構成図チェックAgent【修正案モード】"]
        SUB4["Eng: 合意事項(仮判定確定サマリ+<br>②添削③追記の確定内容)をサブへ渡す<br>「合意事項に基づいて修正案を作って」"]
        SUB4 --> SUB5["【構成図修正指示(案)】<br>不足サービスの追記・矢印方向・<br>ポート/CIDR記入指示+利用者向け依頼文<br>→Eng確認後、S1でスレッド投稿"]
    end

    SUB5 --> U["Mgmt経由で利用者へ連携<br>利用者が構成図を修正・最終版提出"]
    U --> V["Eng最終確認(人)<br>仮判定・レビュー票・最終構成図の妥当性確認"]
    V --> C1["05 クローズ<br>スレッド(チャット)クローズ"]
    C1 --> C2["ツール③ 技術相談ナレッジへ追記<br>Engの明示指示でツール③実行"]
```

## サブAgent統合のポイント

| # | 内容 |
|---|---|
| 1 | **入口(抽出モード)**: Engが構成図画像をサブAgentへアップ → CSP推定(Eng確認のワンクッション付き)→ 適用観点(共通+該当CSP)の全件判定 → 事実レポートを**ツールS1でスレッドへ自動投稿**。以降、親Agentはツール①経由でレポートを自動取得し、**■サービス一覧をSalus照合(ツール②の判断)に使用** |
| 2 | **出口(修正案モード)**: レビュー合意後、Engが合意事項をサブへ渡す → サブは**画像を再解析せず**、抽出モードの事実+合意事項から**構成図修正指示(文案)**を生成(不足サービス・矢印方向・ポート/CIDR記入など)→ S1で投稿し、Mgmt経由で利用者の構成図修正へ |
| 3 | サブAgentは**事実の抽出と合意事項の反映指示のみ**を行い、設計評価・ノックアウト判断はしない(判断は親AgentとEngの責任) |
| 4 | 構成図が無い案件・後日提出の案件は、サブAgentを飛ばして親Agentから開始(構成図はEng目視確認の注記付き) |
| 5 | 画像はチャット添付のみ(スレッド添付の自動読取は不可)。PPT/Excel/PDF内の図はスクリーンショット化して渡す |

## 登場コンポーネントの整理

| コンポーネント | ツール |
|---|---|
| 親: アーキテクチャレビューチェックAgent | ①Teamsスレッド内容取得 / ②ホスティング判断基準取得 / ③技術相談ナレッジ追記 / ④案件レビュー票作成 / ⑤案件レビュー票取得 |
| サブ: 構成図チェックAgent | S1 構成図チェック結果投稿 / S2 構成図チェック観点取得 |
