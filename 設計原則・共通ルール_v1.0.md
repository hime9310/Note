# アーキテクチャレビューAgent 設計原則・共通ルール

## 1. 設計原則(10か条)

| # | 原則 | 内容 |
|---|---|---|
| 1 | 親は連携専任 | 親Agentはツールを持たず、判断・作成をしない。窓口・進行管理・子Agent間のデータ連携(子の出力の保持と原文のままの受け渡し)・次の操作の案内・承認ゲートのみ |
| 2 | 1Agent=1責務 | 構成図Mermaid化 / 一覧抽出 / ホスティング判定 / 構成図チェック / アーキテクチャレビュー票作成 を分離し、モード判定ミスを構造的に排除する |
| 3 | 全件照合はツールで全文渡し | 判定基準・照合資料・票テンプレート・観点など「全行を漏れなくチェックする資料」はKnowledge(RAG)に載せず、ツールで全文を渡す。Knowledgeは断片検索で足りる「過去案件の参考提示」のみ |
| 4 | 人の承認ゲート | Agentの出力はすべて「案」。確定・承認・利用者連絡・ナレッジ登録の判断は人(Eng/Mgmt)が行う |
| 5 | 共通ルールは単一ソース | 検算ルール(R0〜R4)と共通厳守事項(C1〜C7)は本書を正とし、各AgentのInstructions末尾に同一の共通ブロックを貼付する |
| 6 | 1指示=1子Agent | Engの1つの入力に対して親が呼び出す子は1体・1回のみ。子の結果が出たら「次の操作」を案内して停止し、次の入力を待つ。将来、人の判断が要らない箇所(「完了」直後の一覧抽出)だけ自動連鎖に変更する余地を残す |
| 7 | 親はInstructions制御 | 進行制御は親のInstructions(生成オーケストレーション)で行い、トピック・グローバル変数・子の入力定義は使わない。子の**出力定義**だけは使う(出力が無いと親に要約しか返らないため)。決定性が足りない場合はトピック制御(v6.1設計)へ戻せるようにする |
| 8 | 画像を読むのは構成図Mermaid化Agentだけ | 構成図の画像/PDFは構成図Mermaid化Agentだけが読み、人が確認・訂正した**Mermaid+注釈情報**を後続の正とする。後続はテキスト処理のみ |
| 9 | 観点のロジックはデータに置く | 構成図チェックのロジック(適用条件・トリガー・ネクストアクション・AI可否)は観点表(Excel)の行に書き、構成図チェックAgentのInstructionsは列の読み方と汎用の手順だけを持つ。観点の追加・変更でInstructionsを変えない |
| 10 | 親の出力は「次の操作」で終わる | 人の確認や利用者の回答待ちで止まるとき、親の出力の末尾に「状況 → 指示語」の形で次に行うことを明示する(C7) |

---

## 2. 検算ルール(R0〜R4)

「検算」とは、**AIが処理を途中で飛ばしていないかを件数の突き合わせで機械的に確認する仕組み**。

| ID | 名称 | 実施者 | 何と何を突き合わせるか | 不一致時の動作 |
|---|---|---|---|---|
| **R0** | 一覧の抽出漏れ確認 | 構成図情報抽出Agent | ■サービス一覧の件数 ⇔ Mermaidのノード数(複製は1つに数え、枠・利用者・インターネット等の非サービス要素を除く) | 出力せずにやり直す |
| **R1** | 構成図チェック観点の実施漏れ確認 | 構成図チェックAgent | 出力した観点の件数(対象外を含む) ⇔ ツール②が返した「適用行数」(環境・CSP・1次/2次で絞った後の行数) | 出力せずにやり直す |
| **R2** | アーキテクチャレビュー票の標準指摘の仕分け漏れ確認 | アーキテクチャレビュー票作成Agent | ①流用+②添削+④不要 の合計件数 ⇔ テンプレートmdの全行数 | 出力せずにやり直す(③追記は分母に含めない) |
| **R3** | アーキテクチャレビュー票Excelへの転記漏れ確認 | アーキテクチャレビュー票作成Agent | ツール⑥が返した書き込み件数 ⇔ 載せるべき行数(①+②+③) | Engへ報告する |
| **R4** | 利用者回答の仕分け漏れ確認 | アーキテクチャレビュー票作成Agent | [回答済み]+[未回答]+[要確認] の合計件数 ⇔ ツール⑦が返した「全行数: NN件」 | 出力せずにやり直す |

---

## 3. 共通厳守事項(C1〜C7)

| ID | 内容 | 対象 |
|---|---|---|
| **C1** | すべての出力は「案(仮判定・下書き)」である。確定・承認・利用者への連絡・ナレッジ登録の実行判断は人(Eng/Mgmt)が行う | 全Agent |
| **C2** | 推測しない。渡された材料・ツール出力に無い事項を補完しない。判断・指摘には根拠(基準ID・観点No・資料名・記載箇所・ノードID)を必ず添える | 全Agent |
| **C3** | 全件処理する。照合・仕分け対象の全行を順に処理し、スキップしない。該当する検算ルール(R0〜R4)を実施し、不一致のまま出力しない | 全Agent |
| **C4** | 自分の役割の外に出ない。役割定義外の判断・作成を行わない。構成図の画像/PDFの解析は構成図Mermaid化Agentのみが行い、他のAgentはMermaid+注釈情報のテキストだけを読む | 全Agent |
| **C5** | 作業過程・計画・途中経過・ツール出力の原文はチャットに出力しない。最終成果物のみを返す(Engが原文を明示依頼した場合を除く) | 全Agent |
| **C6** | Web上の一般情報は使用しない | 全Agent |
| **C7** | 人の確認や利用者の回答待ちで止まるときは、出力の末尾に「次の操作」ブロック(状況 → 指示語、3〜4個以内。指示語は「依頼の判定」にある言葉だけ)を必ず付ける。「次はどうしますか」とは尋ねない | 親Agentのみ |

---

## 4. 共通ブロック(各子AgentのInstructions末尾にこのまま貼付。親はC7を追加した版を貼付)

```text
# 共通厳守事項D
- C1: すべての出力は「案(仮判定・下書き)」。確定・承認・利用者連絡・ナレッジ登録実行の判断は人(Eng/Mgmt)が行う。
- C2: 推測しない。渡された材料・ツール出力に無い事項を補完しない。判断・指摘には根拠(基準ID・観点No・資料名・記載箇所・ノードID)を必ず添える。
- C3: 全件処理する。照合・仕分け対象の全行を順に処理し、スキップしない。該当する検算ルール(R0〜R4)を実施し、不一致のまま出力しない。
- C4: 自分の役割の外に出ない。役割定義外の判断・作成を行わない。構成図の画像の解析は構成図Mermaid化Agentのみが行う。
- C5: 作業過程・途中経過・ツール出力の原文は出力しない。最終成果物のみを返す(Engが明示依頼した場合を除く)。
- C6: Web上の一般情報は使用しない。
```

※構成図Mermaid化AgentのInstructionsは現行のものを変更しないため、この共通ブロックは貼付しない(指示文内の「絶対に守ること」が同等の役割を担う)。

---

## 5. 用語(全文書で統一)

| 用語 | 定義 |
|---|---|
| 指示語 | Engが親へ送る決まった言葉。「RITMxxxをレビューして」「完了」「抽出して」「判定して」「再判断して」「構成図チェックして」「レビュー票を作成して」「レビュー票を見直して」「確定」「Excelを作成して」「回答を確認して」「2次チェックして」「ナレッジ文案を作成して」「承認」「中止」(加えて、Mermaid確認待ちでの修正内容、編集指示確認待ちでの修正点、判定への訂正・補足は自由文で入力する) |
| 次の操作 | 親の出力末尾のブロック。「状況 → 指示語」の形で次に行うことを示す(C7) |
| 1次チェック / 2次チェック | 構成図チェックの実行タイミング。1次=ホスティング判定後の初回。2次=利用者の回答・構成図修正の後 |
| 判定トリガー(標準) | 観点を実施する条件。3型: `常時` / `存在: 対象A/対象B`(サービス一覧に対象があれば実施) / `回答: 「票の項目」=値`(スレッドまたは回答済み票に該当の回答があれば実施)。2条件は「かつ」で結ぶ |
| 判定値 | 構成図チェックの結果。記載あり / 記載なし / 方向不明 / 判定不能 / 対象外 の5値 |
| ネクストアクション区分 | A 問題なし / B Eng目視確認 / C 利用者へ記載・修正依頼 / D アーキテクチャレビュー票の該当項目で確認 / E 対象外 |
| AI判定 | 観点表の列。○=構成図チェックAgentが判定を確定する / △=判定案と「人に確認してほしいこと」を出し、判定は確定しない(要人手確認) |
| テンプレート対象(No) | 観点表の列。その観点に対応するアーキテクチャレビュー票テンプレートの項目No。判定には使わず、票作成時に行の対応付けに使う |
| 編集指示 | アーキテクチャレビュー票の作り方。①流用(テンプレートNoのまま)/ ②添削(Noのまま内容変更)/ ③追記(テンプレート最後のNo+1から採番)/ ④不要(Excelには書かない) |
| 申し送り事項 | サービス提供対象リソース一覧で「利用者による動作検証が必要=○」のサービスについて、票へ追記する行 |
