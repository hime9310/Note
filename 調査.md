了解です。上から順に全コマンドを「何を見たか/結果/吹き出し文案」で書き出します。最初に1つだけ:**最後の集計コマンドはtypoが2箇所あり、目的の結果が取れていません**(⑧で説明・修正版あり)。

**① iam list-roles(Prisma/redlock名で検索)**
見たもの:IAMロール一覧から該当ロールの名前と作成日。
結果:2件ヒット。
吹き出し案:「Prisma関連ロールの棚卸し。redlockが使うGCS-Prisma-AWS-ReadWriteは2025/4/30に事前作成。スキャン開始(オンボード2026/4/20-21)とは別で、作成日≠課金開始日。2023年作成のReadOnlyロールは今回未使用」

**② lookup-events 1件+ARN抽出**
見たもの:redlockイベント1件の生ログから実行主体ARN。
結果:assumed-role/GCS-Prisma-AWS-ReadWrite/redlock
吹き出し案:「redlockの正体確認。ARNは assumed-role/ロール名/セッション名 の構造で、redlockはセッション名、実行ロールはGCS-Prisma-AWS-ReadWrite → redlock=Prisma Cloudと確定(本調査の決め手)」

**③ export2行+全イベント取得ループ**
見たもの:7/21の24時間(UTC)の全管理イベントをTSV保存。exportはスロットリング対策。
結果:東京59,975件/us-east-1 35,710件。
吹き出し案:「直近の総イベント数=比率の分母を実測。CE課金件数(東京5.5〜5.7万件/日)と約8%差で一致し、お金側(CE)とログ側(CloudTrail)が同じ量を測れていることを確認」

**④ ls -la**:「TSVの正常生成確認(東京4.4MB/us-east-1 2.7MB)」の1行でOK。

**⑤ 比率算出ループ**
見たもの:行数=総件数、Username列がredlockの行数、比率。
結果:東京55.8%(33,465/59,975)、us-east-1 57.2%(20,430/35,710)。
吹き出し案:「直近24時間でも過半がredlock。redlock単独33.5千件/日は導入前の東京全体(4.1千件/日)の約8倍 → 現在も同一要因が継続。※4月の9割(表6)は母数がGuardDuty APIのみで、本比率(全イベント母数)とは別物」

**⑥ redlockのAPI内訳TOP20**
見たもの:redlockが呼んだ「サービス×API」の回数ランキング。
結果:RDS Describe系最多、SSM/EC2/ACM/KMS/Lambda等、全て読み取り系。
吹き出し案:「中身は全てDescribe/List/Get系読み取りAPIで6サービス以上を横断=CSPM巡回スキャンの典型。RDS最多はDBパラメータグループ単位で呼ばれるため。LookupEvents456回はPrismaの監査ログ取込機能」

**⑦ 非redlockのUsername先頭10行(head)**
結果:s3-replication×9、EC2インスタンスID×1。
吹き出し案:「redlock以外の主体の当たり付け。s3-replication(DCS集中ログのレプリケーション)とEC2インスタンスロール=既存バックグラウンドの気配。先頭10行のみのため割合は次の集計で確定」

**⑧ 非redlock集計ループ(今回)→ 未完**
typoは2つ:`{print 4}`が列`$4`でなく数字4を出力(なので「4が26,510回」と表示)、ファイル名が東京固定で`${R}`になっていない(なのでus-east-1も同値)。副産物として**26,510=東京の非redlock総数**(59,975−33,465と一致)だけは確定。修正版:
```bash
for R in ap-northeast-1 us-east-1; do
  echo "======$R======"
  awk -F'\t' '$4!="redlock"{print $4}' all_events_${R}_20260721.tsv \
    | sort | uniq -c | sort -rn | head -10
done
```

**頭の整理用:因果の一本線**
```
Prismaオンボード(4/20-21)【Horio回答】
 → ロールGCS-Prisma-AWS-ReadWrite/セッションredlock【②】
 → 全リージョンで読み取りAPIを定期巡回【⑥・表4】
 → 管理イベント3.7万→40万件/日に段差増【CE日次・表5】
 →(二次)ログ増でs3-replication等も底上げ【⑦⑧】
 → GuardDutyが全件分析=EventsAnalyzed課金【表1】
 → $5→$45。500はPrisma未導入なので増えず
```
データ源は役割分担で覚えると混乱しません:**量とお金=Cost Explorer(表1〜5)、誰が=CloudTrail(表6+7/21TSV)、正体=IAM(①②)、時期の裏付け=担当者回答**。それぞれ独立した4系統が同じ結論を指している、というのが本調査の強さです。