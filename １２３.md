悩みどころはよく分かります。「構成図の情報を親の判断の**途中のあちこちで**使いたい。でもサブAgentを途中で何度も呼ぶ仕組みがない」— これが本質ですね。結論から言うと、**「途中で割り込ませる」のではなく「先に1回で全部読み取らせて、使う場所を親側で制御する」**設計にするのが正解です。

**考え方の転換: サブAgentの仕事は「チェック」ではなく「構造化レポートの作成」**

構成図チェックAgentを「指摘をくれる人」ではなく「**構成図をテキスト化してくれる人**」と捉え直します。出力を次の**固定セクションのレポート1本**にまとめさせます:

```
【A. 読取情報】サービス一覧 / CIDR・NW構成 / 接続関係 / 外部連携
【B. ノックアウト疑い】該当しそうな要素と根拠
【C. 構成上の指摘】推奨構成との差異・網羅性・修正依頼文案
【D. 読取不能箇所】Eng目視が必要な部分
```

**親との連携: 実行順を「構成図が先」にする**

```
Eng: 構成図画像をサブAgentへアップ
 → レポート(テキスト)を受領 → スレッドへ貼る
 → 親Agentへ「RITMxxxxxxxをレビューして」
 → 親はツール①でスレッドからレポートごと取得し、
    パイプラインの各段で「該当セクションだけ」を使う
```

「適切なところで効かせる」制御は、**親のInstructions側**に書きます:

- Salus照合の段(手順7)→ 「構成図レポートの**Aのサービス一覧**を照合対象に含める」
- ノックアウト判定の段(手順6)→ 「**AのCIDR・NW情報とB**を照合材料にする」
- レビュー票仕分けの段(手順8)→ 「**C**を③追記(案件固有指摘)の候補として評価する」

サブは1回動くだけですが、親から見れば「サービス一覧のところでも、ノックアウトのところでも、指摘のところでも、構成図の情報が効いてくる」— つまりご要望の動きになります。

**なぜ「途中で都度呼ぶ」方式を避けるか**

技術的には、Copilot Studioで親の実行途中にサブを呼ぶにはConnected agentsが必要ですが、①プレビューで環境未検証、②**画像を親経由でサブに渡せる保証がない**(そもそも親は画像を受け取らない設計)、③呼ぶタイミングの制御が不安定(オーケストレーター任せ)、と三重の不確実性があります。一方「先に読取→スレッド経由」なら、既存のツール①だけで成立し、再判断ループ(利用者回答後の再依頼)でもレポートが自動で再取得されるおまけ付きです。

**設計への反映ポイント**

1. サブAgentのInstructionsの出力フォーマットを上の**A〜D固定見出し**にする(親がセクションを確実に見つけられるよう、見出し文言を固定するのが肝です)
2. 親のInstructionsに「スレッド内に『【A. 読取情報】』で始まる構成図レポートがある場合、手順6でB、手順7でA、手順8でCを使う。無い場合は『構成図: Eng目視確認が必要』とする」を追記
3. 運用ルール: 「構成図チェック→貼付→レビュー依頼」の順を標準にする(構成図が後から来たら、貼ってから再依頼すればよい)

